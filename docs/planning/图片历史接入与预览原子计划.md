# 图片历史接入与预览原子计划

## 计划信息

- 计划 ID：`ATOMIC-IMAGE-HISTORY-UI-001`
- 父范围：`UNIT-10 / ATOM-36 / ATOM-37`
- 基线提交：`746788d`
- 执行模式：连续执行；每个原子独立定向验证、DDD 复核、提交并推送 `origin/main`。
- 用户验收点：全部原子完成后统一启动程序，验证图片捕获、历史恢复、图片卡片和删除清理。

## 大功能边界

本计划只完成以下闭环：

```text
Windows 图片剪贴板
  → ClipboardIO 复制一种拥有型 PNG/DIBV5/DIB 字节
  → ImageWorker 解码并耐久发布原图与缩略图
  → StorageExecutor 事务写入图片元数据
  → 历史查询/恢复返回图片摘要
  → UI 只为当前可见图片按需加载缩略图
```

明确不包含：

- 把历史图片重新写回系统剪贴板；该能力属于 `ATOM-38`。
- 图片 OCR、联网识别、预览大图、自定义图片目录设置界面。
- 全局界面重设计；图片卡片继承现有黑底白字、固定高度、明确操作区语法。

## 全局不变量

1. ClipboardIO 只在剪贴板打开期间复制一种拥有型 payload，跨类型优先级固定为
   `PNG > DIBV5 > DIB > Unicode 文本`；任何 `HGLOBAL`、`HBITMAP` 都不得跨线程。
2. 图片正式文件已经发布并回读验证后，才允许提交 SQLite。
3. SQLite 提交成功后才允许更新 UI；事务失败不得产生图片卡片。
4. 数据库失败时只回滚本次发布调用实际创建的资产，不删除此前已存在的同哈希资产。
5. 图片哈希冲突使用和文本一致的去重语义：保留原 ID、收藏、创建时间和资产身份，
   只更新最近复制时间、来源和饱和计数。
6. `ImageWorkerResult` 的 finalize 句柄必须恰好消费一次：数据库失败时 rollback；
   数据库成功且采用本次发布资产时 commit；数据库成功但复用旧资产时 rollback 本次
   资产。三个分支都必须等待 ACK，完成前不得开始下一张图片。
7. 历史查询 DTO 只携带图片元数据和可解析的缩略图定位信息，不携带原图字节。
8. 图片解码在线程外完成；Slint `Image` 只在 UI 事件线程创建和写入模型。
9. 未进入可见窗口的图片不得安排缩略图解码；快速滚动时旧代次结果必须作废。
10. 单条删除或清空事务提交后才回收无引用资产；回收失败不得回滚已经提交的历史事务，
    而是进入有界重试/后续对账边界。
11. ImageWorker 的捕获入口可以保持容量一 latest-wins；finalize、回收和重建属于不可
    覆盖的控制命令，必须使用容量有界且带背压的 FIFO。退出顺序固定为：停止新剪贴板
    图片输入 → 排空图片持久化和 finalize → 拒绝新资产控制命令并排空 FIFO/回收重试
    到规定边界 → 关闭历史查询/变更 worker → 停止 ImageWorker → 关闭 StorageExecutor。
12. 诊断日志不得记录剪贴板正文、图片字节、完整本地资产路径或窗口标题。

## 原子顺序

```text
IMG-HIST-01
  ↓
IMG-HIST-02
  ↓
IMG-HIST-03
  ↓
IMG-HIST-04
  ↓
IMG-UI-01
  ↓
IMG-UI-02
```

## IMG-HIST-01 图片事务与根注册

- 风险：L3（持久化事务、幂等和文件/数据库一致性）。
- 唯一目标：StorageExecutor 可以在一个事务中注册当前图片根并 upsert 完整图片元数据。
- 允许修改：`src/storage`、图片领域 DTO、存储定向测试。
- 禁止修改：ClipboardIO、ImageWorker、UI、删除行为。
- 输入：
  - `ImageRootSnapshot` 的根 ID、根类型和规范路径。
  - `ImageMetadata`、来源程序和 `copied_at`。
- 输出：
  - `ImageUpsertResult`，包含 UI/历史所需的最终数据库快照、mutation revision 和
    `adopted_published_assets`。
- 实现契约：
  - 同一事务先 upsert 根注册，再 upsert 图片行，再读取最终快照。
  - 同根 ID 允许更新为当前规范路径，以支持已验证的资产根移动；同路径不同根 ID
    必须由唯一约束拒绝并回滚。
  - 图片行必须让文本正文为 `NULL`，预览只保存稳定元信息文案。
  - 重复哈希不得改变既有资产定位、收藏状态和创建时间。
  - 只有最终数据库快照采用本次提交的根和相对路径时，
    `adopted_published_assets=true`；重复记录保留旧资产身份时必须返回 `false`。
- 定向验证：
  - 新图片、重复图片、根冲突、SQL 故障回滚、重启恢复。
  - `cargo test --lib storage::worker::tests::<图片相关测试名>`
  - `cargo clippy --lib -- -D warnings`
- 计划提交：`feat(storage): [IMG-HIST-01] 提交图片历史事务`

### IMG-HIST-01 完成记录

- 状态：已完成。
- 实现：新增图片根注册与图片历史同事务 upsert；显式区分新建和重复，重复刷新来源、
  复制时间和饱和计数，同时保留旧资产、收藏和创建时间。
- 关键结果：`adopted_published_assets` 只由 INSERT 是否创建新行决定，同根同路径重复
  也返回 `false`，供后续协调器准确消费 finalize。
- 验证：新图片、同根重复、跨根重复、根移动和路径身份冲突三个定向测试通过；
  `cargo clippy --lib -- -D warnings` 与 `git diff --check` 通过。
- DDD：第一轮发现重复采用判定和来源刷新两项问题；修正复测后第二轮 `PASS`。

## IMG-HIST-02 ClipboardIO 图片格式捕获

- 风险：L3（Win32 句柄生命周期、sequence 一致性和并发队列）。
- 唯一目标：ClipboardIO 对一次更新只复制一种最优图片编码，并以拥有型 DTO 发布。
- 允许修改：`src/clipboard/reader.rs`、`src/clipboard/io_worker.rs`、
  `src/history_bridge.rs` 的最小枚举匹配、相关导出和测试。
- 禁止修改：SQLite、ImageWorker 内部、UI；历史桥不得接入图片存储。
- 实现契约：
  - 在同一次打开/关闭剪贴板周期内按
    `PNG > DIBV5 > DIB > Unicode 文本` 选择并复制；一次捕获只发布一个 payload。
  - 捕获 DTO 使用受限枚举区分文本和图片编码；Debug 不输出字节。
  - 读取前后 sequence 一致，关闭失败优先。
  - 允许最小修改唯一历史消费者：显式匹配新枚举；文本继续原路径，图片在
    `IMG-HIST-03` 前安全返回 `Skipped`，不得按文本调用 `summary/as_text`。
- 定向验证：
  - PNG 优先、DIBV5 回退、DIB 回退、文本保持、超限、sequence 改变和关闭失败。
- 计划提交：`feat(clipboard): [IMG-HIST-02] 捕获拥有型图片编码`

### IMG-HIST-02 完成记录

- 状态：已完成。
- 实现：新增拥有型文本/图片捕获枚举；ClipboardIO 在单次打开周期按
  `PNG > DIBV5 > DIB > Unicode` 选择唯一 payload；旧文本 request 和自身写回抑制保持。
- 中间边界：图片在 IMG-HIST-03 前由历史桥返回 `Skipped`，不会调用文本 upsert 或 UI。
- 验证：23 个 reader 定向测试、15 个 ClipboardIO 队列测试和图片安全跳过测试通过；
  `cargo clippy --lib -- -D warnings` 与 `git diff --check` 通过。
- DDD：第一轮发现 PNG 格式注册失败被误判为格式不可用；新增独立错误和故障注入测试后
  第二轮 `PASS`。

## IMG-HIST-03 图片持久化协调器

- 风险：L3（跨 worker 顺序、finalize 恰好一次和退出排空）。
- 唯一目标：把图片捕获串接到 ImageWorker 和图片存储事务，并在提交后发布唯一 UI 事件。
- 允许修改：`src/history_bridge.rs`、`src/main.rs`、ImageWorker 接线、
  `UiClipboardItem` 的最小条目类型/复制能力字段、Slint 复制按钮门禁和集成测试。
- 禁止修改：图片卡片渲染、图片写回剪贴板。
- 实现契约：
  - 文本捕获继续走现有路径；图片捕获提交 ImageWorker 后等待结果。
  - upsert 成功且 `adopted_published_assets=true` 时 commit finalize；upsert 成功但保留
    旧资产身份时 rollback 本次发布资产；upsert 失败同样 rollback。三个分支均恰好消费
    一次 finalize，只有数据库成功才投递 UI。
  - finalize 失败进入有限错误边界且不伪造成功卡片。
  - 图片 UI 摘要从首次投递起就携带 `copy_enabled=false`；复制按钮显示“待支持”且不可
    点击，不能把门禁推迟到后续 UI 原子。
  - 启动时使用根快照注册；退出时按全局不变量排空。
- 定向验证：
  - 成功顺序、DB 失败回滚、UI 关闭继续提交、worker/通道失败、退出排空。
- 计划提交：`feat(history): [IMG-HIST-03] 接入图片持久化协调器`

## IMG-HIST-04 图片删除与清空回收

- 风险：L3（删除提交和文件副作用的一致性）。
- 唯一目标：图片记录删除或清空提交后，异步回收已经无数据库引用的原图和缩略图。
- 允许修改：存储删除结果、历史变更桥、ImageWorker 资产命令和定向测试。
- 禁止修改：图片卡片、图片写回。
- 实现契约：
  - 删除事务返回待回收的受限资产引用；事务前不得删文件。
  - 删除同哈希不存在记录保持幂等。
  - 清空只回收事务实际删除且提交后已无引用的图片。
  - 回收路径必须由已注册根和领域相对路径重建并再次约束在固定子树内。
  - 回收失败不恢复数据库行；进入容量有界、次数有界且带退避的进程内重试队列。
    正常退出前排空到重试上限并记录放弃数量；更长期的磁盘对账仍由 `ATOM-49` 收敛。
  - 发布、回收和后续缩略图重建都由独占 `PreparedImageStorage` 的 ImageWorker 串行
    执行，禁止第二个组件自行拼接可写资产路径。
  - 回收进入不可覆盖的有界 FIFO；生产者按批次背压提交，清空图片数量超过队列容量时
    不得截断或覆盖，关闭时必须先排空 FIFO 和有限重试再 join。
- 定向验证：
  - 单条图片删除、文本删除不碰文件、清空混合类型、事务失败不回收、重复回收。
- 计划提交：`feat(history): [IMG-HIST-04] 回收无引用图片资产`

## IMG-UI-01 图片摘要与历史恢复

- 风险：L2（跨存储/UI DTO 接缝）。
- 唯一目标：查询、分页和启动恢复完整传递图片摘要与缩略图定位，不加载图片像素。
- 允许修改：历史查询、恢复、UI DTO、筛选和定向测试。
- 禁止修改：Slint 图片对象、缩略图文件解码。
- 实现契约：
  - `UiClipboardItem` 显式携带条目类型、图片尺寸和受限缩略图定位。
  - “全部”查询允许返回混合摘要；本原子不增加图片筛选标签，显式图片筛选仍属于
    `ATOM-39`。
  - 图片摘要沿用 `IMG-HIST-03` 已建立的 `copy_enabled=false` 门禁，不重复改变交互。
  - 坏根、坏相对路径或缺字段使该页失败，不把不可定位图片伪装成文本。
  - 文本既有行为、分页游标、选中身份和迟到结果门禁保持不变。
- 定向验证：
  - 混合列表、收藏筛选、分页、重启恢复、坏图片 DTO、图片复制门禁。
- 计划提交：`feat(history): [IMG-UI-01] 传递图片历史摘要`

## IMG-UI-02 可见窗口缩略图卡片

- 风险：L3（异步图片解码、UI 线程所有权和滚动竞态）。
- 唯一目标：固定高度卡片只为可见图片加载缩略图，并在 UI 线程创建 Slint Image。
- 允许修改：缩略图加载器、ImageWorker 的 `RebuildThumbnail` 命令/API、
  `src/app/ui_event.rs`、`ui/app-window.slint`、UI 定向测试。
- 禁止修改：原图预览、图片写回、全局主题和面板尺寸。
- 实现契约：
  - Rust 根据 ListView 可见范围提交最多一屏加小缓冲的图片请求；面板打开、模型
    replace/append、删除/过滤、可见高度变化和 viewport 变化都必须进入同一个
    “可见性重算”入口，递增视口代次并做请求差分。
  - 缩略图加载 worker 只读 WebP 并返回有界 RGBA8 像素 DTO；不得创建 Slint 对象。
  - 请求携带卡片稳定身份和视口代次；滚出视口或条目变化后的迟到结果丢弃。
  - UI 事件线程使用像素 DTO 构造 `slint::Image`；缓存有明确条目数上限。
  - 图片卡片按已批准规范使用约 176px 固定内容高度，文本仍为 96px；窗口化列表用
    条目类型对应的已知固定高度维护可见范围，不按原图尺寸测量。缩略图使用固定预览区
    并保持比例裁剪；缺失、加载中和失败显示稳定占位。
  - 缩略图缺失时由协调器先确认数据库仍引用稳定资产，再向独占存储 capability 的
    ImageWorker 提交按身份去重的 `RebuildThumbnail`；ImageWorker 从受限 original
    解码并通过 staging 原子重建 WebP。结果返回后协调器再次复核数据库引用：仍存在才
    对当前视口代次重新加载，已删除则把回收命令排在同一 ImageWorker 串行队列中。
  - 图片复制门禁沿用 `IMG-UI-01`，不得在本原子误启用。
  - 重建命令进入不可覆盖控制 FIFO；与捕获 latest-wins 槽隔离，停止阶段随回收命令
    一起排空。
- 定向验证：
  - 可见/不可见解码、快速滚动旧结果、坏 WebP、缺文件、横图/竖图/小图、2,000 条
    混合模型、控制命令与捕获竞争、退出排空。
  - Windows Debug 构建和真实桌面启动；最终视觉/交互由用户统一验证。
- 计划提交：`feat(ui): [IMG-UI-02] 展示懒加载图片卡片`

## 完成定义

- 六个子原子全部独立提交并推送。
- `ATOM-36`、`ATOM-37` 和本计划状态回写为完成。
- 定向测试、Windows 构建、中文注释检查和最终 DDD 通过。
- 程序已启动，用户可以复制截图后验证：
  1. 图片记录出现在顶部；
  2. 重复图片只保留一条并置顶；
  3. 关闭再启动仍能恢复图片卡片；
  4. 长列表滚动时只加载可见缩略图且不卡顿；
  5. 删除图片后卡片消失，资产进入回收；
  6. 图片复制按钮本轮不执行写回，等待 `ATOM-38`。

## 计划质疑记录

- 风险等级：L3。
- 三轮隔离审查已完成。
- 已修正：越界图片筛选、中间提交复制门禁、重复图片资产采用分支、ClipboardIO
  消费者兼容、跨类型格式优先级、首次可见触发、缩略图重建 capability、回收重试、
  混合固定高度和资产控制命令不可覆盖 FIFO。
- 最终核对：第三轮剩余三项均为计划契约内部矛盾，已按最小修正统一；没有产品范围
  选择或未解决的实质问题，DDD 路由结果为 `PASS`。
