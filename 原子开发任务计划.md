# Windows 轻量剪贴板工具原子开发任务计划

> 本文档是项目开发期间的唯一原子任务清单。开发时必须按依赖顺序逐项执行、验证、提交和更新状态，不得依赖聊天记录记忆任务，也不得把多个原子合并开发。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 计划编号 | `WCB-PLAN-001` |
| 计划版本 | `v55` |
| 工作名称 | `ClipboardBoard` |
| 项目目录 | `F:\workspace\small-projects\windows-copy` |
| 生成基线 | 空目录、尚未初始化 Git |
| 目标平台 | Windows 10/11 x64 |
| 技术栈 | Rust、Slint、windows-rs、rusqlite、image、BLAKE3、serde、tracing |
| 默认快捷键 | `Alt + V` |
| Git 策略 | 每个原子独立提交并推送 `origin/main`；用户已明确授权远端同步 |
| 当前原子 | `ATOM-30 / PERSIST-02` 定义图片领域元数据 |
| 当前状态 | PERSIST-01 已完成并推送；PERSIST-02 正在执行 |

## 2. 状态标记

- `[ ]`：尚未开始。
- `[-]`：正在执行，同一时刻只能有一个。
- `[x]`：已验证并完成本地提交。
- `[!]`：遇到阻塞，必须在执行记录中说明。
- `[~]`：仓库现状已经满足，无需产生空提交，但必须记录证据。

更新规则：

1. 开始原子前，将该原子改为 `[-]`，同时更新“当前原子”。
2. 只修改该原子允许范围内的文件。
3. 验证、差异审查和必要的风险质疑通过后创建独立本地提交。
4. 写入提交哈希、验证证据和实际变更，再将原子改为 `[x]`。
5. 当前原子完成前，不得把下一个原子标为 `[-]`。
6. 计划需要调整时先更新本文档版本和变更记录，不得在代码中悄悄扩大范围。

## 3. 产品目标

通过全局快捷键呼出的轻量 Windows 剪贴板工作台，支持文本和图片历史、搜索、筛选、收藏、删除、清空、显式复制、持续置顶、托盘、暂停、排除程序、开机启动、无限滚动、图片懒加载和本地数据清理。

核心结果：

- 快捷键呼出，最近内容立即显示。
- 文本和图片都能保存、预览和重新复制，用户在目标应用中手动按 `Ctrl + V` 粘贴。
- 鼠标操作优先，键盘快捷键作为辅助。
- 后台内存和 CPU 占用低。
- 所有数据默认只保存在本机。
- 黑底白字、固定高度卡片和简洁的浮动面板。

明确排除：

- 云同步、用户账号和团队共享。
- OCR、AI 分类和插件系统。
- 在线 URL 标题解析。
- 完整富文本还原。
- 文件、音频和视频历史。
- macOS、Linux 和复杂主题。
- 自动更新和代码签名。

## 4. 默认产品参数

| 参数 | 默认值 |
|---|---:|
| 普通历史上限 | 2,000 条 |
| 普通历史保留 | 30 天 |
| 图片空间上限 | 500 MiB |
| 最近摘要缓存 | 100 条 |
| UI 首批结果 | 30 条 |
| 后续加载 | 每批 50 条 |
| 缩略图最长边 | 320 px |
| 搜索防抖 | 120 ms |
| 文本内容上限 | 5 MiB |
| 收藏清理策略 | 不自动清理 |
| 窗口尺寸 | 560 × 640 |

性能验收目标：

| 指标 | 目标 |
|---|---:|
| 后台待机内存 | ≤ 30 MiB |
| 面板打开后内存 | ≤ 60 MiB |
| 快捷键呼出 P95 | ≤ 100 ms |
| 首批结果显示 | ≤ 50 ms |
| 1,000 条文本搜索 | ≤ 30 ms |
| 空闲 CPU | 接近 0% |
| 长列表滚动 | 不冻结，接近 60 FPS |

## 5. 全局架构与不变量

### 5.1 线程边界

```text
Win32 消息线程
    │ 只接收 WM_HOTKEY / WM_CLIPBOARDUPDATE / 托盘消息
    ▼
ClipboardIoWorker
    │ 打开剪贴板、有界复制数据、关闭剪贴板
    ├───────────────┐
    ▼               ▼
ImageWorker     StorageExecutor
图片解析/编码    唯一 SQLite 连接
文件持久化       迁移、CRUD、搜索、清理
    └───────┬───────┘
            ▼
         UiEvent
            ▼
Slint 事件线程
唯一允许修改 UI 模型和创建 Slint Image 的线程
```

必须始终成立：

1. UI 只能在 Slint 事件线程更新。
2. 所有非 UI 线程只能通过不可变 DTO 和 `UiEvent` 投递结果。
3. Win32 消息线程只处理轻量事件，不读取正文、不压缩图片、不访问 SQLite。
4. `ClipboardIoWorker` 必须在关闭剪贴板之前复制出拥有所有权的数据。
5. `ImageWorker` 不得持有 `HGLOBAL`、`HBITMAP` 或 Slint `Image`。
6. `StorageExecutor` 是唯一 SQLite 连接所有者。
7. 引入 SQLite 后，新增、收藏、删除、清空等状态必须事务提交成功后才更新 UI。
8. 后台不得轮询剪贴板。

### 5.2 剪贴板一致性

1. 事件队列必须有界并采用 `latest-wins`。
2. 打开剪贴板前和复制完成后必须核对 sequence。
3. sequence 失配时丢弃本次读取，不得把新内容记到旧来源名下。
4. 应用自身写回使用两阶段预期写事务：
   - 写入前登记规范哈希和格式集合。
   - 写入后绑定精确 sequence。
   - 监听端只能消费一次。
   - sequence 被越过或超时后立即失效。
5. 自身写回不得增加 `copy_count`、更新时间或改变排序。
6. 单次事件只生成一种内容，优先级为：
   - 注册 PNG。
   - `CF_DIBV5`。
   - `CF_DIB`。
   - `CF_BITMAP`。
   - `CF_UNICODETEXT`。

### 5.3 数据与图片一致性

1. SQLite 是历史记录和元数据真相源。
2. original PNG 是不可重建的耐久图片 payload。
3. thumbnail WebP 是可重新生成的派生缓存。
4. 图片规范哈希基于：
   - 固定域标签。
   - 图片宽高。
   - 顶向下、行连续的 RGBA8 像素。
5. 图片必须先写入独占 staging 文件。
6. 文件 flush、关闭、原子发布并验证后，才能提交 SQLite 记录。
7. 数据库提交失败时只能删除本次捕获确实拥有的文件。
8. 删除记录提交后，才能排队清理无引用资产。
9. 对账前必须经过 StorageExecutor 屏障，并排除 staging、in-flight 和 recovery。
10. 自动清理永不删除收藏；明确执行“清空全部”时可以删除收藏。

### 5.4 交互、复制与分页安全边界

1. 面板显示后必须保持 Windows 置顶；点击其他应用不隐藏；`Esc` 或再次按
   `Alt + V` 明确隐藏到系统托盘。
2. 已显示时从托盘或二次启动请求打开，只重新显示、激活并断言置顶；`Alt + V`
   独立使用显隐开关语义，隐藏时完整取消当前搜索和分页会话。
3. 应用只把选中内容写入系统剪贴板，不得调用 `SendInput`、模拟 `Ctrl + V` 或向目标应用注入其他输入。
4. 鼠标点击卡片是主要选择方式，卡片复制按钮是唯一复制入口；键盘只保留上下选择和 `Esc` 隐藏。
5. 每次选择和复制必须同步携带当前面板代次、记录 ID 与严格 32 字节内容哈希；迟到事件不得作用于新面板或已替换卡片。
6. 复制请求继续使用非阻塞、有界且可关闭的后台队列；关闭开始后不得接收新请求，已经出队的结果只能通过身份门禁决定是否应用。
7. 历史和搜索使用 `copied_at DESC, id DESC` 复合游标分页，首批 30 条、后续每批 50 条，禁止使用大 `OFFSET`。
8. 分页响应必须携带查询 generation、请求 token 和精确起始游标；新搜索、新捕获、隐藏或后续历史变更必须使旧结果失效。
9. 分页游标只在成功追加后推进；失败后不得自动重试风暴，必须等待用户显式重试或重新进入滚动触发区。
10. UI 已加载历史不得超过普通历史上限 2,000 条；最后一页请求数量必须按剩余名额收窄。

### 5.5 中文注释门禁

每个原子都必须检查：

- Rust 文件顶部存在中文文件级 `//!` 或等价说明。
- Slint、PowerShell、SQL 和安装脚本包含中文文件职责说明。
- 类型、结构体字段、枚举、方法、公共 API、参数、返回值、异常和副作用有中文注释。
- 并发、权限、内存所有权、重试、清理、格式解析和不直观分支有中文设计说明。
- 修改行为时同步更新相关注释。
- `TODO`、`FIXME` 必须写明中文原因、当前限制和后续目标。

### 5.6 每个原子的统一完成定义

每个原子只有满足以下全部条件才能标记完成：

- [ ] 唯一目标已经实现。
- [ ] 未提前实现后续原子。
- [ ] 针对性测试通过。
- [ ] `cargo fmt --check` 通过。
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` 通过。
- [ ] 按风险运行的必要回归测试通过。
- [ ] 中文注释静态检查和人工差异检查通过。
- [ ] 完整 diff 没有调试代码、无关格式化或用户修改。
- [ ] L2/L3 原子的风险质疑结果为 `PASS`。
- [ ] 创建了一个独立本地提交。
- [ ] 没有未经授权执行 `git push`。
- [ ] 本文档中的状态和执行记录已经更新。

## 6. 产品交付顺序

| 顺序 | 交付单元 | 用户可观察结果 | 原子 |
|---:|---|---|---|
| 1 | UNIT-01 快捷面板 | Alt+V 呼出和关闭面板 | 01–05 |
| 2 | UNIT-02 托盘控制 | 从托盘打开和退出 | 06–07 |
| 3 | UNIT-03 文本捕获 | 复制文本进入最近历史 | 08–12 |
| 4 | UNIT-04 文本去重 | 重复内容合并并置顶 | 13 |
| 5 | UNIT-05 历史持久化 | 重启后恢复历史 | 14–18 |
| 6 | UNIT-06 初版复制 | 键盘选中并复制历史内容 | 19–20 |
| 7 | UNIT-07 历史自动粘贴实现 | 已完成但由 ATOM-25R 撤销，不再作为产品能力 | 21–22 |
| 8 | UNIT-08 搜索筛选 | 输入关键词和类别筛选 | 23–25、25R、39 |
| 9 | UNIT-08R 交互基线修正 | 置顶持续显示、鼠标选择、显式复制、SQLite 首批与滚动续页 | 25R / WCB-INT-01–10 |
| 10 | UNIT-09 历史管理 | 收藏、删除和清空 | 26–29 |
| 11 | UNIT-10 图片捕获 | 捕获并展示图片 | 30–37 |
| 12 | UNIT-11 图片复用 | 重新复制图片并由用户手动粘贴 | 38 |
| 13 | UNIT-12 大历史浏览 | 扩展图片兼容分页、缩略图 LRU 和性能门禁 | 40–43 |
| 14 | UNIT-13 暂停记录 | 临时或无限期暂停 | 44–45 |
| 15 | UNIT-14 程序排除 | 指定程序内容不被读取 | 46 |
| 16 | UNIT-15 自动清理 | 控制记录数量、天数和图片空间 | 47–49 |
| 17 | UNIT-16 设置 | 历史、隐私、热键和开机启动 | 50–53 |
| 18 | UNIT-17 固定窗口 | 拖动、调整尺寸和固定位置 | 54 |
| 19 | UNIT-18 损坏恢复 | 隔离损坏数据并安全重建 | 55 |
| 20 | UNIT-19 正式发布 | 图标、性能、安装和发布验收 | 56–60 |

## 7. 原子任务清单

### 第一阶段：程序外壳和窗口闭环

#### [x] ATOM-01 建立可构建的 Slint 项目骨架

- 所属交付单元：UNIT-01。
- 唯一目标：得到一个可构建、可运行的黑底白字 Slint Windows 窗口。
- 前置条件：项目代码目录为空；项目文档已由本地基线提交保护。
- 允许修改：
  - 初始化本地 Git。
  - `.gitignore`。
  - `Cargo.toml`。
  - `build.rs`。
  - `src/lib.rs`。
  - `src/main.rs`。
  - `ui/app-window.slint`。
  - 中文注释检查脚本及其测试入口。
- 实现契约：
  - 创建单个 Rust 二进制包，并保留可测试的 library 入口。
  - 使用 `slint-build` 编译 `.slint` 文件。
  - 默认窗口尺寸为 560×640。
  - UI 使用黑色背景、白色主文字和灰色辅助文字。
  - 建立 Rust、Slint 和 Windows 生成文件的 `.gitignore`。
- 明确不修改：不实现热键、托盘、剪贴板、数据库或图片。
- 验收：
  - `cargo check`。
  - `cargo test`。
  - `cargo run` 后窗口可见。
  - 中文文件级注释检查通过。
- 停止条件：
  - Git 缺少 `user.name` 或 `user.email`。
  - Rust MSVC 链接器不可用且无法在当前环境构建。
- 风险等级：L1。
- 计划提交：`feat(app): 建立可运行的黑色看板骨架`。

#### [x] ATOM-02 建立唯一 UI 事件入口

- 所属交付单元：UNIT-01。
- 唯一目标：非 UI 线程可以安全地向 Slint 线程投递不可变结果。
- 依赖：ATOM-01。
- 允许修改：`src/app/`、`src/command.rs`、必要测试。
- 实现契约：
  - 定义 `UiEvent` 和 UI DTO。
  - 通过 `slint::invoke_from_event_loop` 进入 UI 线程。
  - UI 模型的可变引用不得离开 UI 线程。
  - 因 ATOM-01 暂无可变 Slint 属性，本原子以 UI 线程专属 Rust `UiState` 作为可观察 reducer；后续 DTO 到 Slint 类型的转换仍必须留在同一投递闭包内。
- 明确不修改：不创建 Win32 消息线程，不实现剪贴板。
- 验收：后台线程投递后，测试或调试断言证明模型更新发生在 UI 线程。
- 停止条件：Slint 当前版本无法提供可靠事件循环投递接缝时重新规划。
- 风险等级：L2，编码前和提交前执行 DDD。
- 计划提交：`feat(app): 建立线程安全的UI事件入口`。

#### [x] ATOM-03 注册 Alt+V 全局快捷键

- 所属交付单元：UNIT-01。
- 唯一目标：应用运行时按 `Alt + V` 能打开面板。
- 依赖：ATOM-02。
- 允许修改：`src/platform/windows/system_window.rs`、`hotkey.rs`、`src/command.rs`/`src/app/` 中为 `UiEvent::OpenPanel` 提供的最小接缝、应用启动接线和测试。
- 实现契约：
  - 创建隐藏 Win32 消息窗口。
  - 调用 `RegisterHotKey`。
  - `WM_HOTKEY` 只转换为 `UiEvent::OpenPanel`。
  - 注册冲突必须显示明确错误。
- 明确不修改：不监听剪贴板，不实现快捷键设置。
- 验收：实际按键可打开窗口；占用热键时不静默失败。
- 风险等级：L2。
- 计划提交：`feat(hotkey): 支持Alt+V呼出面板`。

#### [x] ATOM-04 保证程序单实例运行

- 所属交付单元：UNIT-01。
- 唯一目标：同一用户只能运行一个后台实例，二次启动只唤起主实例。
- 依赖：ATOM-03。
- 允许修改：单实例模块、启动顺序和进程级测试。
- 实现契约：
  - 在初始化 UI、SQLite 和热键前获取用户级命名互斥体。
  - 二次启动向主实例发送打开面板消息后退出。
  - 不依赖远端服务或管理员权限。
  - 互斥体使用 `Local\\ClipboardBoard.Instance.v1`，主实例必须持有 RAII guard 直到事件循环和热键线程完成关闭。
  - IPC 使用现有 message-only HWND 和编译期固定的打开消息，WndProc 只能投递 `UiEvent::OpenPanel`，禁止直接访问 UI 或业务状态。
  - 二次启动在互斥体已存在但 HWND 尚未就绪时以有界重试查找并发送；通知失败必须保留操作和错误码并返回非零退出码。
  - 如通知窗口在竞态期间消失，二次进程应再次尝试获取互斥体，成功后才能接管为主实例。
- 验收：连续启动两个进程时只有一个热键注册和一个长期存活进程。
- 针对性测试：覆盖 `ERROR_ALREADY_EXISTS` 分支、通知重试、通知失败、主进程退出后新进程重新获取互斥体。
- 风险等级：L3，涉及跨进程并发。
- 计划提交：`feat(app): 保证应用单实例运行`。

#### [x] ATOM-05 完成临时面板生命周期

- 所属交付单元：UNIT-01。
- 唯一目标：面板在正确显示器打开，并按临时窗口规则关闭。
- 依赖：ATOM-04。
- 允许修改：`src/platform/windows/window/`、UI 回调和窗口测试。
- 实现契约：
  - 面板激活前保存原目标 HWND、PID 和完整性级别。
  - 使用鼠标所在显示器工作区计算窗口位置。
  - 再次按热键、按 Esc 或失焦时隐藏。
  - 隐藏不退出后台进程。
  - 原目标只允许来自面板显示前的前台窗口；面板自身 HWND、无效 HWND 或身份变化不得被记录为目标。
  - 位置计算使用物理像素工作区并以面板尺寸居中，显示器 API 失败时回退到当前窗口位置且不得退出后台进程。
  - 失焦隐藏必须通过 UI 线程事件或窗口回调进入，不能从 Win32 回调直接访问 Slint 对象。
  - UI 线程为每次打开生成递增 generation；隐藏事件只有匹配当前 generation 且面板仍可见时才生效，避免旧 Esc/失焦事件关闭新一代面板。
  - 本原子只保存 HWND、PID、完整性级别快照，不执行粘贴；后续自动粘贴原子必须重新核对快照，身份不匹配时只能复制而不能注入按键。
- 明确不修改：不执行自动粘贴，不实现固定模式。
- 验收：单屏、多屏和不同缩放下位置正确；保存目标不是面板自身。
- 风险等级：L2。
- 计划提交：`feat(window): 完成临时面板显示与隐藏`。

### 第二阶段：托盘和诊断

#### [x] ATOM-06 增加系统托盘入口

- 所属交付单元：UNIT-02。
- 唯一目标：用户可从托盘打开或退出应用。
- 依赖：ATOM-05。
- 允许修改：`src/platform/windows/tray.rs`、系统窗口消息分发、UI 事件契约与 reducer、托盘错误枚举、Windows Shell feature、临时托盘图标及对应测试/注释清单。
- 实现契约：
  - 使用 Win32 托盘接口。
  - 菜单只包含“打开”和“退出”。
  - 退出时注销热键、移除托盘图标并停止线程。
- 验收：面板隐藏后仍可打开；退出无残留进程或托盘图标。
- 风险等级：L2。
- 计划提交：`feat(tray): 增加托盘打开与退出操作`。

#### [x] ATOM-07 建立不记录正文的诊断日志

- 所属交付单元：UNIT-02。
- 唯一目标：应用可以诊断事件和错误，同时不记录剪贴板正文。
- 依赖：ATOM-06。
- 允许修改：日志初始化、事件字段、日志测试。
- 实现契约：
  - 只允许记录记录 ID、类型、字节数、耗时、错误码和线程状态。
  - 禁止记录文本正文、图片字节和完整窗口标题。
- 验收：复制特征字符串后扫描日志，正文不出现。
- 风险等级：L2，涉及隐私。
- 计划提交：`feat(log): 建立隐私安全的诊断日志`。

### 第三阶段：文本剪贴板闭环

#### [x] ATOM-08 定义文本记录领域模型

- 所属交付单元：UNIT-03。
- 唯一目标：以稳定领域模型表达文本记录摘要和完整 payload。
- 依赖：ATOM-07。
- 允许修改：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/domain/mod.rs`、`src/domain/clipboard_item.rs`、`src/domain/hash.rs`、注释清单和单元测试；仅为接入 BLAKE3 依赖与公开领域模块增加模块 wiring，不扩展业务行为。
- 实现契约：
  - 拆分 `ClipboardItemSummary` 和 `ClipboardPayload`。
  - 文本规范化只去除尾部 NUL，保留正常空格、换行和制表符。
  - 使用带文本域标签的 BLAKE3 哈希。
  - 摘要长度有上限，不把完整大文本交给列表模型。
- 验收：哈希稳定性、NUL、空白、Unicode 和摘要截断测试通过。
- 风险等级：L1。
- 计划提交：`feat(domain): 定义文本剪贴板记录模型`。

#### [x] ATOM-09 读取复制来源程序

- 所属交付单元：UNIT-03。
- 唯一目标：记录复制事件发生时的来源 exe 和显示名称。
- 依赖：ATOM-08。
- 允许修改：`src/platform/windows/source.rs`、Win32 抽象和测试。
- 实现契约：
  - 在事件产生时读取前台 HWND 和 PID。
  - 使用有限权限打开进程。
  - 默认不读取窗口标题。
  - 进程退出或拒绝访问时返回无来源，不影响剪贴板捕获。
- 验收：普通程序、进程退出和访问失败用例通过。
- 风险等级：L2。
- 计划提交：`feat(source): 识别剪贴板来源程序`。

#### [x] ATOM-10 在 ClipboardIO 线程读取 Unicode 文本

- 所属交付单元：UNIT-03。
- 唯一目标：安全、有限地读取 `CF_UNICODETEXT`。
- 依赖：ATOM-09。
- 允许修改：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/clipboard/mod.rs`、`src/clipboard/io_worker.rs`、`src/clipboard/reader.rs`、Win32 DataExchange/Memory feature、注释清单和测试；仅为模块 wiring 与原生剪贴板 API 绑定增加范围，不接入 WM_CLIPBOARDUPDATE。
- 实现契约：
  - 建立专用 `ClipboardIoWorker`。
  - `OpenClipboard` 使用总时长受限的重试。
  - 读取前后核对 sequence。
  - 在关闭剪贴板前复制到 Rust 自有字符串。
  - 默认文本上限为 5 MiB。
- 验收：中英文、换行、空内容、忙碌剪贴板、失配 sequence 和超限文本测试通过。
- 风险等级：L3。
- 计划提交：`feat(clipboard): 在线程中安全读取Unicode文本`。

#### [x] ATOM-11 接入 WM_CLIPBOARDUPDATE

- 所属交付单元：UNIT-03。
- 唯一目标：剪贴板变化能够可靠触发一次内容捕获。
- 依赖：ATOM-10。
- 允许修改：`src/platform/windows/system_window.rs`、`src/platform/windows/hotkey.rs`、`src/platform/windows/mod.rs`、`src/clipboard/mod.rs`、`src/clipboard/io_worker.rs`、监听器接线、结果桥、事件队列和并发测试；不扩展到 UI、存储或图片业务。
- 实现契约：
  - 注册 `AddClipboardFormatListener`。
  - 消息线程只捕获 sequence 和来源快照并入队。
  - 队列有界且采用 `latest-wins`。
  - worker 发现 sequence 失配时丢弃。
  - 捕获结果必须通过容量为一的 `ClipboardCaptureInbox` 离开消息线程，后续原子可以非阻塞消费正文/来源/错误。
- 验收：快速复制 A、B 不会把 B 记到 A 的来源名下；结果桥可观察到最新成功结果或 sequence 失配错误。
- 风险等级：L3。
- 计划提交：`feat(clipboard): 监听Windows剪贴板更新`。

#### [x] ATOM-12 将文本结果展示在列表顶部

- 所属交付单元：UNIT-03。
- 唯一目标：复制的文本以摘要卡片显示在列表顶部。
- 依赖：ATOM-11。
- 允许修改：`ClipboardCaptureInbox` 的消费接线、`UiEvent` 投递、UI 数据转换、文本卡片、列表模型和 UI 测试。
- 实现契约：
  - worker 结果必须通过 `UiEvent`。
  - 结果从 ATOM-11 暴露的 `ClipboardCaptureInbox` 非阻塞消费，不能访问私有消息线程状态。
  - 卡片显示摘要、来源和相对时间。
  - 列表不得持有完整大文本 payload。
- 明确不修改：当前阶段只使用内存列表，不访问 SQLite。
- 验收：复制后新摘要显示在顶部，线程断言通过。
- 风险等级：L2。
- 计划提交：`feat(history): 展示最近复制的文本`。

#### [x] ATOM-13 合并重复文本

- 所属交付单元：UNIT-04。
- 唯一目标：重复文本不产生重复卡片。
- 依赖：ATOM-12。
- 允许修改：内存历史协调器和去重测试。
- 实现契约：
  - 相同规范哈希只更新时间、增加计数并置顶。
  - 保留原收藏状态。
- 明确不修改：当前是内存规则；持久化后由 ATOM-16 替代为数据库事务。
- 验收：连续复制、复制旧内容和收藏保留测试通过。
- 风险等级：L2。
- 计划提交：`feat(history): 合并重复文本记录`。

### 第四阶段：持久化历史

#### [!] ATOM-14 验证 Slint 大列表策略

- 所属交付单元：UNIT-05。
- 唯一目标：在列表架构定型前验证 Slint 固定高度文本列表性能。
- 依赖：ATOM-13。
- 允许修改：性能探针、测试数据生成器和测量脚本。
- 实现契约：
  - Release 模式生成 20,000 条固定高度摘要。
  - 测量呼出 P95、首批显示、进程内存和长滚动。
- 验收：
  - 打开后内存 ≤ 60 MiB。
  - P95 呼出 ≤ 100 ms。
  - 首批结果 ≤ 50 ms。
  - 长滚动不冻结。
- 停止条件：任一硬门禁失败，必须重排为窗口化自定义 `Model`。
- 风险等级：L2。
- 计划提交：`test(ui): 增加大文本列表性能探针`。

#### [x] ATOM-14R 接入窗口化历史列表

- 所属交付单元：UNIT-05。
- 唯一目标：使用 Slint 的窗口化 `ListView` 让历史列表可滚动且只实例化视口附近卡片。
- 依赖：ATOM-14 已完成探针并确认当前 `VerticalLayout` 不具备长滚动能力。
- 允许修改：`ui/app-window.slint`、ATOM-14 性能探针及其测量脚本、与模型绑定直接相关的 UI 测试和注释清单。
- 实现契约：
  - 看板内容区改为固定高度卡片的 `ListView`，保留黑底白字视觉语法和 `ClipboardCard` 字段。
  - Rust 侧继续通过 `ModelRc` 提供数据，禁止把完整正文或图片资源复制到 UI 组件。
  - 性能探针必须真实执行长滚动窗口更新，不能用“替换模型耗时”伪造滚动证据。
- 明确不修改：不接入 SQLite、不改变历史去重规则、不实现图片懒加载和搜索。
- 验收：重新运行 ATOM-14 Release 探针；工作集、呼出 P95、首批显示均满足原门禁，长滚动能力为 true，且长滚动 P95 ≤ 50ms。
- 停止条件：`ListView` 仍创建全量卡片、滚动事件无法驱动模型或任一硬门禁失败，必须继续拆分窗口化模型/滚动事件原子，不得进入 ATOM-15。
- 风险等级：L3。
- 计划提交：`feat(ui): 接入窗口化历史列表`。

#### [x] ATOM-15 建立唯一 SQLite 存储线程

- 所属交付单元：UNIT-05。
- 唯一目标：所有 SQLite 操作只能在一个存储线程执行。
- 依赖：ATOM-14R。
- 允许修改：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/storage/mod.rs`、`src/storage/worker.rs`、`src/storage/migration.rs`、存储测试、中文注释门禁清单和 v1 SQL。
- 实现契约：
  - 创建 `StorageExecutor` 和存储命令协议。
  - SQLite 连接只能在线程内部创建。
  - 使用 `rusqlite = 0.40.1` 的 `default-features = false, features = ["bundled"]`，锁定 SQLite 构建来源，避免运行时依赖机器上的系统 DLL。
  - v1 迁移使用 `PRAGMA user_version` 作为版本载体：版本 0 在同一事务内执行精确 v1 DDL 并提交为 1；版本 1 重新校验表、约束和索引后视为幂等；版本大于 1、已有不兼容表结构或校验失败均拒绝启动并回滚本次事务。
  - v1 固定表为 `clipboard_items`，包含 `id INTEGER PRIMARY KEY AUTOINCREMENT`、`item_type TEXT NOT NULL`、`text_content TEXT`、`preview_text TEXT NOT NULL`、`content_hash BLOB NOT NULL`、`source_exe TEXT`、`source_app TEXT`、`copy_count INTEGER NOT NULL DEFAULT 1`、`is_pinned INTEGER NOT NULL DEFAULT 0`、`created_at INTEGER NOT NULL`、`copied_at INTEGER NOT NULL`、`last_used_at INTEGER`；建立 `content_hash` 唯一索引及 `copied_at DESC, id DESC` 复合索引。
  - `StorageExecutor::open` 只有在线程创建连接、迁移事务提交并完成 schema 校验后才返回；初始化失败、迁移失败或 worker panic 必须以错误返回，不得返回半初始化执行器。
  - executor 的命令发送端不对外暴露、不实现 `Clone`；`Drop`/`shutdown` 先发送有界 Shutdown 命令，等待 worker 关闭连接后再 `join`，停止后命令只能返回 channel closed。
  - 默认路径从 `%LOCALAPPDATA%` 解析为 `%LOCALAPPDATA%\ClipboardBoard\data\clipboard.db`；变量缺失或目录不可创建明确返回错误。测试必须使用 `open_at` 注入隔离临时目录，停止并释放执行器后再清理目录，不得污染用户数据库。
  - 线程证明命令必须在 worker 内对实际 `Connection` 执行 `SELECT 1`，同时回传连接创建线程、探针执行线程和调用方可比较的 `ThreadId`；测试断言连接创建线程等于探针线程且不同于调用线程，命令 API 不得暴露 `rusqlite::Connection`。
  - 数据库目录位于 `%LOCALAPPDATA%\ClipboardBoard\data`。
- 验收：迁移幂等且保留预置哨兵数据；版本 0→1、版本 1 重跑、版本大于 1 和坏 schema 均有结果断言；失败不会留下半迁移表或错误版本；线程 ID 测试证明实际连接探针未跨线程；默认路径缺失和隔离路径测试通过。
- 风险等级：L3。
- 计划提交：`feat(storage): 建立单线程SQLite执行器`。

#### [x] ATOM-16 实现文本事务 upsert

- 所属交付单元：UNIT-05。
- 唯一目标：文本记录可以按内容哈希事务写入或更新。
- 依赖：ATOM-15。
- 允许修改：存储命令、仓储内部实现和存储集成测试。
- 实现契约：
  - 首次内容插入新记录。
  - upsert 输入必须拥有 `[u8; 32]` `content_hash`、完整文本、预览、可选来源和 `copied_at`；首次插入固定 `item_type='text'`、`copy_count=1`、`is_pinned=0`、`created_at=copied_at`。
  - 公共接缝固定为 `upsert_text(input: TextUpsertInput) -> Result<TextUpsertResult, StorageError>`；输入字段为 `content_hash: [u8; 32]`、`text_content: String`、`preview_text: String`、`source_exe/source_app: Option<String>`、`copied_at: i64`；结果至少返回 `id`、`content_hash`、`preview_text`、来源、`copy_count`、`is_pinned`、`created_at`、`copied_at` 和 `last_used_at`，不返回 Connection/Statement。
  - 重复哈希只更新 `copied_at`，并以 SQLite `INTEGER` 上限 `i64::MAX` 饱和增加 `copy_count`；已有 `copy_count <= 0` 视为异常历史，重复写入直接归一为 2；`created_at`、正文、预览、来源、`is_pinned` 和 `last_used_at` 必须保留旧值。
  - 写入和结果读取必须在 StorageExecutor worker 的同一个 SQLite 事务内完成；事务失败不得产生半更新，命令 API 不暴露 Connection/Statement。
- 验收：首次、重复、同毫秒、计数饱和、零/负计数归一、收藏/旧字段保留和通过预置 `BEFORE UPDATE ... RAISE(ABORT,...)` trigger 制造的事务回滚测试通过；回滚后旧行全部字段不变且 worker 仍可执行只读 status 探针。
- 风险等级：L2。
- 计划提交：`feat(storage): 支持文本历史事务写入`。

#### [x] ATOM-17 实现历史摘要游标查询

- 所属交付单元：UNIT-05。
- 唯一目标：通过复合游标稳定查询历史摘要。
- 依赖：ATOM-16。
- 允许修改：`src/storage/mod.rs`、`src/storage/worker.rs` 的查询命令、仓储内部 SQL 和存储测试。
- 实现契约：
  - 公共接缝固定为 `list_history_summaries(&mut self, cursor: Option<HistoryCursor>, limit: u32) -> Result<HistoryPage, StorageError>` 和 `get_history_payload(&mut self, id: i64) -> Result<Option<HistoryPayload>, StorageError>`；两者只返回值对象，不暴露 Connection/Statement。
  - `HistoryCursor` 固定包含 `copied_at: i64`、`id: i64`；摘要页返回按 `copied_at DESC, id DESC` 排序的 `HistorySummary` 列表和可选 `next_cursor`。
  - 摘要固定返回 `id: i64`、`item_type: String`、`preview_text: String`、`source_exe/source_app: Option<String>`、`copy_count: i64`、`is_pinned: bool`、`created_at/copied_at: i64`、`last_used_at: Option<i64>`；不得加载完整正文作为摘要字段。
  - 完整 payload 固定包含 `id: i64`、`item_type: String`、`text_content: Option<String>`、`preview_text: String`、`content_hash: Vec<u8>`、`source_exe/source_app: Option<String>`、`copy_count: i64`、`is_pinned: bool` 和全部时间字段；不存在的 ID 返回 `Ok(None)`。
  - `limit=0` 返回空页；`limit>100` 返回 `StorageError::InvalidPageSize { requested, max: 100 }`；有效页通过多取一行判断是否存在下一页，禁止使用 `OFFSET`，避免大偏移扫描。
  - 后续页条件固定为 `copied_at < :cursor_time OR (copied_at = :cursor_time AND id < :cursor_id)`，同毫秒记录必须以 ID 稳定断开；只有存在多取行时才返回游标，且 `next_cursor` 固定锚定本页最后一条已返回记录的 `(copied_at,id)`，不得返回隐藏的第 `limit+1` 行；无额外行或 `limit=0` 时为 `None`。
- 验收：相同毫秒记录分页不重复、不遗漏；首页、尾页、空页和越界页大小边界正确；按 ID 可取完整 payload，未知 ID 返回 `None`。
- 风险等级：L2。
- 计划提交：`feat(storage): 支持历史摘要游标查询`。

#### [x] ATOM-18A 接入捕获提交后可见

- 所属交付单元：UNIT-05。
- 唯一目标：捕获结果只有在 SQLite 文本 upsert 成功后才进入 UI。
- 依赖：ATOM-17。
- 允许修改：`src/main.rs`、`src/command.rs`、`src/history.rs`、`src/app/ui_event.rs`、捕获持久化桥和相关测试。
- 实现契约：
  - 主程序在启动热键前只创建一个 `StorageExecutor`，并把它移动给结果泵线程；所有捕获持久化命令通过该实例发送，不得为每次捕获新建执行器或连接。
  - 结果泵从 `ClipboardCaptureResult` 构造 `TextUpsertInput`：正文来自 `payload.as_text()`，哈希/预览来自同一次 `payload.summary()`，来源映射为 `source_exe=ProcessSource.executable`、`source_app=ProcessSource.display_name`，`copied_at` 为结果泵处理时的饱和 Unix 毫秒时间戳。
  - 捕获成功后先调用 `upsert_text`；只有返回成功的 `TextUpsertResult` 才能构造并投递 `UiEvent::ClipboardCaptured`。UI 卡片 ID、哈希、预览、来源、计数和收藏状态必须来自持久化结果。
  - UI 单一 `source` 固定按 `source_app → source_exe → “未知来源”` 选择，运行时捕获的 `relative_time` 固定为“刚刚”；不从新 capture 覆盖数据库重复记录的旧来源/预览。
  - `ClipboardCaptured` reducer 不得再对重复哈希本地递增或保留旧 DTO 的 ID、预览、来源、收藏和计数；必须采用持久化结果传入的最终字段，仅按 UI 需要更新相对时间和顺序。已有仅供非持久化测试/恢复前路径使用的旧协调器语义不得被捕获提交路径复用。
  - 纯处理桥返回固定 `Result<CaptureProcessOutcome, CaptureProcessError>`：`Ok(Posted)` 表示 sink 成功且泵继续，`Ok(UiClosed)` 表示 sink=false/UI loop 已停止且泵必须退出，`Ok(Skipped)` 仅作为未来非 UI 捕获类型的保留状态；当前输入域只有有效文本 `ClipboardCaptureResult`，不得把任何有效文本捕获标记为 `Skipped`。`Err(Storage(_))` 或 `Err(InvalidPersistedRecord)` 表示 upsert/DTO 错误，泵记录后继续等待后续捕获。
  - 数据库失败、ID 无法安全转换为 UI 的 `u64` 或结果 DTO 不可转换时不得投递幽灵 UI 记录；纯处理桥通过可注入 `FnMut(UiEvent) -> bool` sink 测试失败无投递，结果泵按上述三态处理。
  - 原有 sequence 失配/读取错误仍只丢弃本次结果；UI 事件循环停止时结果泵退出并释放 StorageExecutor。
- 验收：模拟成功捕获后可观察到一次 upsert→UI 事件顺序且结果为 `Posted`；使用 `open_at + BEFORE UPDATE` trigger 注入 upsert 错误，断言 `Storage` 错误不投递 UI 且同一执行器可处理后续新哈希；构造不可转换的持久化 DTO 断言 `InvalidPersistedRecord` 不投递 UI；sink 返回 `false` 断言结果为 `UiClosed` 并回收唯一 `StorageExecutor`；重复哈希和 `i64::MAX` 计数测试断言 UI 直接采用持久化最终计数、ID、预览、来源和收藏，不发生本地加一或溢出；当前有效文本捕获测试不得返回 `Skipped`，仅验证该状态为未来非 UI 输入保留；不实现启动恢复和 UI 首批裁剪。
- 风险等级：L3。
- 计划提交：`feat(history): 提交成功后再显示捕获历史`。

#### [x] ATOM-18B 接入启动恢复和有界 UI 缓存

- 所属交付单元：UNIT-05。
- 唯一目标：启动时从 SQLite 恢复有限历史摘要，并让 UI 只渲染首批记录。
- 依赖：ATOM-18A。
- 允许修改：`src/main.rs`、`src/app/ui_event.rs`、`src/command.rs`、启动恢复桥和相关测试。
- 实现契约：
  - 使用 ATOM-18A 创建的同一个 `StorageExecutor`，在启动热键/结果泵之前读取首页最多 100 条摘要；每条摘要按 ID 获取 payload 只提取 UI 所需元数据，完整正文不得进入 UI DTO 或长期缓存。
  - 恢复失败必须在启动阶段返回错误，不启动热键、剪贴板监听或结果泵；未知/不兼容 payload 不得伪造 UI 记录。
  - 恢复快照通过 UI 事件循环的 `ReplaceSnapshot` 投递；UI 内存历史缓存容量固定为 100 条，窗口模型每次只设置前 30 条，保留后续分页所需的缓存状态。
  - 首次恢复和后续捕获沿用现有哈希去重、收藏保留和选中项安全恢复规则；不在本原子接入滚动分页、搜索或选择导航。
- 验收：预置 100 条数据库记录启动后 UI 状态最多保留 100 条且窗口首批最多 30 条；预置 20,000 条时恢复查询和 UI 模型创建均不构造 20,000 个卡片；数据库读取失败时无热键/捕获线程启动和幽灵 UI 记录。
- 风险等级：L3。
- 计划提交：`feat(history): 恢复持久化摘要并限制首批模型`。

### 第五阶段：历史键盘复制和自动粘贴实现

> 本阶段记录 ATOM-19 至 ATOM-22 已完成的历史实现。用户在真实试用后撤销自动粘贴方向；当前产品行为和后续代码删除以 ATOM-25R 为准，不得把本节再次当作目标交互。

#### [x] ATOM-19 实现键盘选择导航

- 所属交付单元：UNIT-06。
- 唯一目标：用户可以使用上下键选择记录。
- 依赖：ATOM-18B。
- 允许修改：UI 选择状态、滚动定位和交互测试。
- 实现契约：
  - `AppWindow` 的 FocusScope 只新增上/下键选择回调；Enter、Esc 和粘贴动作留给后续原子，不在本原子消费。
  - 面板打开且当前首批非空时默认选择第一项；空列表保持 `None`。
  - 选择索引只在当前窗口已构造的首批 `min(history_len, 30)` 条内移动，向上/向下到边界后保持不变，不允许选择未构造的第 31 条及以后记录；后续分页原子再扩展范围。
  - 选中项自动保持可见，使用现有固定 106px delegate 和 `history-viewport-y` 视口属性，不把正文或新的模型节点带入选择逻辑。
  - 所有选择变化仍在 UI 事件循环线程 reducer 内完成，后台线程不能直接修改选中索引。
  - 模型重置后选择安全恢复。
- 验收：空列表、单条、30 条边界、超过 30 条边界、上下键连续移动和结果重置测试通过；选中项对应卡片始终位于当前视口。
- 风险等级：L1。
- 计划提交：`feat(ui): 支持键盘选择历史记录`。

#### [x] ATOM-20 实现 Ctrl+Enter 仅复制

- 所属交付单元：UNIT-06。
- 唯一目标：选中文本记录后可只写回系统剪贴板。
- 依赖：ATOM-19。
- 允许修改：剪贴板 writer、预期写事务、UI 回调和集成测试。
- 实现契约：
  - 按 ID 从 StorageExecutor 获取 payload。
  - ClipboardIO 写入 Unicode 文本。
  - 写入前登记哈希和格式，写入后绑定精确 sequence。
  - 监听端一次性消费匹配事件。
  - 写回前用完整正文重算文本哈希；数据库哈希或 UI 哈希不一致、正文含内部 NUL 时拒绝写回。
  - `SetClipboardData` 已转移所有权但 `CloseClipboard` 失败时仍保留一次性自身写回预期。
  - 预期按 token 放入有界多项队列；写回登记到 sequence 绑定期间串行化，未绑定预期不消费；超过 TTL 的异常旧项淘汰，队列满则拒绝写入。
  - 写入线程在仍持有 `OpenClipboard` 独占锁时读取本次 sequence，关闭后不再重新查询；结果桥关闭时丢弃尚未执行的复制命令。
- 验收：系统剪贴板内容正确；历史计数、时间和顺序不变化。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持仅复制文本记录`。

#### [x] ATOM-21 实现 Enter 自动粘贴

- 所属交付单元：UNIT-07。
- 唯一目标：向面板打开前的普通权限目标自动粘贴。
- 依赖：ATOM-20。
- 允许修改：粘贴协调器、目标验证、焦点恢复和 Windows 集成测试。
- 实现契约：
  - 使用 ATOM-05 保存的目标会话。
  - Enter 回调同步捕获当前面板代次、选中记录 ID、内容哈希，并通过单调请求令牌防止重复提交和迟到结果污染。
  - 后台写回复用 ATOM-20 的正文、类型、NUL 和哈希校验；复制/关闭时清除尚未执行的自动粘贴命令。
  - Enter 时复核 HWND、PID、`IsWindow`、完整性和前台身份；`SetForegroundWindow` 后再次确认仍是同一普通中完整性目标。
  - 同级目标才允许隐藏面板、恢复焦点并调用完整四事件 `SendInput` 序列。
  - `SendInput` 部分接受时按已接受前缀逆序释放按键；释放调用通过可注入 seam 测试，目标变化时不再发送任何输入。
  - 自动粘贴成功先在同一 UI 事件闭包内按代次/令牌更新状态，再隐藏物理面板，避免旧成功隐藏新请求。
  - 目标变化时不得向新前台窗口发送按键。
- 验收：普通同级目标的自动粘贴路径通过状态、目标和输入序列测试；失效或变化目标不收到输入，管理员/失败提示留给 ATOM-22。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持安全自动粘贴`。

#### [x] ATOM-22 实现自动粘贴降级提示

- 所属交付单元：UNIT-07。
- 唯一目标：不能自动注入时可靠降级为仅复制。
- 依赖：ATOM-21。
- 允许修改：粘贴结果模型、提示 UI 和管理员目标测试。
- 实现契约：
  - 高完整性目标不调用 `SendInput`。
  - 后台写回已成功但目标复核、焦点恢复或输入失败时，保持系统剪贴板内容并提示“内容已复制，请按 Ctrl + V”。
  - 后台写回未成功或请求桥拒绝时，不得声称内容已复制；提示“无法准备剪贴板内容，请重试”。
  - 提示只展示固定文案，不携带剪贴板正文、窗口标题或错误堆栈；当前请求完成后允许用户继续浏览或按 Esc 关闭面板。
  - 失败事件必须携带 generation、request_token 和失败类别；迟到、取消或重复失败不得污染新请求，成功事件不得显示失败提示。
  - 不误报自动粘贴成功；ATOM-22 的降级处理不得重试 `Ctrl+V` 或调用 `SendInput`。ATOM-21 已接受输入前缀后的纯 key-up 释放是底层输入清理的唯一例外，不属于降级重试。
- 验收：管理员目标、目标变化、焦点恢复失败和模拟输入失败均只保留已写回的剪贴板内容并显示复制提示；写回失败显示重试提示；旧失败事件不影响新面板；有效提示会重新显示面板且不重新捕获目标。
- 风险等级：L2。
- 计划提交：`feat(paste): 增加自动粘贴失败降级`。

#### ATOM-22 完成记录

- 状态：已完成。
- 完成内容：新增 `PasteFailureReason` 固定失败类别；工作桥和写回失败统一保守提示“无法准备剪贴板内容，请重试”，目标变化、焦点恢复失败和输入注入失败提示“内容已复制，请按 Ctrl + V”。失败事件携带 generation、request_token 和类别，仅在面板可见、请求仍 pending 且代次/令牌匹配时生效；有效失败清理执行状态并重新显示面板，取消、成功、隐藏、新捕获和新请求会清除旧提示。
- UI 接缝：Slint 增加不携带正文或系统错误详情的固定提示属性；提示属性独立于卡片模型刷新，目标执行失败后只调用 `window.show()` 恢复可见性，不重新捕获目标、不重试 `Ctrl+V`，ATOM-21 已接受输入前缀的 key-up 清理例外保持不变。
- 实际修改文件：`src/app/ui_event.rs`、`src/command.rs`、`src/history_bridge.rs`、`src/lib.rs`、`src/main.rs`、`src/platform/windows/window/mod.rs`、`ui/app-window.slint`。
- 针对性验证：目标/焦点/输入失败固定复制提示、写回失败固定重试提示、准备阶段缺少目标、取消或成功后的迟到失败隔离、`CloseFailed` 保守归类、高完整性禁止 `SendInput` 均有测试覆盖。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（133 个测试通过）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：失败类别、提示文案隐私边界、旧事件门禁、面板重新显示和桥接保守降级原因均已补充中文职责与关键逻辑注释。
- 风险等级：L2。
- DDD 结果：前置终审提出并修正失败类别缺失、提示未同步、迟到事件污染、缺目标静默和面板被目标窗口遮挡问题；提交前复核 PASS，ATOM-21 partial key-up cleanup 作为唯一底层清理例外保留。
- 本地提交：`f4647e6 feat(paste): 增加自动粘贴失败降级`。
- 远端同步：已推送 `origin/main`。
- 计划偏差：为保证失败提示在不刷新卡片模型时也可见，增加独立 `paste_notice` UI 属性和最小 `ShowPasteNotice` 窗口副作用；未扩展图片、搜索、分页或设置。
- 下一原子：ATOM-23 已完成；当前已进入 ATOM-24 搜索防抖和旧结果作废。

### 第六阶段：搜索和历史管理

#### [x] ATOM-23 实现存储层搜索和筛选查询

- 所属交付单元：UNIT-08。
- 唯一目标：按关键词、来源、类型和收藏查询历史。
- 依赖：ATOM-22。
- 允许修改：存储查询命令、SQL、筛选 DTO 和测试。
- 实现契约：
  - 新增拥有型 `HistoryQuery` DTO：`keyword/source/item_type/is_pinned` 为可选筛选，`cursor/limit` 负责复合游标分页；查询必须由 `StorageExecutor` 的唯一 SQLite worker 执行。
  - `keyword` 在 `text_content` 或 `preview_text` 中做包含匹配；`source` 在 `source_app` 或 `source_exe` 中做包含匹配；`item_type` 精确匹配；`is_pinned` 按布尔值精确匹配；空字符串等同于未提供筛选。
  - 用户输入必须按 `\`、`%`、`_` 顺序转义，并使用 SQLite `LIKE ... ESCAPE '\'`；不得把用户输入拼接进 SQL。
  - 所有筛选结果仍按 `copied_at DESC, id DESC` 排序；复合游标必须在筛选条件之后生效，不使用 `OFFSET`，多取一行决定 `next_cursor`。
  - 保留既有页大小上限和零页大小语义；查询只返回 `HistorySummary`，不得加载完整正文。
- 验收：中文、英文、符号、`%`/`_`/`\` 字面量、来源、类型、收藏、组合筛选、无结果、同毫秒分页、尾游标、零页和超限页大小测试通过。
- 风险等级：L2。
- 计划提交：`feat(search): 支持历史搜索与筛选查询`。

### 2026-07-28 ATOM-23 启动存储层搜索和筛选查询

- 状态：正在执行。
- 启动原因：ATOM-22 已完成失败降级提示并推送，用户要求继续执行后续原子。
- 唯一目标：为 SQLite 历史摘要增加安全的关键词、来源、类型和收藏筛选，并保持复合游标分页。
- 明确不做：不接入搜索框、筛选标签、搜索防抖、UI 状态、FTS5、删除/收藏写操作、图片查询或正文加载。
- 远端策略：启动记录、代码提交和完成记录均提交后推送 `origin/main`。
- 下一步：先锁定 `HistoryQuery` 公共接缝、LIKE 转义和筛选后游标 SQL，再实现测试并执行全量验证。

#### ATOM-23 完成记录

- 状态：已完成。
- 完成内容：新增拥有型 `HistoryQuery` DTO 和 `StorageExecutor::query_history_summaries` 接口；关键词在正文/预览中包含匹配，来源在应用名/可执行文件名中包含匹配，类型和收藏精确筛选；空字符串按未提供处理。
- SQL 安全：用户输入按反斜杠、百分号、下划线转义后绑定为 `LIKE ... ESCAPE '\'` 参数；SQL 不拼接用户输入，筛选条件先于复合游标，仍按 `copied_at DESC, id DESC` 排序、多取一行生成 `next_cursor`，不加载正文、不使用 `OFFSET`。
- 实际修改文件：`src/storage/worker.rs`、`src/storage/mod.rs`。
- 针对性验证：中文/英文/符号关键词、`%`/`_`/`\` 字面匹配、来源应用与可执行文件、类型、收藏、组合筛选、无结果、筛选内同毫秒游标分页、零页和 101 页上限均通过测试。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（135 个测试通过）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：`HistoryQuery` 字段、唯一 worker 接口、SQL 参数安全、通配符转义和筛选游标边界均已补充中文职责与关键逻辑注释。
- 风险等级：L2。
- DDD 结果：按原子边界复核了参数化 SQL、通配符转义、筛选条件与游标组合、摘要不携带正文和既有分页兼容性；测试与全量检查通过，提交前复核 PASS。
- 本地提交：`15da738 feat(search): 支持历史搜索与筛选查询`。
- 远端同步：已推送 `origin/main`。
- 计划偏差：为兼容 ATOM-17 的无筛选分页调用，保留 `list_history_summaries` 包装接口并将其转发到统一查询；未接入 UI、FTS5 或搜索防抖。
- 当时的下一原子：ATOM-25；当前恢复顺序已由 v37 调整为 ATOM-25R，完成后才进入 ATOM-26。

#### [x] ATOM-24 实现搜索防抖和旧结果作废

- 所属交付单元：UNIT-08。
- 唯一目标：快速输入时只展示最新查询结果。
- 依赖：ATOM-23。
- 允许修改：搜索协调器、generation 模型和并发测试。
- 实现契约：
  - 新增 UI 线程拥有的 `SearchCoordinator`；默认防抖窗口固定为 120 ms，并允许测试注入时钟和更短窗口，不在测试中真实 sleep。
  - 每次提交查询生成单调递增的 `SearchGeneration`；未到截止时间的请求可被后续输入替换，截止后只派发最后一个请求。
  - 派发请求进入 in-flight 状态；新提交、取消或关闭都会使旧 generation 失效；结果只有在 generation 仍为当前 in-flight 且尚未应用时才能被接受，重复结果也必须拒绝。
  - 协调器只负责 `HistoryQuery` 请求的时序和代次，不执行 SQLite、不访问 UI、不携带正文；ATOM-25 再接入搜索框和结果模型。
- 验收：连续输入三个关键词只派发最后一个；120 ms 前不派发、到期恰好派发；旧请求已派发后新输入会使其结果失效；取消、重复结果和关闭后的迟到结果均被拒绝；generation 单调且不回到零。
- 风险等级：L2。
- 计划提交：`feat(search): 增加搜索防抖与结果代次`。

### 2026-07-28 ATOM-24 启动搜索防抖和旧结果作废

- 状态：正在执行。
- 启动原因：ATOM-23 已完成安全存储筛选查询并推送，用户要求继续执行后续原子。
- 唯一目标：建立不依赖真实 sleep 的 120 ms 搜索防抖和结果 generation 门禁。
- 明确不做：不接入 Slint 搜索框、SQLite worker、FTS5、筛选标签、正文读取或网络服务。
- 远端策略：启动记录、代码提交和完成记录均提交后推送 `origin/main`。
- 下一步：先实现 UI 线程可拥有的 `SearchCoordinator` 状态机，再用可控时间和旧结果竞态测试验证。

#### ATOM-24 完成记录

- 状态：已完成。
- 完成内容：新增 `SearchCoordinator`，默认 120 ms 防抖窗口；每次提交分配从 1 开始的单调 `SearchGeneration`，待处理请求采用 latest-wins；到期后进入 in-flight，只有当前代次的第一个结果可接受。
- 失效语义：新输入清除旧 in-flight，`cancel` 同时清理 pending/in-flight，重复结果、旧代次结果和取消/关闭后的迟到结果均拒绝；代次耗尽时返回明确错误而不回绕。
- 接缝边界：`SearchCoordinator` 只保存拥有型 `HistoryQuery` 和请求身份，不执行 SQLite、不访问 Slint、不携带正文；使用注入 `Instant` 的 `poll` 测试截止时间，不真实 sleep。
- 实际修改文件：`src/search/mod.rs`、`src/lib.rs`。
- 针对性验证：三次快速输入只派发最后请求、119/120 ms 边界、旧结果被新输入作废、取消后迟到结果、重复结果、从 1 开始的单调代次和零窗口立即派发均通过测试。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（141 个测试通过）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：协调器状态字段、公共请求/错误 API、截止时间、代次门禁和取消语义均已补充中文职责与关键逻辑注释。
- 风险等级：L2。
- DDD 结果：复核了 latest-wins、防抖截止点、in-flight 结果一次性消费、取消清理、代次耗尽不回绕和 UI/SQLite 隔离；测试与全量检查通过，提交前复核 PASS。
- 本地提交：`02084bd feat(search): 增加搜索防抖与结果代次`。
- 远端同步：已推送 `origin/main`。
- 计划偏差：为让 UI 事件线程可直接拥有状态，未创建常驻搜索线程；结果竞态由 generation/in-flight 门禁串行化，真实 worker 接线留给 ATOM-25 及后续原子。
- 下一原子：ATOM-25 接入搜索框和筛选标签（尚未启动）。

### 2026-07-28 ATOM-25 启动搜索框和筛选标签

- 状态：正在执行。
- 启动原因：ATOM-24 已完成 120 ms 防抖和 generation 结果门禁，当前需要把用户输入、标签筛选和结果状态接入看板。
- 唯一目标：用户可在面板输入关键词、切换“全部/文本/收藏”，并看到 loading、无结果和错误状态的明确反馈。
- 明确不做：不启用图片筛选；不重写 ATOM-23 的 SQL 查询；不重写 ATOM-24 的防抖协调器；不接入 FTS5、网络服务或完整图片历史。
- 允许修改：`src/command.rs`、`src/app/ui_event.rs`、`ui/app-window.slint` 及其直接 UI 测试；如需补充状态映射，只能修改同一接缝的测试和注释。
- 时序契约：输入或标签变化生成新的查询代次，旧计时器不能消费新请求；防抖到期后只应用当前代次，面板关闭/隐藏时取消待处理搜索。
- 数据契约：搜索结果只从现有内存历史卡片重建可见集合；筛选不得改变 `MemoryHistory` 的完整缓存，选中项在结果刷新后按当前集合重新定位。
- 加载契约：防抖期间保留上一批可见结果并显示 loading，当前代次完成后再替换卡片；不得因输入瞬间跳空。
- 验收：搜索关键词和三类标签结果正确；快速连续输入不闪回旧结果；loading、空结果和错误状态可区分；关闭后迟到事件不改变面板；Slint 构建和 UI 状态测试通过。
- 风险等级：L2（UI 状态、异步代次和用户可见空/错状态交界）。
- 计划提交：`feat(ui): 接入历史搜索和筛选标签`。

#### [x] ATOM-25 接入搜索框和筛选标签

- 所属交付单元：UNIT-08。
- 唯一目标：用户可以输入关键词并切换基础筛选。
- 依赖：ATOM-24。
- 允许修改：搜索框、全部/文本/收藏标签和 UI 测试。
- 实现契约：
  - 输入即搜索。
  - 切换标签重置游标和选择。
  - loading、无结果和错误状态可区分。
- 明确不修改：图片标签在 ATOM-39 启用。
- 验收：搜索和标签结果正确，旧结果不闪回。
- 风险等级：L2。
- 计划提交：`feat(ui): 接入历史搜索和筛选标签`。

#### ATOM-25 完成记录

- 状态：已完成。
- 完成内容：新增 `SearchFilter`、`SearchStatus` 和搜索 UI 事件；看板接入标准 `LineEdit` 搜索框及“全部/文本/收藏”标签，状态可显示 loading、无结果和错误；图片筛选仍未启用。
- 状态机接线：输入或标签变化复用 ATOM-24 `SearchCoordinator` 生成新代次；Slint 计时器只投递 `SearchDebounceElapsed`，旧代次先经过 `latest_generation` 检查，关闭/隐藏时取消 pending/in-flight；当前代次到期后按关键词和收藏条件重建可见卡片。
- 交互细节：防抖加载期间保留上一批结果并显示“搜索中…”，避免列表跳空；结果应用后重置选择并定位首项；空结果显示“没有匹配的剪贴内容”，错误显示固定“搜索暂不可用，请稍后重试”，不透传内部错误或剪贴板正文。
- 数据边界：筛选只重建 `snapshot.items`，不修改 `MemoryHistory` 完整 100 条摘要缓存；查询 DTO 只在 UI 接缝构造，不重复实现 ATOM-23 SQL 或加载正文。
- 实际修改文件：`src/command.rs`、`src/app/ui_event.rs`、`ui/app-window.slint`。
- 针对性验证：关键词防抖后匹配、旧计时器不闪回、收藏筛选重置选择且保留完整历史、面板关闭后的迟到事件丢弃、loading/结果状态均通过 UI reducer 测试。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（145 个测试通过）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：搜索筛选枚举、结果状态、代次门禁、加载期间旧结果保留、窗口回调和状态同步均已补充中文职责与关键逻辑注释。
- 风险等级：L2。
- DDD 结果：首轮发现 loading 期间清空旧结果违反产品可见状态契约；已改为保留上一批卡片并补充测试，第二轮复核确认代次、关闭隔离、内存历史不变和注释一致性，最终 PASS。
- 本地提交：`49d79f4 feat(ui): 接入历史搜索和筛选标签`。
- 远端同步：已推送 `origin/main`。
- 计划偏差：本原子按已有限文本摘要在内存中重建结果，没有启动 SQLite 搜索 worker；真实持久化搜索结果接线和图片筛选按原计划留给后续原子。
- 下一原子：ATOM-25R 修正已通过试用暴露的交互基线问题；ATOM-26 顺延。

#### [x] ATOM-25R 修正置顶、鼠标复制和真实分页交互基线

- 所属交付单元：UNIT-08R。
- 唯一目标：把当前偏键盘命令面板修正为持续置顶、鼠标优先、显式复制且可滚动浏览 SQLite 历史的看板。
- 依赖：ATOM-25。
- 当前状态：十个子原子已全部实现、定向验证、DDD 复核并推送。
- 详细计划：[`docs/planning/交互基线修正原子计划.md`](docs/planning/交互基线修正原子计划.md)。
- 执行子原子：
  1. [x] `WCB-INT-01` 固定面板级键盘契约。
  2. [x] `WCB-INT-02` 删除自动输入基础设施。
  3. [x] `WCB-INT-03` 建立持久面板激活协议。
  4. [x] `WCB-INT-04` 设置 Windows 始终置顶。
  5. [x] `WCB-INT-05` 支持鼠标选择卡片。
  6. [x] `WCB-INT-06` 增加显式复制按钮。
  7. [x] `WCB-INT-07` 让历史摘要携带严格内容哈希。
  8. [x] `WCB-INT-08` 拆分存储所有者与共享客户端。
  9. [x] `WCB-INT-09` 接入 SQLite 首批搜索。
  10. [x] `WCB-INT-10` 接入滚动续页。
- 产品规则：点击其他软件不隐藏；`Esc` 或再次按 `Alt + V` 隐藏到托盘；普通
  `Enter` 无动作；应用不模拟粘贴；鼠标复制后由用户在目标应用手动按 `Ctrl + V`。
- 分页规则：首次查询 30 条，滚动续页每批 50 条，复合游标、latest-wins 结果邮箱、精确 generation/token/cursor 身份和 2,000 条硬上限。
- 明确不修改：不实现收藏、删除、图片捕获、设置或网络能力；这些仍由后续原子负责。
- 验收：详细计划的 `SC-01` 至 `SC-08`、每个子原子的针对性测试、全量 Rust 检查、Windows Release 构建和真实桌面鼠标验收全部通过。
- 风险等级：L3，涉及窗口生命周期、Win32 topmost、存储关闭线性化、异步查询和分页幂等。
- DDD 结果：计划经过三轮隔离审查，最终窗口交互与分页并发两条审查线均为 `PASS`；提交前仍须逐高风险子原子复审。
- 计划提交：每个 `WCB-INT-*` 子原子独立提交并推送，提交信息必须包含子原子 ID。
- 后续原子：全部十个子原子完成后进入 ATOM-26。
- 实际结果：真实视口边沿驱动 30/50 复合游标分页，失败显式重试，2,000 条有界模型和跨页鼠标/键盘选择均已落地；20,000 条 ListView 探针与隔离 85 条 Windows 滚轮冒烟通过。

#### [x] ATOM-26 支持收藏切换

- 所属交付单元：UNIT-09。
- 唯一目标：记录可以被收藏或取消收藏。
- 依赖：ATOM-25R。
- 允许修改：存储命令、UI 回调和测试。
- 实现契约：先提交事务，再更新 UI；重复复制保留收藏。
- 验收：重启后状态存在；失败时 UI 不提前变化。
- 风险等级：L2。
- 细化执行计划：[`docs/planning/收藏管理原子计划.md`](docs/planning/收藏管理原子计划.md)，依次执行 `FAV-01` 至 `FAV-03`。
- 计划提交：`feat(history): 支持收藏历史记录`。
- 实际结果：期望状态收藏事务、有界异步命令桥和卡片收藏交互已完成；收藏重启
  持久化、重复复制保留、收藏筛选刷新、深分页记录更新和失败不乐观变化均有定向证据。
- 子原子提交：`17b55e8`（FAV-01）、`d3d32c0`（FAV-02）、FAV-03 最终提交。
- DDD 结果：三个门禁均为 `PASS`；ATOM-27 未启动，等待用户实际验证。
- 实际验证修正：长文本曾因固有换行高度突破 96px 固定卡片；现已把预览限制为
  约三行、保留来源行并启用卡片裁剪。软件渲染回归中卡片间隔浅色像素由 344 降为 0。

#### [x] ATOM-27 支持删除单条记录

- 所属交付单元：UNIT-09。
- 唯一目标：删除一条指定历史记录。
- 依赖：ATOM-26。
- 允许修改：删除命令、UI 行为和测试。
- 实现契约：事务成功后从 UI 移除；重复删除返回幂等结果。
- 验收：重启不恢复；失败时记录仍存在。
- 风险等级：L2。
- 细化执行计划：[`docs/planning/单条删除原子计划.md`](docs/planning/单条删除原子计划.md)，
  依次执行 `DEL-01` 至 `DEL-03`。
- 实现结果：卡片提供独立删除按钮；SQLite 成功后才移除，失败保留并显示固定提示；
  删除后推进查询代次，旧首页和续页不能复活记录；重启不恢复已删除记录。
- 计划提交：`feat(history): 支持删除单条记录`。

#### [x] ATOM-28 支持清空未收藏记录

- 所属交付单元：UNIT-09。
- 唯一目标：确认后清空所有未收藏记录。
- 依赖：ATOM-27。
- 允许修改：批量删除命令、确认 UI 和测试。
- 实现契约：单个事务只删除未收藏文本；收藏和非文本保持不变；以唯一存储线程的
  单调操作修订号区分清空前迟到捕获与清空后新捕获。
- 验收：取消无变化；确认成功后只移除清空事务前的未收藏文本；失败不乐观变化；
  旧捕获、旧首页和旧续页不能复活记录。
- 风险等级：L3。
- 细化执行计划：[`docs/planning/清空未收藏原子计划.md`](docs/planning/清空未收藏原子计划.md)，
  依次执行 `CLR-01` 至 `CLR-03`。
- 实现结果：提供“清理未收藏”内联二次确认；取消和失败不修改历史，事务成功后只移除
  未收藏文本并保留收藏、非文本和清空后新捕获；存储修订水位与查询代次共同阻止旧捕获、
  旧首页和旧续页复活记录。
- 计划提交：`feat(history): 支持清空未收藏记录`。

#### [x] ATOM-29 支持强确认清空全部

- 所属交付单元：UNIT-09。
- 唯一目标：经过明确强确认后删除包括收藏在内的全部记录。
- 依赖：ATOM-28。
- 允许修改：全量删除命令、强确认 UI 和测试。
- 实现契约：默认清空操作不得隐式进入此路径。
- 验收：取消无变化；确认后数据库和 UI 均为空。
- 风险等级：L3。
- 细化执行计划：[`docs/planning/清空全部原子计划.md`](docs/planning/清空全部原子计划.md)，
  依次执行 `FULL-01` 至 `FULL-03`。
- 实现结果：增加显式 `All` 范围、单事务全量删除和固定文字强确认；只有精确输入
  “清空全部”才能提交，警告明确包括收藏。成功后删除全部类型和收藏状态的事务前记录，
  以独立捕获账本保留事务后新复制，并统一失效旧查询、旧游标和旧选择。
- 验证结果：存储事务、双范围桥、Slint 合同和 reducer 定向测试通过；三个子原子均
  经 L3 DDD 复核并独立提交推送。
- 计划提交：`feat(history): 支持确认后清空全部历史`。

### 第七阶段：图片捕获与展示

#### [-] ATOM-30 建立图片持久化契约

- 所属交付单元：UNIT-10。
- 唯一目标：建立可迁移、可恢复的图片元数据和磁盘目录契约。
- 依赖：ATOM-29。
- 允许修改：v2 迁移、图片领域元数据、目录模块和迁移测试。
- 实现契约：
  - 增加图片路径、尺寸和格式字段。
  - 建立 original、thumbnail、staging 和 recovery 目录。
  - 原图路径按哈希前缀分片。
- 验收：v1 升级不丢文本；迁移重复执行安全。
- 风险等级：L3。
- 细化执行计划：[`docs/planning/图片持久化契约原子计划.md`](docs/planning/图片持久化契约原子计划.md)，
  依次执行 `PERSIST-01` 至 `PERSIST-04`。
- 计划提交：`feat(storage): 增加图片记录迁移与目录契约`。

#### [ ] ATOM-31 定义规范图片哈希

- 所属交付单元：UNIT-10。
- 唯一目标：相同像素内容跨 PNG、DIB 和 DIBV5 得到相同哈希。
- 依赖：ATOM-30。
- 允许修改：图片领域模型、规范化和测试。
- 实现契约：
  - 顶向下。
  - 行连续。
  - RGBA8。
  - 哈希包含域标签、宽度、高度和像素。
  - 忽略编码元数据。
- 验收：同像素跨格式一致，不同像素或尺寸不同。
- 风险等级：L2。
- 计划提交：`feat(image): 定义规范图片内容哈希`。

#### [ ] ATOM-32 读取注册 PNG 格式

- 所属交付单元：UNIT-10。
- 唯一目标：从剪贴板安全读取注册 PNG。
- 依赖：ATOM-31。
- 允许修改：ClipboardIO 图片读取、PNG 解码入口和畸形数据测试。
- 实现契约：
  - 在关闭剪贴板前复制有界字节。
  - 校验 sequence。
  - 检查文件长度、像素数、尺寸乘法溢出和解码结果。
- 验收：有效、截断、超限、损坏和 sequence 失配用例通过。
- 风险等级：L3。
- 计划提交：`feat(image): 支持读取剪贴板PNG`。

#### [ ] ATOM-33 读取 DIB 和 DIBV5

- 所属交付单元：UNIT-10。
- 唯一目标：安全解析常见剪贴板 DIB 和 DIBV5。
- 依赖：ATOM-32。
- 允许修改：DIB 解析器、ClipboardIO 复制路径和测试。
- 实现契约：
  - 支持24/32位 `BI_RGB`。
  - 支持32位 `BI_BITFIELDS`。
  - 处理 top-down、bottom-up、行填充和 alpha。
  - 所有偏移与尺寸使用 checked arithmetic。
- 验收：颜色、方向、掩码、截断、溢出和畸形头测试通过。
- 风险等级：L3。
- 计划提交：`feat(image): 支持读取DIB与DIBV5`。

#### [ ] ATOM-34 增加 CF_BITMAP 回退

- 所属交付单元：UNIT-10。
- 唯一目标：缺少 PNG/DIB 时仍可读取 `CF_BITMAP`。
- 依赖：ATOM-33。
- 允许修改：ClipboardIO 位图读取、Win32 资源封装和测试。
- 实现契约：
  - 在 ClipboardIO 中调用 `GetDIBits`。
  - 复制为自有像素 DTO。
  - 不把 HBITMAP 交给 ImageWorker。
  - 正确管理本进程创建的 GDI 资源。
- 验收：回退成功且无句柄泄漏。
- 风险等级：L3。
- 计划提交：`feat(image): 增加CF_BITMAP读取回退`。

#### [ ] ATOM-35 建立耐久图片处理流水线

- 所属交付单元：UNIT-10。
- 唯一目标：把规范像素安全持久化为原图和缩略图。
- 依赖：ATOM-34。
- 允许修改：ImageWorker、编码器、staging 文件和测试。
- 实现契约：
  - staging 文件对本次捕获独占。
  - 生成 PNG 原图和最长边320px WebP。
  - 执行 flush、关闭、原子发布和发布后验证。
  - 不创建 Slint Image。
- 验收：中途失败不产生正式文件；低分辨率图片不被放大。
- 风险等级：L3。
- 计划提交：`feat(image): 建立耐久图片处理流水线`。

#### [ ] ATOM-36 提交图片记录并接入历史

- 所属交付单元：UNIT-10。
- 唯一目标：图片文件和数据库记录以一致顺序进入历史。
- 依赖：ATOM-35。
- 允许修改：图片持久化协调器、存储命令、删除回收队列和集成测试。
- 实现契约：
  - 文件发布成功后才提交数据库。
  - 数据库成功后才更新 UI。
  - 数据库失败只回滚本次拥有的文件。
  - 重复图片合并。
  - 单条删除和清空提交后排队回收无引用资产。
- 验收：无缺失原图记录；失败无幽灵卡片；删除后最终清理资产。
- 风险等级：L3。
- 计划提交：`feat(history): 接入持久化图片历史`。

#### [ ] ATOM-37 展示按需加载的图片卡片

- 所属交付单元：UNIT-10。
- 唯一目标：列表中显示图片缩略图且不长期解码不可见图片。
- 依赖：ATOM-36。
- 允许修改：图片卡片、缩略图加载器、像素 DTO 和 UI 测试。
- 实现契约：
  - 工作线程只产生可发送像素缓冲。
  - Slint Image 只在 UI 线程创建。
  - 图片卡片采用固定高度。
  - 缺失缩略图显示占位并请求重建。
- 验收：不可见图片不解码；快速滚动不阻塞 UI。
- 风险等级：L3。
- 计划提交：`feat(ui): 展示懒加载图片卡片`。

#### [ ] ATOM-38 支持复制和粘贴图片

- 所属交付单元：UNIT-11。
- 唯一目标：历史图片可以重新写回剪贴板并粘贴。
- 依赖：ATOM-37。
- 允许修改：图片 writer、预期写事务接线、粘贴协调器和集成测试。
- 实现契约：
  - 从耐久 original 读取。
  - 同一写事务提供注册 PNG 和 DIBV5。
  - 复用类型无关的自身写回抑制。
  - 只接入卡片显式复制按钮；Enter 与 Ctrl+Enter 均无动作。
- 验收：可粘贴到画图等程序；历史计数和排序不变化。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持复制与粘贴图片`。

#### [ ] ATOM-39 启用图片筛选标签

- 所属交付单元：UNIT-08。
- 唯一目标：用户可以只查看图片历史。
- 依赖：ATOM-38。
- 允许修改：筛选枚举、存储查询和 UI 标签。
- 验收：全部、文本、图片和收藏结果正确，游标连续。
- 风险等级：L1。
- 计划提交：`feat(filter): 支持图片类型筛选`。

### 第八阶段：大历史分页和内存控制

#### [ ] ATOM-40 建立分页协调器

- 所属交付单元：UNIT-12。
- 唯一目标：在 ATOM-25R 文本分页基线上扩展图片兼容状态和统一性能观测，不重复实现通用分页协议。
- 依赖：ATOM-39。
- 允许修改：分页协调器、游标状态和测试。
- 实现契约：
  - 复用 ATOM-25R 的首批 30、后续 50、generation/token/cursor 和失败重试协议。
  - 增加图片卡片与文本卡片共用的分页状态和性能指标。
  - 不得出现第二套并行分页协调器。
- 验收：切换查询后不混入旧页面；同页不重复。
- 风险等级：L2。
- 计划提交：`feat(history): 建立统一分页协调器`。

#### [ ] ATOM-41 接入滚动到底自动加载

- 所属交付单元：UNIT-12。
- 唯一目标：验证并补齐图片混合列表对 ATOM-25R 既有滚动续页协议的兼容性。
- 依赖：ATOM-40。
- 允许修改：ListView 回调、加载指示和交互测试。
- 实现契约：保持选择和键盘导航，不显示传统分页按钮。
- 验收：连续滚动无重复、遗漏或选择跳变。
- 风险等级：L2。
- 计划提交：`feat(ui): 支持历史无限滚动`。

#### [ ] ATOM-42 实现缩略图 LRU

- 所属交付单元：UNIT-12。
- 唯一目标：限制已解码缩略图和 UI 纹理数量。
- 依赖：ATOM-41。
- 允许修改：缩略图缓存、视口计算和缓存测试。
- 实现契约：
  - 默认只保留可视区及上下各10条。
  - 离开范围后释放 UI 纹理和解码缓冲。
  - 重新进入时按需加载。
- 验收：滚动500张图片时缓存容量保持有界。
- 风险等级：L3。
- 计划提交：`perf(image): 限制缩略图纹理缓存`。

#### [ ] ATOM-43 执行混合列表硬性能门禁

- 所属交付单元：UNIT-12。
- 唯一目标：证明最终文本和图片混合列表满足性能契约。
- 依赖：ATOM-42。
- 允许修改：混合性能场景、测量脚本和必要的原子内优化。
- 实现契约：
  - 10,000条文本摘要。
  - 10,000条图片摘要和代表性缩略图。
  - 使用最终卡片、分页和 LRU。
- 验收：达到内存、呼出、首批和滚动目标。
- 停止条件：失败必须切换窗口化 Model 并重新规划相关原子，不得拖到发布阶段。
- 风险等级：L2。
- 计划提交：`test(perf): 增加混合历史性能门禁`。

### 第九阶段：配置、隐私和清理

#### [ ] ATOM-44 建立配置工作线程

- 所属交付单元：UNIT-13。
- 唯一目标：设置可以可靠保存并从损坏中恢复。
- 依赖：ATOM-43。
- 允许修改：配置模型、SettingsWorker、默认值、原子保存和测试。
- 实现契约：
  - 配置位于 `%LOCALAPPDATA%\ClipboardBoard\config\settings.json`。
  - 同目录临时文件。
  - flush 后通过 Windows 原子替换发布。
  - 保留可恢复备份。
  - UI 线程不做文件 IO。
- 验收：缺失、损坏、未知字段、写入失败和备份恢复测试通过。
- 风险等级：L3。
- 计划提交：`feat(settings): 建立可靠配置存储`。

#### [ ] ATOM-45 支持暂停记录

- 所属交付单元：UNIT-13。
- 唯一目标：用户可以暂停读取剪贴板内容。
- 依赖：ATOM-44。
- 允许修改：记录策略、托盘菜单、计时状态和测试。
- 实现契约：
  - 支持5分钟、30分钟和无限期。
  - 在 ClipboardIO 读取正文前检查。
  - 定时暂停到期自动恢复。
  - 无限暂停跨重启保持。
- 验收：暂停期间文本和图片均未被读取或保存。
- 风险等级：L2。
- 计划提交：`feat(privacy): 支持暂停剪贴板记录`。

#### [ ] ATOM-46 支持排除指定程序

- 所属交付单元：UNIT-14。
- 唯一目标：被排除程序的复制内容不进入应用进程。
- 依赖：ATOM-45。
- 允许修改：排除规则、事件路由、配置快照和隐私测试。
- 实现契约：
  - 支持规范化完整路径和 exe 文件名。
  - Windows 大小写差异不影响匹配。
  - 在 ClipboardIO 读取正文前阻断。
- 验收：被排除程序的文本和图片均不进入历史，日志也不含正文。
- 风险等级：L3。
- 计划提交：`feat(privacy): 支持排除指定程序`。

#### [ ] ATOM-47 实现记录数量和保留天数清理

- 所属交付单元：UNIT-15。
- 唯一目标：普通历史按数量和保留天数自动收敛。
- 依赖：ATOM-46。
- 允许修改：StorageExecutor 清理命令、触发器和测试。
- 实现契约：
  - 启动完成后、插入后和配置变更后触发。
  - 删除最旧未收藏记录。
  - 不使用周期轮询。
- 验收：超过2000条或30天时收敛；收藏不被删除。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 清理过量和过期历史`。

#### [ ] ATOM-48 实现图片空间配额

- 所属交付单元：UNIT-15。
- 唯一目标：图片磁盘使用量自动回到配置上限。
- 依赖：ATOM-47。
- 允许修改：空间统计、配额清理命令和测试。
- 实现契约：
  - 统计 original 和 thumbnail。
  - 超限时淘汰最旧未收藏图片记录。
  - 记录删除提交后排队资产回收。
- 验收：最终空间不超过500 MiB；收藏图片保持。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 控制图片存储空间`。

#### [ ] ATOM-49 实现图片资产对账

- 所属交付单元：UNIT-15。
- 唯一目标：安全修复数据库和图片文件之间的非正常不一致。
- 依赖：ATOM-48。
- 允许修改：对账任务、StorageExecutor 屏障、缓存重建和测试。
- 实现契约：
  - 排除 staging、in-flight 和 recovery。
  - 删除前重新查询引用。
  - 缺失缩略图从原图重建。
  - 缺失原图记录隔离为不可用，不伪装可粘贴。
  - 启动后延迟执行，不阻塞首屏。
- 验收：不误删正在提交或共享哈希资产；异常状态最终收敛。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 对账图片记录与磁盘资产`。

### 第十阶段：设置和窗口模式

#### [ ] ATOM-50 增加历史设置界面

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以配置历史和图片限制。
- 依赖：ATOM-49。
- 允许修改：设置窗口、历史设置 DTO、校验和测试。
- 实现契约：
  - 最大记录数。
  - 保留天数。
  - 图片空间。
  - 是否记录图片。
  - 是否记录来源程序。
  - 保存后立即触发相关策略。
- 验收：非法值被拒绝；重启后保留。
- 风险等级：L2。
- 计划提交：`feat(settings): 增加历史记录设置`。

#### [ ] ATOM-51 增加隐私设置界面

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以管理排除程序和隐私开关。
- 依赖：ATOM-50。
- 允许修改：隐私设置页面、排除列表 UI 和测试。
- 实现契约：
  - 添加和删除排除程序。
  - 查看与调整暂停状态。
  - 窗口标题记录默认关闭。
  - 改动应用到下一次事件。
- 验收：配置保存和运行时策略同步。
- 风险等级：L2。
- 计划提交：`feat(settings): 增加隐私设置`。

#### [ ] ATOM-52 支持安全修改全局快捷键

- 所属交付单元：UNIT-16。
- 唯一目标：修改快捷键失败时不丢失当前可用热键。
- 依赖：ATOM-51。
- 允许修改：热键设置 UI、注册事务、配置保存和测试。
- 实现契约：
  - 先注册新热键。
  - 注册成功后保存配置。
  - 保存成功后使旧热键失效。
  - 旧热键注销失败时 handler 忽略旧 ID，并在下次启动清除。
  - 注册或保存失败时旧热键继续工作。
- 验收：冲突、保存失败和成功重启测试通过。
- 风险等级：L3。
- 计划提交：`feat(hotkey): 支持安全修改全局快捷键`。

#### [ ] ATOM-53 支持开机启动

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以控制当前账户登录时是否启动应用。
- 依赖：ATOM-52。
- 允许修改：HKCU 启动模块、设置 UI 和注册表抽象测试。
- 实现契约：
  - 使用当前用户 Run 项。
  - 正确引用包含空格的路径。
  - 操作幂等。
  - 不要求管理员权限。
- 验收：开启、关闭和重复操作正确。
- 风险等级：L2。
- 计划提交：`feat(startup): 支持当前用户开机启动`。

#### [ ] ATOM-54 支持固定窗口模式

- 所属交付单元：UNIT-17。
- 唯一目标：用户可以把临时面板切换为可长期停留的固定窗口。
- 依赖：ATOM-53。
- 允许修改：窗口状态机、固定按钮、拖动调整和尺寸配置。
- 实现契约：
  - 固定后失焦不隐藏。
  - 支持拖动和调整尺寸。
  - 保存合法尺寸。
  - 取消固定后恢复临时窗口规则。
- 验收：模式切换、重启恢复和非法尺寸回退测试通过。
- 风险等级：L2。
- 计划提交：`feat(window): 支持固定看板模式`。

### 第十一阶段：恢复和发布

#### [ ] ATOM-55 实现数据库损坏恢复

- 所属交付单元：UNIT-18。
- 唯一目标：数据库损坏时保留可恢复证据并启动新库。
- 依赖：ATOM-54。
- 允许修改：恢复协调器、存储启动、资产根切换和恢复测试。
- 实现契约：
  - 停止相关工作线程。
  - 把损坏数据库和整个图片资产根移动到时间戳 recovery 包。
  - 新数据库使用新的资产根。
  - 新库对账不得处理旧 recovery 资产。
  - 不自动删除 recovery 包。
- 验收：损坏库可被隔离；应用可启动；旧原图不会被误清。
- 风险等级：L3。
- 计划提交：`feat(recovery): 隔离损坏数据库与历史资产`。

#### [ ] ATOM-56 增加正式版本和图标资源

- 所属交付单元：UNIT-19。
- 唯一目标：Release 程序具有正确产品身份和图标。
- 依赖：ATOM-55。
- 允许修改：版本、Windows 资源、应用图标和托盘状态图标。
- 实现契约：Release 构建不显示控制台窗口。
- 验收：资源管理器、任务管理器和托盘显示正确。
- 风险等级：L1。
- 计划提交：`chore(release): 增加应用版本与图标资源`。

#### [ ] ATOM-57 建立发布性能回归

- 所属交付单元：UNIT-19。
- 唯一目标：自动判断 Release 构建是否满足性能预算。
- 依赖：ATOM-56。
- 允许修改：性能脚本、测试夹具和结果输出。
- 实现契约：
  - 采集待机内存。
  - 面板打开内存。
  - 呼出 P95。
  - 首批结果。
  - 搜索耗时。
  - 空闲 CPU。
  - 长滚动状态。
- 验收：全部指标达到第4节预算。
- 风险等级：L2。
- 计划提交：`test(perf): 建立发布性能回归`。

#### [ ] ATOM-58 创建 Inno Setup 安装程序

- 所属交付单元：UNIT-19。
- 唯一目标：生成不要求管理员权限的 Windows 安装包。
- 依赖：ATOM-57。
- 允许修改：Inno Setup 脚本、Release 构建入口和安装测试。
- 实现契约：
  - 当前用户安装。
  - 创建开始菜单入口。
  - 支持升级。
  - 卸载时由用户选择保留或删除应用数据。
- 明确不修改：不做代码签名和自动更新。
- 停止条件：缺少 Inno Setup 编译器且当前环境无法获得。
- 风险等级：L3。
- 计划提交：`build(installer): 增加Windows安装程序`。

#### [ ] ATOM-59 增加自动化发布冒烟测试

- 所属交付单元：UNIT-19。
- 唯一目标：自动验证 Release 二进制和安装包的核心路径。
- 依赖：ATOM-58。
- 允许修改：PowerShell 冒烟脚本、测试夹具和结果报告。
- 实现契约：
  - 启动。
  - 单实例。
  - 文本复制。
  - 重启恢复。
  - 搜索。
  - 清理。
  - 安装与卸载。
- 验收：测试可以重复执行且不会依赖手工遗留状态。
- 风险等级：L2。
- 计划提交：`test(release): 增加Windows发布冒烟测试`。

#### [ ] ATOM-60 完成发布验收矩阵

- 所属交付单元：UNIT-19。
- 唯一目标：形成正式版本发布决定所需的完整证据。
- 依赖：ATOM-59。
- 允许修改：验收脚本、测试夹具和中文发布验收记录。
- 验收范围：
  - Windows 10。
  - Windows 11。
  - 100%、150%、200% DPI。
  - 单屏和多屏。
  - Chrome、微信、VS Code、截图工具和画图来源。
  - 快速连续复制。
  - 管理员目标降级。
  - 剪贴板忙碌。
  - 数据库损坏。
  - 磁盘空间不足。
  - 图片缺失和损坏。
- 完成判定：所有必须项有通过证据；性能失败、数据丢失或错误窗口注入均阻止发布。
- 风险等级：L2。
- 计划提交：`test(release): 完成正式版本验收矩阵`。

## 8. 里程碑

### M0：技术骨架

- 原子范围：ATOM-01 至 ATOM-07。
- 结果：应用可以运行、通过热键打开、通过托盘退出，并具备线程和日志基础。

### M1：文本历史实现基线

- 原子范围：ATOM-08 至 ATOM-22。
- 结果：文本历史、持久化、键盘选择、仅复制和历史自动粘贴实现完成；其中自动粘贴随后由 ATOM-25R 按新产品决策撤销。
- 建议内部版本：`v0.1.0`。

### M2：文本完整 MVP

- 原子范围：ATOM-23 至 ATOM-29，包含 ATOM-25R。
- 结果：具备持续置顶、鼠标选择、显式复制、SQLite 分页、搜索、筛选、收藏、删除和清空。

### M3：图片完整 MVP

- 原子范围：ATOM-30 至 ATOM-43。
- 结果：图片捕获、缩略图、图片复制、图片兼容分页、无限滚动和内存控制完成。
- 建议内部版本：`v0.2.0`。

### M4：隐私和设置

- 原子范围：ATOM-44 至 ATOM-54。
- 结果：暂停、排除、清理、设置、启动项和固定窗口完成。

### M5：正式版本

- 原子范围：ATOM-55 至 ATOM-60。
- 结果：恢复、性能、安装和发布验收完成。
- 建议版本：`v1.0.0`。

## 9. 逐原子执行记录模板

完成每个原子后，在本节末尾追加记录，不覆盖历史。

```markdown
### YYYY-MM-DD ATOM-XX 原子名称

- 状态：已完成 / 已满足 / 已阻塞。
- 完成内容：
- 实际修改文件：
- 明确未做：
- 针对性验证：
- 回归验证：
- 中文注释检查：
- 风险等级：
- DDD 结果：
- 本地提交：`<hash> <message>`
- 远端同步：未授权，未推送 / 已按用户授权推送。
- 计划偏差：
- 下一原子：
```

### 2026-07-28 ATOM-01 建立可构建的 Slint 项目骨架

- 状态：已完成。
- 完成内容：建立 Cargo/Slint 项目骨架、黑底白字的 560×640 浮动窗口、应用构造入口和中文注释检查。
- 实际修改文件：`.gitignore`、`Cargo.toml`、`Cargo.lock`、`build.rs`、`src/lib.rs`、`src/main.rs`、`ui/app-window.slint`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划及项目阶段记录。
- 明确未做：全局快捷键、托盘、剪贴板读写、持久化、图片支持与业务命令。
- 针对性验证：`cargo check`、`cargo test`、`cargo clippy --all-targets --all-features -- -D warnings` 均通过；使用 `SLINT_BACKEND=winit-software` 实际启动窗口并通过 Win32 `PrintWindow` 核验黑色界面、白色主文字与灰色辅助文字已渲染。
- 回归验证：`cargo fmt --check` 通过。
- 中文注释检查：`scripts/check_comments.ps1` 与 `tests/comment_policy.rs` 均通过。
- 风险等级：L1。
- DDD 结果：不适用；本原子不涉及高风险边界。
- 本地提交：`c58862f feat(ATOM-01): 建立可运行的 Slint 黑色看板骨架`。
- 远端同步：未授权，未推送。
- 计划偏差：Slint 当前版本不允许同时指定固定宽高与最小宽高，已保留固定 560×640 尺寸；窗口尺寸策略将在 ATOM-54 单独实现。
- 下一原子：ATOM-02 统一 UI 事件入口与跨线程投递边界。

### 2026-07-28 ATOM-02 建立唯一 UI 事件入口

- 状态：已完成。
- 完成内容：定义拥有型 `UiEvent`/UI DTO，建立唯一 `post_ui_event` 入口，并用 UI 线程私有 `thread_local` reducer 接收事件。
- 实际修改文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/command.rs`、`src/app/mod.rs`、`src/app/ui_event.rs`、`tests/ui_event.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划及项目工作台。
- 明确未做：Win32 消息线程、全局热键、剪贴板监听/写回、窗口生命周期、业务存储和图片处理。
- 针对性验证：独立集成测试 `cargo test --test ui_event -- --exact 后台事件在事件循环线程更新状态 --test-threads=1` 通过；测试确认后台线程与 UI 线程不同，事件闭包在 UI 线程更新状态并返回快照。
- 回归验证：`cargo fmt --check`、`cargo check`、`cargo test`、`cargo clippy --all-targets --all-features -- -D warnings` 均通过；`git diff --check` 通过。
- 中文注释检查：`scripts/check_comments.ps1` 与 `tests/comment_policy.rs` 已扩展覆盖 ATOM-02 新增源码和测试，并均通过。
- 风险等级：L2。
- DDD 结果：提交前隔离审查 PASS；无阻断问题。已记录测试后端单测试隔离、DTO Send/Sync 和唯一入口不变量。
- 本地提交：`b661502 feat(ATOM-02): 建立线程安全的 UI 事件入口`。
- 远端同步：未授权，未推送。
- 计划偏差：ATOM-01 尚无可变 Slint 属性，因此以 UI 线程专属 Rust `UiState` 证明线程边界；后续接入 Slint 模型时必须继续在同一投递闭包内转换和写入。
- 下一原子：ATOM-03 注册 Alt+V 全局快捷键。

### 2026-07-28 ATOM-03 注册 Alt+V 全局快捷键

- 状态：已完成。
- 完成内容：创建专用 Windows message-only HWND 线程，注册/注销 Alt+V，过滤固定热键 ID，并将 `WM_HOTKEY` 唯一转换为 `UiEvent::OpenPanel`。
- 实际修改文件：`Cargo.toml`、`Cargo.lock`、`src/main.rs`、`src/lib.rs`、`src/command.rs`、`src/app/mod.rs`、`src/app/ui_event.rs`、`src/platform/mod.rs`、`src/platform/windows/mod.rs`、`src/platform/windows/hotkey.rs`、`src/platform/windows/system_window.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划及项目工作台。
- 明确未做：单实例互斥、托盘、剪贴板监听/写回、快捷键设置、焦点恢复和多显示器窗口生命周期。
- 针对性验证：Windows 桌面冒烟通过；向实际创建的 `ClipboardBoardHotkey` message-only HWND 注入一次 `WM_HOTKEY(id=0x4342)` 后，主面板从不可见变为可见，并用 `SLINT_BACKEND=winit-software` 截取黑底看板；预占 Alt+V 后应用明确退出码为 1。
- 回归验证：`cargo check --all-targets`、`cargo test --all-targets`、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo fmt --check` 和 `git diff --check` 均通过。
- 中文注释检查：`scripts/check_comments.ps1` 与 `tests/comment_policy.rs` 已覆盖全部 Windows 热键模块并通过。
- 风险等级：L2。
- DDD 结果：编码前和提交前隔离审查均 PASS；已确认 HWND 线程所有权、热键冲突硬失败、WM_HOTKEY 单次投递和关闭注销路径。
- 本地提交：`9f0ba72 feat(ATOM-03): 支持 Alt+V 呼出面板`。
- 远端同步：未授权，未推送。
- 计划偏差：采用轻量 `windows-sys` 原生绑定；为实现“按热键才显示”，启动时隐藏 Slint 窗口，`OpenPanel` 在 UI 闭包内升级弱引用并显示，后续 ATOM-05 将继续完善焦点和失焦生命周期。
- 下一原子：ATOM-04 保证程序单实例运行。

### 2026-07-28 ATOM-04 保证程序单实例运行

- 状态：已完成。
- 完成内容：在 UI、SQLite 和热键初始化前获取 `Local\\ClipboardBoard.Instance.v1` 命名互斥体；主实例持有 `SingleInstanceGuard` 至事件循环和热键线程退出；二次启动在有界重试内查找主实例 message-only HWND，通过固定 `WM_APP + 1` 消息投递 `UiEvent::OpenPanel` 后退出；通知失败时再次尝试夺取互斥体，避免主实例竞态退出造成死锁。
- 实际修改文件：`Cargo.toml`、`src/main.rs`、`src/platform/windows/mod.rs`、`src/platform/windows/system_window.rs`、`src/platform/windows/single_instance.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划及项目工作台。
- 明确未做：SQLite 初始化、剪贴板监听/写回、托盘、管理员权限提升、远端 IPC 和快捷键设置。
- 针对性验证：Windows 进程测试确认主实例保持存活，第二实例以退出码 0 结束，只有一个 message-only HWND，且实际主面板最终可见；强制结束主实例后新进程重新取得互斥体并创建 HWND；PowerShell 仅持有同名互斥体而不创建 HWND 时，程序在约 1.9 秒内以退出码 1 结束，证明通知失败不会静默成功。
- 回归验证：`cargo fmt --check`、`cargo check --all-targets`、`cargo test --all-targets`（8 个 Windows 单元测试、注释策略集成测试和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`git diff --check` 均通过。
- 中文注释检查：`scripts/check_comments.ps1` 与 `tests/comment_policy.rs` 已覆盖单实例模块并通过。
- 风险等级：L3。
- DDD 结果：编码前隔离审查补齐启动竞态、通知失败、重新夺取和用户级命名契约；提交前隔离审查 PASS，无阻断问题。
- 本地提交：`7ee7d67 feat(ATOM-04): 保证应用单实例运行`。
- 远端同步：未授权，未推送。
- 计划偏差：采用固定 `WM_APP + 1` 与唯一窗口类名作为本版本 IPC 协议，没有引入跨用户 SID 计算或 `RegisterWindowMessageW`；`Local` 命名空间和调用方默认 DACL 保持用户会话级、无管理员依赖。
- 下一原子：ATOM-05 完成临时面板生命周期。

### 2026-07-28 ATOM-05 完成临时面板生命周期

- 状态：已完成。
- 完成内容：在 UI 线程建立打开代次 reducer；支持重复 `Alt + V` 切换、Esc 和失焦关闭；显示前捕获前台目标 HWND、PID 与完整性级别；按鼠标所在显示器工作区和 Windows 外框物理尺寸居中；窗口创建异步时用有界 Slint 定时器重试原生定位；显示器查询失败时回退主显示器或保持当前窗口位置。
- 实际修改文件：`src/command.rs`、`src/app/ui_event.rs`、`ui/app-window.slint`、`src/platform/windows/mod.rs`、`src/platform/windows/window/mod.rs`、`src/platform/windows/window/lifecycle.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划、项目工作台和阶段记录。
- 明确未做：不执行自动粘贴、不恢复目标焦点、不实现固定模式、不实现剪贴板读写和托盘；保存的目标快照必须由后续自动粘贴原子重新查询并比较，身份不匹配只能复制。
- 针对性验证：Windows 桌面冒烟向实际 `ClipboardBoardHotkey` HWND 注入 `WM_HOTKEY(id=0x4342)`，确认面板可见；再次注入确认隐藏且进程继续存活；第三次注入后发送 `WM_KEYDOWN/WM_KEYUP(VK_ESCAPE)`，确认隐藏；读取实际外框矩形 `672,200–1248,879`，与当前显示器工作区中心一致；代码路径覆盖失焦回调和 generation 过期事件。当前自动化桌面没有可激活的前台窗口（`GetForegroundWindow()` 返回空），因此失焦未做真实跨窗口手工闭环。
- 回归验证：`cargo fmt --all -- --check`、`cargo check --all-targets`、`cargo test --all-targets`（12 个单元测试、注释策略集成测试和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过；令牌 SID 子指针判空后的完整回归再次通过。
- 中文注释检查：新增 Windows 窗口模块、Slint 回调和测试清单均已加入机械检查；脚本与集成测试均通过。
- 风险等级：L2。
- DDD 结果：编码前隔离审查提出的目标快照复核、generation 关闭协议和工作区边界已补入契约；提交前隔离审查 PASS，已确认 SID 指针安全降级、句柄释放、UI/Win32 线程隔离和不执行粘贴边界。
- 本地提交：`cddfc65 feat(ATOM-05): 完成临时面板显示与隐藏`。
- 远端同步：未授权，未推送。
- 计划偏差：为覆盖无交互桌面增加主显示器 fallback；Slint/Winit 首次显示会覆盖位置，因此延迟到 HWND 创建后使用 `SetWindowPos`，并读取外框物理尺寸而非只使用内容尺寸；继续沿用项目既有 `windows-sys` 绑定。
- 下一原子：ATOM-06 增加系统托盘入口。

### 2026-07-28 ATOM-06 增加系统托盘入口

- 状态：已完成。
- 完成内容：在热键消息线程创建绑定 message-only HWND 的托盘图标；提供仅含“打开/退出”的菜单；“打开”投递幂等 `ShowPanel`；“退出”投递 UI `Quit`，由 UI 线程退出事件循环后复用主线程 stop 流程。
- 实际修改文件：`Cargo.toml`、`src/command.rs`、`src/app/ui_event.rs`、`src/main.rs`、`src/platform/windows/hotkey.rs`、`src/platform/windows/mod.rs`、`src/platform/windows/system_window.rs`、`src/platform/windows/tray.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划。
- 明确未做：不实现托盘暂停、设置、动态图标、多级菜单和剪贴板业务；菜单严格保持“打开/退出”两项。
- 针对性验证：`ShowPanel` 幂等测试、`Quit` 闩锁测试、托盘 ID/消息过滤和 `NOTIFYICON_VERSION_4` 高低字回调解码测试通过；Windows 启动冒烟确认进程存活、`ClipboardBoardHotkey` 消息窗口存在且 v4 托盘回调可投递；自动化桌面未暴露 Shell 托盘菜单窗口，无法真实点击菜单项，故记录为环境限制。
- 回归验证：`cargo fmt --all -- --check`、`cargo check --all-targets`、`cargo test --all-targets`（18 个单元测试、注释策略集成测试和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：托盘模块、UI 事件、系统窗口清理路径和测试清单均已纳入机械检查并通过。
- 风险等级：L2。
- DDD 结果：编码前审查补齐幂等显示、退出闩锁、资源所有权和同步菜单退出边界；提交前隔离审查 PASS，并修正 `NOTIFYICON_VERSION_4` 回调高低字解码。
- 本地提交：`91cbb4f feat(ATOM-06): 增加系统托盘入口`。
- 远端同步：未授权，未推送。
- 计划偏差：为满足线程边界新增 `ShowPanel/Quit` 事件和 `Quit` reducer 闩锁；显式启用 `Win32_UI_Shell`；使用系统应用图标的私有副本并在 Drop 销毁；message-only HWND 无法置前时仅记录诊断而继续菜单调用。
- 下一原子：ATOM-07 建立不记录正文的诊断日志。

### 2026-07-28 ATOM-07 建立不记录正文的诊断日志

- 状态：已完成。
- 完成内容：建立仅包含记录 ID、有限类型、字节数、耗时、错误码和线程状态的 `DiagnosticEvent`；固定字段序列化；全局 `OnceLock<Mutex<...>>` writer 使用非阻塞 `try_lock`；本地文件写入失败时回退固定 stderr，写入或刷新失败后静默禁用；仅主实例在单实例判定后初始化日志，并记录启动、运行、停止生命周期。
- 实际修改文件：`src/diagnostics.rs`、`src/lib.rs`、`src/main.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划。
- 明确未做：不记录剪贴板正文、图片字节、窗口标题、线程名称、路径、错误消息或任意外部字符串；未引入第三方日志依赖、轮转和远程上报。
- 针对性验证：诊断模块 3 个测试通过；特征正文仅作为测试变量且最终输出不包含；耗时转换按毫秒饱和；失败 writer 进入静默禁用；Windows 实际启动后在 `%LOCALAPPDATA%\\ClipboardBoard\\logs\\diagnostics.log` 生成固定字段日志，日志尾部仅含 `starting` 与 `running` 状态。
- 回归验证：`cargo fmt --all`、`cargo check --all-targets`、`cargo test --all-targets`（21 个测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：诊断模块公共字段、私有 writer 字段、序列化边界、失败降级和并发入口均已补充中文职责说明，机械检查通过。
- 风险等级：L2。
- DDD 结果：提交前隔离审查 PASS；已确认正文不可达、固定字段白名单、主实例初始化时序、try-lock 非阻塞、单行一次写入、flush 失败静默禁用和 stderr 回退边界。
- 本地提交：`53a3eaf feat(ATOM-07): 建立隐私安全诊断日志`。
- 远端同步：未授权，未推送。
- 计划偏差：采用固定本地文件加 stderr 回退，未引入 `tracing`；诊断事件只服务启动生命周期，后续剪贴板业务事件仍须复用同一白名单接口。
- 下一原子：ATOM-08 定义文本记录领域模型。

### 2026-07-28 ATOM-08 定义文本记录领域模型

- 状态：已完成。
- 完成内容：新增 `ClipboardPayload::Text` 完整拥有型正文和 `ClipboardItemSummary` 列表摘要；文本规范化只去除尾部 NUL；摘要保留空格、换行、制表符和 Unicode，并在 512 个 Unicode 字符处截断；BLAKE3 哈希使用固定 `ClipboardBoard/text/v1` 域标签。
- 实际修改文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/domain/mod.rs`、`src/domain/clipboard_item.rs`、`src/domain/hash.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划。
- 明确未做：不读取 Windows 剪贴板、不采集来源程序、不写 SQLite、不提供图片 payload、不把摘要接入 UI 列表。
- 针对性验证：领域模块 6 个测试通过，覆盖尾部/内部 NUL、空白、Unicode、固定域标签、摘要边界和 hash 一致性；大文本摘要只保留上限内字符，完整正文仍由 payload 持有。
- 回归验证：`cargo fmt --all`、`cargo check --all-targets`、`cargo test --all-targets --locked`（27 个测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：领域模块、公开结构体字段、枚举、方法参数边界和哈希/摘要关键分支均有中文职责说明，机械检查通过。
- 风险等级：L1。
- DDD 结果：不适用；纯领域数据和函数无跨线程、权限、持久化或不可逆副作用。
- 本地提交：`d4b1c69 feat(ATOM-08): 定义文本剪贴板记录模型`。
- 远端同步：未授权，未推送。
- 计划偏差：因原计划未列出 BLAKE3 依赖和 Rust 模块入口，已先将计划升级至 v5 并限定新增范围为依赖锁定、模块 wiring 和注释门禁；未扩展业务行为。
- 下一原子：ATOM-09 读取复制来源程序。

### 2026-07-28 ATOM-09 读取复制来源程序

- 状态：已完成。
- 完成内容：新增 `ProcessSource` 来源快照和 `capture_foreground_source` 入口；从前台 HWND 获取 PID，以 `PROCESS_QUERY_LIMITED_INFORMATION` 打开进程并读取映像路径；仅保留 exe 文件名和由文件名派生的显示名称，默认不读取窗口标题、内容或进程内存；所有失败均返回 `None`。
- 实际修改文件：`src/platform/windows/source.rs`、`src/platform/windows/mod.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划。
- 明确未做：不接入 WM_CLIPBOARDUPDATE、不保存来源到 SQLite、不读取窗口标题、不读取版本资源、不实现排除列表。
- 针对性验证：来源模块 5 个测试通过，覆盖路径只保留文件名、空路径、空窗口句柄、无效 PID 和当前进程受限查询；当前进程查询确认可得到非空 exe/显示名和 PID。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（32 个测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：来源结构体字段、公开入口、句柄关闭、权限降级、UTF-16 边界和路径隐私处理均有中文职责说明，机械检查通过。
- 风险等级：L2。
- DDD 结果：提交前隔离审查 PASS；已确认最小进程权限、句柄必然关闭、无标题读取、无效来源 fail-safe 和目录路径不进入快照。
- 本地提交：`46093ff feat(ATOM-09): 识别剪贴板来源程序`。
- 远端同步：未授权，未推送。
- 计划偏差：显示名称采用 exe 文件名去除扩展名的本地派生值，不读取版本资源，避免引入额外 API 和权限；后续若要显示正式产品名需另立原子。
- 下一原子：ATOM-10 在线程中读取 Unicode 文本。

### 2026-07-28 ATOM-10 在 ClipboardIO 线程读取 Unicode 文本

- 状态：已完成。
- 完成内容：建立容量为 1 的 `ClipboardIoWorker`；用 `OpenClipboard` 200ms 总预算和 5ms 间隔重试；读取前后核对 sequence；`CF_UNICODETEXT` 通过 HGLOBAL 边界读取，在关闭剪贴板前复制成拥有型 `ClipboardPayload`；采用两遍 `decode_utf16`，第一遍精确累计 UTF-8 字节并在 5 MiB 上限前拒绝，第二遍按精确容量构造正文。
- 实际修改文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/clipboard/mod.rs`、`src/clipboard/io_worker.rs`、`src/clipboard/reader.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`、本计划。
- 明确未做：不注册 `WM_CLIPBOARDUPDATE`、不读取来源快照、不写 SQLite、不更新 UI、不处理图片格式、不实现自身写回事务。
- 针对性验证：ClipboardIO 43 个测试通过，覆盖中英文/换行/空文本、3 MiB ASCII 合法边界、超限多字节提前拒绝、非法 UTF-16、缺少 NUL、剪贴板忙碌重试、持续占用有界失败、前后 sequence 失配、关闭失败和 worker 异步停止。
- 回归验证：`cargo fmt --all`、`cargo check --all-targets`、`cargo test --all-targets --locked`（43 个测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：ClipboardIO 模块、公开错误/策略/API、Win32 句柄所有权、锁定边界、重试和 worker 关闭路径均有中文职责说明，机械检查通过。
- 风险等级：L3。
- DDD 结果：提交前对抗审查首轮发现 UTF-16 扫描预算会误拒绝合法 ASCII 且可能先分配超限正文；已改为 `max_bytes + 1` 扫描和两遍增量解码，第二轮 DDD/终审 PASS。
- 本地提交：`fb86ad4 feat(ATOM-10): 在线程中安全读取Unicode文本`。
- 远端同步：未授权，未推送。
- 计划偏差：新增 `Win32_System_DataExchange`、`Win32_System_Memory` feature 和 ClipboardIO 模块 wiring；硬编码 Windows `CF_UNICODETEXT=13` 是 windows-sys 当前绑定缺少该常量的兼容处理；明确未提前接入监听原子。
- 下一原子：ATOM-11 接入 `WM_CLIPBOARDUPDATE`。

### 2026-07-28 ATOM-11 接入 WM_CLIPBOARDUPDATE

- 状态：已完成。
- 完成内容：在 message-only HWND 上注册和注销 `AddClipboardFormatListener`；消息线程收到 `WM_CLIPBOARDUPDATE` 后只捕获剪贴板 sequence 与前台来源快照，并把请求交给容量为一的 `Mutex + Condvar` latest-wins 队列；worker 复核读取前后 sequence，失配时丢弃正文；新增公开 `ClipboardCaptureInbox` 结果桥和 `HotkeyManager::clipboard_inbox()`，让后续原子可以在不访问私有 HWND/TLS 的前提下非阻塞消费结果。
- 实际修改文件：`src/clipboard/io_worker.rs`、`src/clipboard/mod.rs`、`src/platform/windows/hotkey.rs`、`src/platform/windows/system_window.rs`、本计划。
- 明确未做：不把捕获结果接入 UI、SQLite、图片处理、去重或自身写回事务；`WM_CLIPBOARDUPDATE` 消息线程不读取正文，结果桥只保留最新一项。
- 针对性验证：ClipboardIO、latest-wins 请求队列、sequence/source 同请求绑定、结果桥 latest-wins、结果桥关闭后保留已发布结果、worker 结果桥回传和剪贴板消息过滤测试通过；共 49 个库测试，另有注释策略与 UI 事件集成测试通过。
- 回归验证：`cargo fmt --all`、`cargo check --all-targets`、`cargo test --all-targets --locked`（49 个库测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：监听注册/注销、消息线程职责、latest-wins 队列、结果桥、关闭顺序、公共消费 API 和来源/序号绑定均已补充中文职责说明，机械检查通过。
- 风险等级：L3。
- DDD 结果：提交前隔离审查 PASS；首轮发现私有 Receiver 无下游、结果不可观测和停止时结果桥关闭竞态，已改为公开 `ClipboardCaptureInbox`，并调整为先关闭请求队列并 join worker、再关闭结果桥；复核确认监听生命周期、A/B 绑定、latest-wins 和结果接缝通过。
- 本地提交：`e91f7e4 feat(ATOM-11): 监听Windows剪贴板更新`。
- 远端同步：未授权，未推送。
- 计划偏差：计划升级至 v8，明确结果桥文件边界并扩展 ATOM-12 的消费接缝；真实桌面剪贴板事件未在自动化环境执行，使用 Win32 编译验证与可注入/队列单元测试覆盖核心边界。
- 下一原子：ATOM-12 将文本结果展示在列表顶部。

### 2026-07-28 ATOM-12 将文本结果展示在列表顶部

- 状态：已完成。
- 完成内容：新增 `UiClipboardItem` 摘要 DTO 和 `ClipboardCaptured` 事件；结果泵从 `ClipboardCaptureInbox` 消费捕获结果，只转换为摘要、来源和“刚刚”文案，再通过唯一 `post_ui_event` 进入 UI 线程；reducer 将新卡片置顶并将内存列表限制为 50 条；Slint 看板新增黑底白字文本摘要卡片、来源/时间信息和空状态。
- 实际修改文件：`src/app/ui_event.rs`、`src/command.rs`、`src/lib.rs`、`src/main.rs`、`tests/ui_event.rs`、`ui/app-window.slint`。
- 明确未做：不访问 SQLite、不写回系统剪贴板、不实现重复合并、图片卡片、滚动虚拟化和完整历史正文模型。
- 针对性验证：`UiClipboardItem::from_capture`、来源缺失回退、摘要置顶和选中索引移动测试通过；结果泵只经 `post_ui_event` 投递，完整正文未进入 `UiEvent`、`UiSnapshot` 或 Slint 卡片模型。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（53 个库测试、注释策略集成测试和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --all-targets`、`scripts/check_comments.ps1`、`git diff --check` 均通过；Windows MSVC 目标编译证据保留在本轮审查中。
- 中文注释检查：新增 Rust、Slint 文件职责、DTO 字段、线程泵、UI 模型转换和卡片布局说明均通过机械检查。
- 风险等级：L2。
- DDD 结果：编码后隔离终审 PASS；确认结果泵仅在独立线程等待 Condvar，不阻塞 UI、Win32 消息线程或 worker；所有 Slint setter 都在 `invoke_from_event_loop` 闭包内；关闭顺序不会造成死锁。计划中的“非阻塞消费”按不阻塞关键生产/UI路径解释，`wait_take` 的等待不会扩大阻塞面。
- 本地提交：`6683f79 feat(ATOM-12): 展示最近复制的文本摘要`。
- 远端同步：未授权，未推送。
- 计划偏差：为保持 UI 和消息线程空闲，结果泵使用 Condvar 阻塞等待而不是 UI 线程轮询；当前卡片模型只保留最多 50 条摘要，真实滚动/虚拟列表性能将在 ATOM-14 验证。
- 下一原子：ATOM-13 合并重复文本。

### 2026-07-28 ATOM-13 合并重复文本

- 状态：已完成。
- 完成内容：新增 `MemoryHistory` 深模块，按规范化 `content_hash` 合并重复文本；重复复制只更新时间、饱和增加 `copy_count` 并置顶，保留原摘要、来源、ID 和 `is_pinned`；新记录和恢复快照都遵守容量上限与唯一哈希不变量；UI reducer 按哈希重定位选中项。
- 实际修改文件：`src/history.rs`、`src/command.rs`、`src/lib.rs`、`src/app/ui_event.rs`、`tests/ui_event.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`。
- 明确未做：不接入 SQLite、不写回系统剪贴板、不改变 ClipboardIO 线程、不实现图片和搜索。
- 针对性验证：覆盖不同/相同哈希排序、重复合并、收藏保留、恢复快照去重、零容量、容量淘汰、零计数归一化、`u64::MAX` 计数饱和，以及去重/容量裁剪后的选中项身份保持。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（62 个库测试、注释策略集成测试和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --all-targets`、`cargo check --target x86_64-pc-windows-msvc --all-targets`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：新增历史模块及其公共接口、字段、容量/去重分支均有中文职责说明；两个注释门禁清单已纳入 `src/history.rs`。
- 风险等级：L2。
- DDD 结果：提交前隔离终审 PASS；曾发现恢复快照去重后按旧索引截断可能错选，已改为按内容哈希重定位；曾发现新历史文件未进入注释门禁，已补齐 PowerShell 与集成测试清单。
- 本地提交：`25b0c24 feat(ATOM-13): 合并重复文本历史`。
- 远端同步：未授权，未推送。
- 计划偏差：历史模块暂时承载 UI 摘要 DTO，作为 ATOM-18 接入 SQLite 前的内存实现；数据库真相源和事务可见性仍由后续原子替换。
- 下一原子：ATOM-14 已按实测停止条件触发重规划，转入 ATOM-14R 窗口化历史列表。

### 2026-07-28 ATOM-14 验证 Slint 大列表策略

- 状态：已阻塞（硬门禁未通过，按契约重规划）。
- 完成内容：新增 Windows Release 性能探针、20,000 条固定高度摘要数据生成器、工作集采样和硬门禁测量脚本。
- 实际修改文件：`Cargo.toml`、`tests/list_performance.rs`、`scripts/measure_list_performance.ps1`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`。
- 明确未做：不把当前 `VerticalLayout` 宣称为可滚动，不改生产 UI、不接入存储、不改变历史规则。
- 针对性验证：Release 实测 `item_count=20000`、呼出 P95 `29.725ms`、首批 `0.032ms`、工作集 `57.242MiB`；前三项达标，但 `long_scroll_supported=false`，脚本按停止条件返回非零退出码。
- 回归验证：普通 `cargo test --all-targets --locked`（62 个库测试、注释策略和 UI 事件集成测试）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --all-targets`、`cargo check --target x86_64-pc-windows-msvc --all-targets`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：性能测试和测量脚本的文件职责、常量、Windows API 边界、失败判定均有中文说明，并已纳入两个注释门禁清单。
- 风险等级：L2。
- DDD 结果：隔离终审待回报；当前失败是可重复的结构性证据，不应通过放宽门禁掩盖。
- 本地提交：`4bff4e0 test(ui): 增加大文本列表性能探针`。
- 远端同步：未授权，未推送。
- 计划偏差：前三项资源指标满足目标，但当前看板只有 `VerticalLayout`，缺少真实长滚动；按停止条件新增 ATOM-14R，ATOM-15 改为依赖 ATOM-14R。
- 下一原子：ATOM-14R 接入窗口化历史列表。

### 2026-07-28 ATOM-14R 接入窗口化历史列表

- 状态：已完成。
- 完成内容：将看板历史内容区从 `VerticalLayout` 改为 Slint compiler-recognized `ListView`/`Flickable` 窗口化列表；固定 delegate 高度为 106px（96px 黑色卡片加 10px 间距），保留黑底白字视觉语法，并暴露模型长度、视口偏移和实际内容高度探针。
- 实际修改文件：`ui/app-window.slint`、`tests/list_performance.rs`、`scripts/measure_list_performance.ps1`。
- 明确未做：不接入 SQLite、不改变历史去重规则、不实现图片懒加载和搜索。
- 针对性验证：Windows Release 20,000 条模型实测 `open_p95_ms=0.146`、`first_batch_ms=0.140`、峰值工作集 `16.371 MiB`、`long_scroll_supported=true`、`long_scroll_p95_ms=0.162`、200 次滚动采样、最大窗口批次 5 行、首行 0、末行 19999、空批次 0、视口不匹配 0；探针通过 `mock_elapsed_time`、`CountingModel` 和实际 viewport/content-height 驱动滚动，并非替换模型耗时伪造。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（62 个库测试、注释策略和 UI 事件集成测试，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --all-targets`、`cargo check --target x86_64-pc-windows-msvc --all-targets`、`scripts/check_comments.ps1`、`git diff --check`，以及直接执行和 Windows PowerShell `-File` 方式执行测量脚本均通过。
- 中文注释检查：新增 Slint ListView/Flickable 约束、性能测试模型/滚动采样、PowerShell 参数解析和失败门禁说明，并纳入两个中文注释门禁清单。
- 风险等级：L3。
- DDD 结果：隔离终审 PASS；确认 ListView 使用编译器窗口化 repeater，测试通过实际行访问和视口高度证据验证；headless backend 的耗时是结构性更新证据，不冒充原生桌面像素帧率，原生 Windows 人工帧率仍留给后续验证。
- 本地提交：`0d48d4c feat(ui): 接入窗口化历史列表`。
- 远端同步：未授权，未推送。
- 计划偏差：首次探针暴露 `VerticalLayout` 缺少长滚动能力，按 ATOM-14 停止条件新增本原子；本原子完成后 ATOM-15 恢复为下一步。
- 下一原子：ATOM-15 建立唯一 SQLite 存储线程。

### 2026-07-28 ATOM-15 建立唯一 SQLite 存储线程

- 状态：已完成。
- 完成内容：新增不可 Clone 的 `StorageExecutor`，以有界命令协议独占 worker 线程内的唯一 `rusqlite::Connection`；连接就绪前完成 v1 事务迁移，公共 API 只返回不可变状态和线程探针，不暴露 Connection/Statement/SQL 句柄。
- 实际修改文件：`Cargo.toml`、`Cargo.lock`、`src/lib.rs`、`src/storage/mod.rs`、`src/storage/worker.rs`、`src/storage/migration.rs`、`src/storage/migrations/001_initial.sql`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`。
- 明确未做：不实现文本 upsert、游标查询、UI 持久化接线、剪贴板写回、搜索、图片和清理。
- 针对性验证：固定 `rusqlite = 0.40.1`、`default-features = false`、`bundled`；`PRAGMA user_version` 覆盖 0→1、1 重跑、未来版本拒绝；精确 v1 `clipboard_items` 字段/默认值、AUTOINCREMENT、content_hash 全局唯一索引、copied_at DESC/id DESC 复合索引校验；partial/表达式/非 BINARY 索引拒绝；迁移失败后版本与索引无半更新；哨兵记录重启后保留；真实连接执行 `SELECT 1` 的创建线程、探针线程和调用线程断言；显式 shutdown 在目录清理前完成 join；LOCALAPPDATA 缺失和 `open_at` 临时目录隔离测试通过。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（73 个库测试、注释策略和 UI 事件集成测试，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check`、`cargo check --target x86_64-pc-windows-msvc --all-targets` 均通过。
- 中文注释检查：存储错误、公共执行器/状态字段、迁移约束、线程所有权、ready/关闭顺序、测试隔离和 SQL 文件职责均有中文说明，并纳入注释门禁清单。
- 风险等级：L3。
- DDD 结果：编码前 v10 预审发现 root wiring、迁移精度、生命周期、路径隔离和真实连接线程证据缺口，已升级计划 v11 并补齐；提交前复审又补充 partial/表达式索引和 panic 错误优先级，最终 PASS。
- 本地提交：`1bc4d6e feat(storage): 建立单线程SQLite执行器`。
- 远端同步：未授权，未推送。
- 计划偏差：为使坏 schema 不被误判为幂等，迁移增加了 `PRAGMA table_info/index_xinfo` 和 DDL/索引排序校验；`status` 使用 `&mut self` 以便在 worker 断连时回收句柄并保留 panic 诊断，这不改变后续业务边界。
- 下一原子：ATOM-16 实现文本事务 upsert。

### 2026-07-28 ATOM-16 实现文本事务 upsert

- 状态：已完成。
- 完成内容：为 `StorageExecutor` 增加公开的 `TextUpsertInput`、`TextUpsertResult` 和有界 `UpsertText` 命令；首次文本写入固定默认字段，重复哈希只更新 `copied_at` 并按 `i64::MAX` 饱和递增 `copy_count`，异常的零/负计数归一为 2；结果在同一 SQLite 事务中读取后才提交。
- 实际修改文件：`src/storage/worker.rs`、`src/storage/mod.rs`。
- 明确未做：不实现游标查询、完整 payload 查询、UI 持久化接线、剪贴板写回、搜索、图片和清理。
- 针对性验证：首次插入、同毫秒重复只保留一行、正文/预览/来源/创建时间/收藏/最近使用字段保留；`i64::MAX` 饱和、零/负计数归一；`BEFORE UPDATE ... RAISE(ABORT,...)` 触发器使事务回滚，旧行全部字段不变且 worker 仍可执行 `status` 探针。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（76 个库测试、注释策略和 UI 事件集成测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过；`cargo check --target x86_64-pc-windows-gnu --locked` 因本机未安装该 Rust target 未形成目标编译证据。
- 中文注释检查：新增公共输入/结果字段、命令协议、事务 SQL、饱和分支、线程失败映射和回滚测试均补充中文职责与边界说明。
- 风险等级：L2。
- DDD 结果：提交前隔离终审 PASS；确认公共接缝、唯一连接、同事务读取、幂等计数、旧字段保留和失败后 worker 可用性均符合 v13 契约；不同 `copied_at` 的额外断言属于非阻塞增强项。
- 本地提交：`a5e8e85 feat(storage): 支持文本历史事务写入`。
- 远端同步：未授权，未推送。
- 计划偏差：无；SQLite schema 和历史正文仍由 ATOM-15 的 v1 结构承载，本原子只增加文本写入行为。
- 下一原子：ATOM-17 实现历史摘要游标查询。

### 2026-07-28 ATOM-17 实现历史摘要游标查询

- 状态：已完成。
- 完成内容：为 `StorageExecutor` 增加 `HistoryCursor`、有界 `HistoryPage`、摘要查询和按 ID payload 查询；首页与后续页均按 `copied_at DESC, id DESC` 排序，后续页使用复合游标谓词，多取一行决定 `next_cursor`，并固定锚定本页最后一条已返回记录；摘要不加载完整正文。
- 实际修改文件：`src/storage/mod.rs`、`src/storage/worker.rs`。
- 明确未做：不接入捕获事件/UI、不开启启动恢复、不实现搜索筛选、图片、清理或剪贴板写回，不使用 `OFFSET`。
- 针对性验证：5 条含同毫秒记录分页得到 `[3,2]`、`[1,4]`、`[5]`，无重复和遗漏；空库、零页大小、101 越界、尾部游标均有断言；按 ID payload 返回正文、预览、来源、哈希、收藏和时间字段；非 text 类型、NULL 正文/来源和短 BLOB 哈希语义保留；未知 ID 返回 `None`。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（80 个库测试、注释策略和 UI 事件集成测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：新增公共游标/摘要页/payload 字段、页大小错误、SQL 排序/游标谓词、多取一行边界和 payload NULL 映射均有中文说明。
- 风险等级：L2。
- DDD 结果：编码前 v14 预审发现 next_cursor 锚点和 payload NULL/类型语义缺口，计划升级到 v15 后重新预审 PASS；提交前终审 PASS，确认排序、谓词、无 OFFSET、尾页和唯一 worker 接缝符合契约。
- 本地提交：`77a6de5 feat(storage): 支持历史摘要游标查询`。
- 远端同步：未授权，未推送。
- 计划偏差：无；页大小上限 100 是 v15 契约明确的有界内存保护，未改变查询行为范围。
- 下一原子：ATOM-18B 接入启动恢复和有界 UI 缓存。

### 2026-07-28 ATOM-18A 接入捕获提交后可见

- 状态：已完成。
- 完成内容：主程序在启动热键前创建唯一 `StorageExecutor` 并移动给剪贴板结果泵；新增 `history_bridge`，按“构造 upsert → 等待 SQLite 事务 → 转换持久化结果 → 投递 UI”顺序处理捕获；数据库错误、DTO 转换错误和 UI sink 关闭均有固定控制流，读取/sequence 错误继续丢弃当前结果。
- 实际修改文件：`src/main.rs`、`src/command.rs`、`src/history_bridge.rs`、`src/history.rs`、`src/app/ui_event.rs`、`src/lib.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`。
- 明确未做：不实现启动恢复、首批 30 条裁剪、滚动分页、搜索、图片、剪贴板写回和清理。
- 针对性验证：成功路径以数据库返回 ID/预览/来源/计数/收藏生成 UI；`open_at + BEFORE UPDATE` trigger 使 upsert 失败且无 UI 事件，同一执行器随后可处理新哈希；非法 DTO 返回 `InvalidPersistedRecord`；sink=false 返回 `UiClosed`；有效文本不返回保留态 `Skipped`；重复哈希和 `u64::MAX` 计数由 reducer 直接采用持久化最终快照，不本地加一。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（88 个库测试、注释策略和 UI 事件集成测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：新增捕获桥、持久化 DTO 转换、三态错误边界、唯一执行器生命周期和最终快照 reducer 说明，并纳入两个注释门禁清单；同步修正 `main.rs`、`lib.rs`、`command.rs` 的历史持久化过期说明。
- 风险等级：L3。
- DDD 结果：编码前 v19 复核要求明确 `Skipped` 仅为未来非 UI 输入保留；v20 复核要求扩大 reducer seam 以消除本地计数偏离；提交前终审 PASS。
- 本地提交：`694529d feat(history): 提交成功后再显示捕获历史`；计划契约提交：`d57d66a docs(ATOM-18A): 固定持久化结果驱动 UI`。
- 远端同步：未授权，未推送。
- 计划偏差：为保证 UI 与 SQLite 真相源一致，允许范围从原始桥接文件扩展到 `history.rs` 和 `app/ui_event.rs`，新增 `record_persisted`；旧 `record` 仅保留给非持久化测试/恢复前路径。
- 下一原子：ATOM-19 实现键盘选择导航。

### 2026-07-28 ATOM-18B 接入启动恢复和有界 UI 缓存

- 状态：已完成。
- 完成内容：新增启动恢复桥，使用 ATOM-18A 创建并传入结果泵的同一个 `StorageExecutor` 读取首页最多 100 条摘要；每条按 ID 读取 payload，校验 text 类型、正文存在、ID、计数和 32 字节哈希后只生成摘要 UI DTO；恢复快照通过 `ReplaceSnapshot` 在热键注册前投递。UI 内存历史固定 100 条，窗口模型固定只构造首批 30 条。
- 实际修改文件：`src/history_restore.rs`、`src/main.rs`、`src/app/ui_event.rs`、`src/lib.rs`、`scripts/check_comments.ps1`、`tests/comment_policy.rs`。
- 明确未做：不实现键盘上下导航、滚动分页、搜索、图片、剪贴板写回、删除/收藏交互和清理。
- 针对性验证：恢复文本摘要按时间倒序且不携带正文；120 条预置数据只恢复 100 条；非 text、缺正文、缺 payload、坏哈希长度、非法 ID/计数均阻止恢复；UI 内存容量和窗口首批分别断言 100/30。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（94 个库测试、注释策略和 UI 事件集成测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：启动恢复错误边界、payload 短生命周期、UI 100/30 上限、启动顺序和注释门禁清单均已补齐。
- 风险等级：L3。
- DDD 结果：提交前隔离终审 PASS；确认恢复发生在热键/监听前、错误不启动后台线程、正文不进入 UI 长期状态、同一执行器交接和有界模型均符合契约。
- 本地提交：`cd84b36 feat(history): 恢复持久化摘要并限制首批模型`。
- 远端同步：未授权，未推送。
- 计划偏差：为实现首批模型可验证性，在 `ui_event.rs` 增加纯裁剪 helper 和 100/30 单元测试，不接入分页状态或搜索行为。
- 下一原子：ATOM-19 实现键盘选择导航。

### 2026-07-28 ATOM-19 实现键盘选择导航

- 状态：已完成。
- 完成内容：FocusScope 新增 UpArrow/DownArrow 方向回调；UI reducer 新增 `MoveSelection`，面板打开时首批非空默认选中第一项，选择索引始终限制在当前构造的首批 30 条；隐藏面板忽略迟到选择事件。选中项通过固定 106px delegate 和负向 `history-viewport-y` 自动保持在视口内。
- 实际修改文件：`src/command.rs`、`ui/app-window.slint`、`src/app/ui_event.rs`、本计划及项目工作台、阶段记录。
- 明确未做：不消费 Enter、Ctrl+Enter、Esc 或粘贴动作；不接入分页扩展、搜索、图片和剪贴板写回。
- 针对性验证：空列表、单条默认选择、30 条上下边界、超过首批索引夹紧、快照非法索引夹紧、隐藏面板迟到事件、连续移动和固定高度视口计算均通过；选择事件不重建 `VecModel`，快照/捕获刷新后重新定位选中项。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（101 个测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：新增事件、选择 reducer、视口公式、迟到事件防护和模型刷新边界均已补充中文职责与关键逻辑注释。
- 风险等级：L1。
- DDD 结果：编码前 v21 预审锁定首批 30 条、负向 viewport-y、固定 106px 计算和仅 UI reducer 修改；提交前终审先发现并修复选择事件重建模型、刷新后不定位和非法索引问题，最新复审 PASS。
- 本地提交：`1ae70af feat(ui): 支持键盘选择历史记录`；契约提交：`a0b6010 docs(ATOM-19): 固定首批键盘选择与视口契约`。
- 远端同步：未授权，未推送。
- 计划偏差：为保证窗口化列表不被上下键重置，`post_ui_event` 以事件类型区分模型刷新与选择视口副作用；未扩大到新的 UI 模型字段。
- 下一原子：ATOM-20 实现 Ctrl+Enter 仅复制。

### 2026-07-28 ATOM-20 实现 Ctrl+Enter 仅复制

- 状态：已完成；按用户指示在原子边界暂停，未启动 ATOM-21。
- 完成内容：UI 只通过 `id + content_hash` 发出 Ctrl+Enter 请求；结果泵按 ID 从 `StorageExecutor` 读取完整 text payload，校验类型、正文、哈希长度、正文重算哈希和内部 NUL 后写回 `CF_UNICODETEXT`。新增严格 HGLOBAL 所有权处理、UTF-16 NUL 终止、Open/Empty/Set/Close 错误边界和稳定错误描述。
- 自身写回抑制：预期使用 token 化 32 项有界队列；从登记到 Win32 写入、在仍持有 `OpenClipboard` 独占锁时读取 sequence 并绑定的过程持串行锁；监听端只按精确 sequence、hash、format 一次消费，乱序保留、5 秒 TTL 回收，队列满拒绝写回；`SetClipboardData` 成功但 `CloseClipboard` 失败仍保留预期。
- 关闭边界：结果桥关闭时清空尚未执行的 `pending_copy`，只保留已发布捕获结果，避免 UI 退出后继续写系统剪贴板。
- 实际修改文件：`src/clipboard/writer.rs`、`src/clipboard/io_worker.rs`、`src/history_bridge.rs`、`src/main.rs`、`src/app/ui_event.rs`、`src/command.rs`、`src/platform/windows/hotkey.rs`、`src/platform/windows/system_window.rs`、`src/app/mod.rs`、`src/clipboard/mod.rs`、`src/lib.rs`、`ui/app-window.slint`、`scripts/check_comments.ps1`。
- 明确未做：不实现 Enter 自动粘贴、UIPI 降级提示、图片/富文本写回、搜索、分页扩展、设置或其他 ATOM-21 及后续原子行为。
- 针对性验证：复制请求桥 latest-wins/关闭拒绝；预期 token 队列容量、同/异哈希连续写回、未绑定不消费、乱序保留、TTL 回收、队列满拒绝、Close 失败保留预期；正文完整性、合法长度篡改哈希、内部 NUL、非 text、缺正文和历史不变写回前校验均通过。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（118 个测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：writer 所有权、sequence 串行锁、token 队列/TTL、关闭清理、正文哈希和内部 NUL 防护均已补充文件级、公共 API 与关键逻辑中文注释。
- 风险等级：L3。
- DDD 结果：提交前隔离终审经历并修正哈希重算、内部 NUL、Close 失败所有权、单槽预期覆盖、未绑定同哈希竞态、关闭后 pending_copy 和关闭后 sequence 误绑定等问题，最终 PASS。
- 本地提交：`c451e71 feat(paste): 支持仅复制文本记录`。
- 远端同步：未授权，未推送。
- 计划修订：计划版本 v23–v25 补充哈希/NUL、token 有界队列与串行锁、独占锁内 sequence、TTL 和关闭清理契约；当前版本 v26 记录完成并暂停。
- 下一原子：ATOM-21 实现 Enter 自动粘贴（未启动，等待用户继续）。

### 2026-07-28 ATOM-21 启动 Enter 自动粘贴

- 状态：正在执行。
- 启动原因：用户已明确要求同步远端并继续开发；本原子从 ATOM-20 完成边界恢复。
- 唯一目标：选中历史文本后，仅在面板打开前捕获的 HWND、PID 和完整性级别仍匹配且为普通同级目标时，安全写回并模拟 `Ctrl + V`。
- 明确不做：不实现管理员目标或失败场景的用户提示、不扩展图片/富文本粘贴、不实现搜索、分页、设置或其他后续原子行为。
- 远端策略：启动记录、代码提交和完成记录均提交后推送 `origin/main`。
- 下一步：完成 DDD 预审后实现粘贴协调器、目标复核、焦点恢复、`SendInput` 接缝和可重复测试。

### 2026-07-28 ATOM-21 完成 Enter 自动粘贴

- 状态：已完成。
- 完成内容：Slint Enter 回调在 UI 线程同步捕获代次、记录 ID 和内容哈希；自动粘贴工作桥采用 latest-wins，并与 Ctrl+Enter 互斥，关闭时清除尚未出队的复制/粘贴命令。后台复用 ATOM-20 的 payload 类型、正文、内部 NUL 和哈希校验后才允许写回系统剪贴板。
- 目标安全：执行阶段只使用面板打开前保存的 HWND/PID/完整性快照；写回后复核 `IsWindow`、PID、普通中完整性和前台身份，`SetForegroundWindow` 后再次复核，目标变化时不向新窗口注入输入。
- 输入安全：只把完整四事件 `Ctrl↓、V↓、V↑、Ctrl↑` 视为成功；部分接受时按已接受前缀逆序释放按键，清理本身部分接受时逐事件补发，目标变化后禁止发送清理输入。释放顺序通过可注入发送 seam 测试。
- UI 并发：`paste_pending`、`paste_executing` 和 `request_token` 隔离重复 Enter、迟到失败和旧代次结果；成功结果先在同一 UI 闭包内同步更新 reducer，再执行物理隐藏，旧成功不能隐藏新请求。
- 实际修改文件：`src/app/ui_event.rs`、`src/clipboard/io_worker.rs`、`src/clipboard/mod.rs`、`src/command.rs`、`src/history_bridge.rs`、`src/lib.rs`、`src/main.rs`、`src/platform/windows/window/lifecycle.rs`、`src/platform/windows/window/mod.rs`、`ui/app-window.slint`。
- 明确未做：不实现管理员或输入失败的用户提示/仅复制降级 UI（ATOM-22）；不扩展图片、富文本、搜索、分页、设置或其他后续原子行为。
- 针对性验证：代次/令牌重入与迟到结果、选中项身份漂移、复制/粘贴互斥、关闭清理、旧哈希拒绝、普通中完整性边界、目标变化保护、完整四事件要求、部分输入逆序释放和清理补发均通过。
- 回归验证：`cargo fmt --all`、`cargo test --all-targets --locked`（128 个测试通过，性能探针按契约忽略）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo check --target x86_64-pc-windows-msvc --locked`、`cargo build --target x86_64-pc-windows-msvc --release --locked`、`scripts/check_comments.ps1`、`git diff --check` 均通过。
- 中文注释检查：自动粘贴状态机、目标权限边界、输入 ABI、部分清理、关闭语义和 UI/worker 线程边界均已补充中文职责与关键逻辑注释。
- 风险等级：L3。
- DDD 结果：提交前隔离终审首次发现部分输入清理顺序和成功先隐藏竞态，修正后最终 PASS。
- 本地提交：`b664ac5 feat(paste): 支持安全自动粘贴`。
- 远端同步：已推送 `origin/main`。
- 计划偏差：在原允许的粘贴协调、目标验证、焦点恢复和 Windows 测试范围内增加了最小事件令牌、工作桥取消和 UI 同步完成接缝，以闭合并发安全契约；未扩展用户可见功能。
- 下一原子：ATOM-22 实现自动粘贴降级提示（尚未启动）。

### 2026-07-28 ATOM-22 启动自动粘贴降级提示

- 状态：正在执行。
- 启动原因：ATOM-21 已完成普通同级目标的安全输入闭环；用户明确要求继续执行后续原子。
- 唯一目标：自动粘贴不能注入时可靠地降级为仅复制，并在面板内显示不泄露正文的固定提示。
- 明确不做：不实现图片/富文本降级、不增加系统通知中心或外部网络服务、不扩展搜索、分页、设置或其他后续原子行为。
- 远端策略：启动记录、代码提交和完成记录均提交后推送 `origin/main`。
- 下一步：先完成失败类别、提示状态和 UI 接缝的 DDD 预审，再实现、验证并提交。

## 10. 计划变更记录

| 日期 | 版本 | 变更 | 原因 |
|---|---|---|---|
| 2026-07-28 | v3 | 首次落地60个原子任务、全局不变量、状态规则和执行记录模板 | 避免开发途中遗漏任务或破坏原子边界 |
| 2026-07-28 | v4 | ATOM-06 明确允许 UI `ShowPanel/Quit` 事件和 Windows Shell feature | 托盘打开必须幂等，退出必须经 UI 线程统一清理，且 Win32 Shell API 需要显式 feature |
| 2026-07-28 | v5 | ATOM-08 允许 BLAKE3 依赖、领域模块 wiring 与注释清单 | 文本领域模型需要稳定哈希；`src/domain` 子模块必须由库入口公开，且新增源码必须纳入中文注释门禁 |
| 2026-07-28 | v6 | ATOM-10 允许 ClipboardIO 模块 wiring 与 Win32 DataExchange/Memory feature | 安全读取 `CF_UNICODETEXT` 需要原生剪贴板、全局内存 API 及独立 worker 入口；明确不提前接入更新监听 |
| 2026-07-28 | v7 | ATOM-11 明确系统窗口、ClipboardIO latest-wins 队列与监听接线的文件边界 | `WM_CLIPBOARDUPDATE` 需要消息线程捕获 sequence/来源快照并交给有界 worker；本原子不接入 UI、存储或图片业务 |
  | 2026-07-28 | v8 | ATOM-11 增加 `ClipboardCaptureInbox` 结果桥并扩展 ATOM-12 消费接缝 | 私有 Receiver 无人消费会使成功捕获不可观测且滞留正文；必须让结果以容量为一的 latest-wins 方式离开消息线程，再由后续原子投递 `UiEvent` |
| 2026-07-28 | v9 | ATOM-14 Release 实测发现当前 `VerticalLayout` 没有长滚动能力，新增 ATOM-14R 窗口化 `ListView` 原子并让 ATOM-15 依赖它 | 三项性能指标达标但长滚动硬门禁失败；必须先完成真实窗口化列表，不能带着结构性缺陷进入 SQLite 存储 |
| 2026-07-28 | v10 | ATOM-15 明确允许 `src/lib.rs` 注册存储模块及同步更新中文注释门禁 | 仅新增 `src/storage` 会形成未编译孤立目录，后续原子无法通过 crate 公共接缝调用；补充最小 wiring 不改变存储行为边界 |
| 2026-07-28 | v11 | 固定 ATOM-15 的 v1 DDL、`PRAGMA user_version` 迁移状态、启动/停止错误语义、路径注入、真实连接线程探针和 `rusqlite 0.40.1 + bundled` 构建来源 | L3 预审发现原契约无法判定坏 schema、回滚、生命周期、测试隔离和线程所有权；补足后再编码 |
| 2026-07-28 | v12 | 固定 ATOM-16 文本 upsert 的输入字段、首次默认值、冲突更新范围、i64 计数饱和和事务回滚测试方式 | 避免执行时重新猜测重复记录是否覆盖旧正文/来源/收藏及时间字段 |
| 2026-07-28 | v13 | 固定 `TextUpsertInput/Result` 与 `upsert_text` 接口、异常计数归一化及可重复 trigger 故障注入断言 | 预审发现没有公共接缝和可观察结果，无法判定同事务读取、字段保留和 worker 在回滚后的可用性 |
| 2026-07-28 | v14 | 固定 ATOM-17 的游标/摘要页/payload 公共接缝、页大小上限、`next_cursor` 规则和 `OFFSET` 禁止项 | 原始卡只描述排序和游标，无法判定分页终点、异常页大小、摘要字段和按 ID 未找到语义 |
| 2026-07-28 | v15 | 补齐 `next_cursor` 锚点及 payload 的 NULL、类型和完整字段语义 | 预审发现多取一行游标可能被实现为隐藏行，且 v1 允许 NULL 正文和非 text 类型，公共结果仍有猜测空间 |
| 2026-07-28 | v16 | 将原 ATOM-18 拆为捕获提交后可见（18A）与启动恢复/首批裁剪（18B）两个垂直原子 | 原卡同时改变结果泵生命周期、SQLite 可见性、启动顺序、UI 缓存容量和窗口模型，无法独立验证或安全回退 |
| 2026-07-28 | v17 | 固定 18A 的来源优先级、运行时相对时间和可注入 UI sink 失败测试接缝 | 预审发现双来源到单字段映射、时间文案和 DB 失败后无幽灵 UI 的可观察语义仍可由实现自行猜测 |
| 2026-07-28 | v18 | 固定 `CaptureProcessOutcome` 三态和 `CaptureProcessError` 错误映射 | 预审发现 sink 关闭、可跳过记录和持久化/DTO 错误若无返回契约，结果泵可能错误地继续或提前退出 |
| 2026-07-28 | v19 | 明确 `Skipped` 仅为未来非 UI 输入保留状态，当前有效文本捕获不得跳过；补充 trigger、DTO、sink=false 和后续哈希验收路径 | 预审发现当前输入域没有可验证的 `Skipped` 触发条件，若不约束可能吞掉已经成功持久化的有效捕获 |
| 2026-07-28 | v20 | 扩大 ATOM-18A 允许范围，要求捕获 reducer 采用持久化最终字段并覆盖饱和计数 | 预审发现旧内存去重器会对重复捕获本地加一，可能让 UI 计数、ID、摘要或收藏状态偏离 SQLite 真相源 |
| 2026-07-28 | v21 | 固定 ATOM-19 的选择范围为当前首批 30 条，并定义 Slint 上/下键回调、边界夹紧和固定 delegate 视口定位 | ATOM-18B 为性能只构造 30 张卡片，若选择索引直接遍历 100 条缓存会产生不可见选中项和无法滚动到目标的矛盾 |
| 2026-07-28 | v22 | 完成 ATOM-19 并切换 ATOM-20；记录选择事件跳过模型刷新、刷新后视口重定位和非法索引夹紧修复 | 终审发现若每次上下键重建 `VecModel` 会破坏窗口化列表连续性，且模型重置后必须恢复选中项视口位置 |
| 2026-07-28 | v23 | 补充 ATOM-20 的写回完整性契约：重算正文哈希、拒绝内部 NUL，并在 Set 成功/Close 失败时保留自身事件抑制预期 | DDD 终审发现篡改数据库哈希或关闭剪贴板失败后撤销预期会导致自身写回重新进入历史；CF_UNICODETEXT 还无法无损表示内部 NUL |
| 2026-07-28 | v24 | 将 ATOM-20 自身写回预期改为 token 化有界队列，并用写入串行锁消除未绑定序号窗口；补充快速连续同/异哈希和序号淘汰测试 | DDD 终审发现单槽预期会被后续 Ctrl+Enter 覆盖，且未绑定阶段按哈希消费可能吞掉真实用户复制或让自身回捕获进入历史 |
| 2026-07-28 | v25 | 将 sequence 读取移动到仍持有剪贴板独占锁的 Set 成功路径，并在结果桥关闭时清空待执行复制命令 | DDD 终审发现 CloseClipboard 后再查询 sequence 存在外部同哈希写入导致误绑定，且停止阶段保留 pending_copy 会在 UI 退出后重新写回系统剪贴板 |
| 2026-07-28 | v26 | 完成 ATOM-20 并记录 c451e71、118 个测试和最终 DDD PASS；当前按用户指示暂停，不启动 ATOM-21 | 仅复制文本闭环已完成独立验证，后续自动粘贴属于新的原子和新的用户继续指令 |
| 2026-07-28 | v27 | 用户授权远端同步并启动 ATOM-21；固定自动粘贴只面向原目标、同级完整性和未变化 HWND/PID，失败提示留给 ATOM-22 | 远端仓库已完成历史同步，后续每个原子需独立提交并推送；先锁定安全边界再实现输入注入 |
| 2026-07-28 | v28 | 完成 ATOM-21，补齐代次/令牌、工作桥关闭清理、目标二次复核、部分输入逆序释放和成功先同步状态再隐藏契约；切换 ATOM-22 未启动 | 终审发现并修复 partial `SendInput` 遗留按键和旧成功物理隐藏竞态；128 个测试及 Windows 目标构建通过 |
| 2026-07-28 | v29 | 启动 ATOM-22，固定失败类别、复制内容是否已可靠写回、固定提示文案和代次/令牌隔离；明确失败路径不得再次调用 `SendInput` | 避免把剪贴板写回失败误报为“内容已复制”，并防止旧失败事件覆盖新面板状态 |
| 2026-07-28 | v30 | 完成 ATOM-22，接入固定降级提示、失败事件门禁、面板重新显示和桥接错误保守归类；切换 ATOM-23 未启动 | 133 个测试、Clippy、Windows Debug/Release 构建和中文注释门禁通过，代码提交 `f4647e6` 已推送；明确 ATOM-21 key-up 清理是降级路径之外的唯一输入释放例外 |
| 2026-07-28 | v31 | 启动 ATOM-23，固定 `HistoryQuery` 筛选字段、LIKE 转义、筛选后复合游标和只返回摘要的边界 | 避免搜索实现把用户输入拼入 SQL、破坏同毫秒分页或提前加载完整正文；明确 UI 搜索和 FTS5 留给后续原子 |
| 2026-07-28 | v32 | 完成 ATOM-23，接入 StorageExecutor 统一筛选摘要查询并保持旧分页接口；切换 ATOM-24 未启动 | 135 个测试、Clippy、Windows Debug/Release 构建和中文注释门禁通过，代码提交 `15da738` 已推送 |
| 2026-07-28 | v33 | 启动 ATOM-24，固定 120 ms 防抖、单调 generation、in-flight 结果门禁和取消/关闭失效语义 | 避免快速输入触发无界查询、旧结果闪回或重复结果覆盖当前状态；明确 SQLite 和 UI 接线留给后续原子 |
| 2026-07-28 | v34 | 完成 ATOM-24，接入 UI 线程可拥有的搜索防抖协调器和结果代次门禁；切换 ATOM-25 未启动 | 141 个测试、Clippy、Windows Debug/Release 构建和中文注释门禁通过，代码提交 `02084bd` 已推送 |
| 2026-07-28 | v35 | 启动 ATOM-25，固定搜索框、全部/文本/收藏标签、结果状态和旧计时器隔离边界 | 仅更新原子契约与恢复文档，代码实现和验证待完成 |
| 2026-07-28 | v36 | 完成 ATOM-25，接入搜索框、基础筛选、结果状态和 loading 期间旧结果保留；切换 ATOM-26 未启动 | 145 个测试、Clippy、DDD PASS、Windows Debug/Release 构建和中文注释门禁通过，代码提交 `49d79f4` 已推送 |
| 2026-07-28 | v37 | 根据真实试用反馈新增 ATOM-25R，并拆为 WCB-INT-01 至 WCB-INT-10；同步撤销自动粘贴产品方向，固定持续置顶、鼠标选择、显式复制和 SQLite 滚动分页契约 | 当前窗口失焦即隐藏、不是 topmost、卡片不可鼠标选择、只能键盘复制且首批 30 条后无法续页；三轮隔离质疑已补齐焦点、关闭、队列、generation/token/cursor、重试和 2,000 条边界，最终 PASS |
| 2026-07-28 | v38 | 按用户复核删除 Ctrl+Enter 辅助复制并启动 WCB-INT-01；鼠标按钮成为唯一复制入口 | 用户明确要求直接去除 Ctrl+Enter，避免保留与鼠标优先目标冲突的隐藏键盘操作 |
| 2026-07-29 | v39 | 剩余交互修正原子改用定向测试加 DDD 复核并连续执行，不再运行全量测试 | 用户确认 DDD 与定向测试互补，要求 WCB-INT-02 至 WCB-INT-10 中间不停并逐原子推送 |
| 2026-07-29 | v40 | WCB-INT-05 完成稳定身份鼠标选择、明确选中态及独立操作区，进入 WCB-INT-06 | 29 个 reducer 定向测试、Slint 鼠标集成测试、Windows 检查、桌面点击冒烟和 DDD 复核通过 |
| 2026-07-29 | v41 | WCB-INT-06 完成显式复制按钮、锁自由 latest-wins 邮箱和退出关闭门禁，进入 WCB-INT-07 | 定向测试、真实 Windows 第二卡片全文写回冒烟和 DDD 终审通过 |
| 2026-07-29 | v42 | WCB-INT-07 完成历史摘要严格哈希及摘要/payload 一致性校验，进入 WCB-INT-08 | 存储与恢复定向测试、库检查和 DDD 复核通过 |
| 2026-07-29 | v43 | WCB-INT-08 完成单一存储所有者、受控客户端和两阶段关闭，进入 WCB-INT-09 | 满队列、等待回执、worker panic、逃逸 clone、UI 关闭后排空 Capture 等定向测试与 DDD 复核通过 |
| 2026-07-29 | v44 | WCB-INT-09 完成 SQLite 异步首页、latest-wins 双向邮箱和严格结果身份，进入 WCB-INT-10 | 首页/防抖/捕获乱序、无丢唤醒、100 条外检索、失败保留和退出 join 定向测试及 DDD 通过 |
| 2026-07-29 | v45 | WCB-INT-10 完成真实滚动续页，ATOM-25R 十个交互修正子原子全部收口 | 30+50+5、三元身份、失败重试、2,000 边界、跨页选择、20,000 条 ListView 与 Windows 85 条滚轮冒烟通过 |
| 2026-07-29 | v51 | ATOM-28 三个子原子完成并暂停等待验证；迟到捕获门禁不再信任旧收藏快照 | 清空事务、修订水位、异步桥、确认 UI、定向测试和修正后 DDD PASS |
| 2026-07-29 | v52 | 启动 ATOM-29，拆分全量事务、双范围桥和文字强确认三个原子 | 固定显式 scope、包括收藏警告、committed-clear reducer 和两轮计划 DDD |
| 2026-07-29 | v53 | 完成 ATOM-29 的全量删除事务、显式双范围桥和文字强确认 | 增加事务期间捕获账本与全局历史变更互斥；定向测试和 L3 DDD 终审通过，等待用户验证 |
| 2026-07-29 | v54 | 修复面板关闭快捷键并恢复 `Alt + V` 显隐开关 | 用户实际验证确认重复热键应关闭；区分热键切换、托盘和二次启动幂等显示，恢复前台焦点并复用 Esc 的完整隐藏收口 |
| 2026-07-29 | v55 | 启动 ATOM-30，拆分 v2 迁移、图片领域元数据、纯路径布局和安全目录准备四个子原子 | 用户确认默认与自定义图片路径契约；L3 计划复核补齐多根注册、路径哈希绑定、可恢复 marker、同卷 recovery 和 Windows reparse capability 后 PASS |
