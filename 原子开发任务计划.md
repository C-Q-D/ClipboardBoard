# Windows 轻量剪贴板工具原子开发任务计划

> 本文档是项目开发期间的唯一原子任务清单。开发时必须按依赖顺序逐项执行、验证、提交和更新状态，不得依赖聊天记录记忆任务，也不得把多个原子合并开发。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 计划编号 | `WCB-PLAN-001` |
| 计划版本 | `v3` |
| 工作名称 | `ClipboardBoard` |
| 项目目录 | `F:\workspace\small-projects\windows-copy` |
| 生成基线 | 空目录、尚未初始化 Git |
| 目标平台 | Windows 10/11 x64 |
| 技术栈 | Rust、Slint、windows-rs、rusqlite、image、BLAKE3、serde、tracing |
| 默认快捷键 | `Alt + V` |
| Git 策略 | 每个原子独立本地提交；未经用户明确授权不得推送远端 |
| 当前原子 | `ATOM-01` |
| 当前状态 | 尚未开始开发 |

## 2. 状态标记

- `[ ]`：尚未开始。
- `[-]`：正在执行，同一时刻只能有一个。
- `[x]`：已验证并完成本地提交。
- `[!]`：遇到阻塞，必须在执行记录中说明。
- `[~]`：仓库现状已经满足，无需产生空提交，但必须记录证据。

更新规则：

1. 开始原子前，将该原子改为 `[-]`，同时更新“当前原子”。
2. 只修改该原子允许范围内的文件。
3. 验证、差异审查和必要的风险质疑通过后创建独立本地提交。
4. 写入提交哈希、验证证据和实际变更，再将原子改为 `[x]`。
5. 当前原子完成前，不得把下一个原子标为 `[-]`。
6. 计划需要调整时先更新本文档版本和变更记录，不得在代码中悄悄扩大范围。

## 3. 产品目标

通过全局快捷键呼出的轻量 Windows 剪贴板工作台，支持文本和图片历史、搜索、筛选、收藏、删除、清空、键盘复制、自动粘贴、托盘、暂停、排除程序、开机启动、无限滚动、图片懒加载和本地数据清理。

核心结果：

- 快捷键呼出，最近内容立即显示。
- 文本和图片都能保存、预览和重新粘贴。
- 键盘操作优先，鼠标操作可用。
- 后台内存和 CPU 占用低。
- 所有数据默认只保存在本机。
- 黑底白字、固定高度卡片和简洁的浮动面板。

明确排除：

- 云同步、用户账号和团队共享。
- OCR、AI 分类和插件系统。
- 在线 URL 标题解析。
- 完整富文本还原。
- 文件、音频和视频历史。
- macOS、Linux 和复杂主题。
- 自动更新和代码签名。

## 4. 默认产品参数

| 参数 | 默认值 |
|---|---:|
| 普通历史上限 | 2,000 条 |
| 普通历史保留 | 30 天 |
| 图片空间上限 | 500 MiB |
| 最近摘要缓存 | 100 条 |
| UI 首批结果 | 30 条 |
| 后续加载 | 每批 50 条 |
| 缩略图最长边 | 320 px |
| 搜索防抖 | 120 ms |
| 文本内容上限 | 5 MiB |
| 收藏清理策略 | 不自动清理 |
| 窗口尺寸 | 560 × 640 |

性能验收目标：

| 指标 | 目标 |
|---|---:|
| 后台待机内存 | ≤ 30 MiB |
| 面板打开后内存 | ≤ 60 MiB |
| 快捷键呼出 P95 | ≤ 100 ms |
| 首批结果显示 | ≤ 50 ms |
| 1,000 条文本搜索 | ≤ 30 ms |
| 空闲 CPU | 接近 0% |
| 长列表滚动 | 不冻结，接近 60 FPS |

## 5. 全局架构与不变量

### 5.1 线程边界

```text
Win32 消息线程
    │ 只接收 WM_HOTKEY / WM_CLIPBOARDUPDATE / 托盘消息
    ▼
ClipboardIoWorker
    │ 打开剪贴板、有界复制数据、关闭剪贴板
    ├───────────────┐
    ▼               ▼
ImageWorker     StorageExecutor
图片解析/编码    唯一 SQLite 连接
文件持久化       迁移、CRUD、搜索、清理
    └───────┬───────┘
            ▼
         UiEvent
            ▼
Slint 事件线程
唯一允许修改 UI 模型和创建 Slint Image 的线程
```

必须始终成立：

1. UI 只能在 Slint 事件线程更新。
2. 所有非 UI 线程只能通过不可变 DTO 和 `UiEvent` 投递结果。
3. Win32 消息线程只处理轻量事件，不读取正文、不压缩图片、不访问 SQLite。
4. `ClipboardIoWorker` 必须在关闭剪贴板之前复制出拥有所有权的数据。
5. `ImageWorker` 不得持有 `HGLOBAL`、`HBITMAP` 或 Slint `Image`。
6. `StorageExecutor` 是唯一 SQLite 连接所有者。
7. 引入 SQLite 后，新增、收藏、删除、清空等状态必须事务提交成功后才更新 UI。
8. 后台不得轮询剪贴板。

### 5.2 剪贴板一致性

1. 事件队列必须有界并采用 `latest-wins`。
2. 打开剪贴板前和复制完成后必须核对 sequence。
3. sequence 失配时丢弃本次读取，不得把新内容记到旧来源名下。
4. 应用自身写回使用两阶段预期写事务：
   - 写入前登记规范哈希和格式集合。
   - 写入后绑定精确 sequence。
   - 监听端只能消费一次。
   - sequence 被越过或超时后立即失效。
5. 自身写回不得增加 `copy_count`、更新时间或改变排序。
6. 单次事件只生成一种内容，优先级为：
   - 注册 PNG。
   - `CF_DIBV5`。
   - `CF_DIB`。
   - `CF_BITMAP`。
   - `CF_UNICODETEXT`。

### 5.3 数据与图片一致性

1. SQLite 是历史记录和元数据真相源。
2. original PNG 是不可重建的耐久图片 payload。
3. thumbnail WebP 是可重新生成的派生缓存。
4. 图片规范哈希基于：
   - 固定域标签。
   - 图片宽高。
   - 顶向下、行连续的 RGBA8 像素。
5. 图片必须先写入独占 staging 文件。
6. 文件 flush、关闭、原子发布并验证后，才能提交 SQLite 记录。
7. 数据库提交失败时只能删除本次捕获确实拥有的文件。
8. 删除记录提交后，才能排队清理无引用资产。
9. 对账前必须经过 StorageExecutor 屏障，并排除 staging、in-flight 和 recovery。
10. 自动清理永不删除收藏；明确执行“清空全部”时可以删除收藏。

### 5.4 自动粘贴安全边界

1. 面板激活前保存原目标 HWND、PID 和完整性级别。
2. Enter 时只复核保存的目标，不重新把当前前台窗口当作目标。
3. 目标已经失效或身份变化时只复制，不向其他窗口发送按键。
4. 普通权限程序不得尝试向高完整性目标注入输入。
5. 焦点恢复或 `SendInput` 失败时只提示用户手动按 `Ctrl + V`。

### 5.5 中文注释门禁

每个原子都必须检查：

- Rust 文件顶部存在中文文件级 `//!` 或等价说明。
- Slint、PowerShell、SQL 和安装脚本包含中文文件职责说明。
- 类型、结构体字段、枚举、方法、公共 API、参数、返回值、异常和副作用有中文注释。
- 并发、权限、内存所有权、重试、清理、格式解析和不直观分支有中文设计说明。
- 修改行为时同步更新相关注释。
- `TODO`、`FIXME` 必须写明中文原因、当前限制和后续目标。

### 5.6 每个原子的统一完成定义

每个原子只有满足以下全部条件才能标记完成：

- [ ] 唯一目标已经实现。
- [ ] 未提前实现后续原子。
- [ ] 针对性测试通过。
- [ ] `cargo fmt --check` 通过。
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` 通过。
- [ ] 按风险运行的必要回归测试通过。
- [ ] 中文注释静态检查和人工差异检查通过。
- [ ] 完整 diff 没有调试代码、无关格式化或用户修改。
- [ ] L2/L3 原子的风险质疑结果为 `PASS`。
- [ ] 创建了一个独立本地提交。
- [ ] 没有未经授权执行 `git push`。
- [ ] 本文档中的状态和执行记录已经更新。

## 6. 产品交付顺序

| 顺序 | 交付单元 | 用户可观察结果 | 原子 |
|---:|---|---|---|
| 1 | UNIT-01 快捷面板 | Alt+V 呼出和关闭面板 | 01–05 |
| 2 | UNIT-02 托盘控制 | 从托盘打开和退出 | 06–07 |
| 3 | UNIT-03 文本捕获 | 复制文本进入最近历史 | 08–12 |
| 4 | UNIT-04 文本去重 | 重复内容合并并置顶 | 13 |
| 5 | UNIT-05 历史持久化 | 重启后恢复历史 | 14–18 |
| 6 | UNIT-06 仅复制 | 键盘选中并复制历史内容 | 19–20 |
| 7 | UNIT-07 自动粘贴 | 自动粘贴或安全降级 | 21–22 |
| 8 | UNIT-08 搜索筛选 | 输入关键词和类别筛选 | 23–25、39 |
| 9 | UNIT-09 历史管理 | 收藏、删除和清空 | 26–29 |
| 10 | UNIT-10 图片捕获 | 捕获并展示图片 | 30–37 |
| 11 | UNIT-11 图片复用 | 重新复制和粘贴图片 | 38 |
| 12 | UNIT-12 大历史浏览 | 无限滚动且内存有界 | 40–43 |
| 13 | UNIT-13 暂停记录 | 临时或无限期暂停 | 44–45 |
| 14 | UNIT-14 程序排除 | 指定程序内容不被读取 | 46 |
| 15 | UNIT-15 自动清理 | 控制记录数量、天数和图片空间 | 47–49 |
| 16 | UNIT-16 设置 | 历史、隐私、热键和开机启动 | 50–53 |
| 17 | UNIT-17 固定窗口 | 固定、拖动和调整尺寸 | 54 |
| 18 | UNIT-18 损坏恢复 | 隔离损坏数据并安全重建 | 55 |
| 19 | UNIT-19 正式发布 | 图标、性能、安装和发布验收 | 56–60 |

## 7. 原子任务清单

### 第一阶段：程序外壳和窗口闭环

#### [ ] ATOM-01 建立可构建的 Slint 项目骨架

- 所属交付单元：UNIT-01。
- 唯一目标：得到一个可构建、可运行的黑底白字 Slint Windows 窗口。
- 前置条件：项目目录为空。
- 允许修改：
  - 初始化本地 Git。
  - `.gitignore`。
  - `Cargo.toml`。
  - `build.rs`。
  - `src/lib.rs`。
  - `src/main.rs`。
  - `ui/app-window.slint`。
  - 中文注释检查脚本及其测试入口。
- 实现契约：
  - 创建单个 Rust 二进制包，并保留可测试的 library 入口。
  - 使用 `slint-build` 编译 `.slint` 文件。
  - 默认窗口尺寸为 560×640。
  - UI 使用黑色背景、白色主文字和灰色辅助文字。
  - 建立 Rust、Slint 和 Windows 生成文件的 `.gitignore`。
- 明确不修改：不实现热键、托盘、剪贴板、数据库或图片。
- 验收：
  - `cargo check`。
  - `cargo test`。
  - `cargo run` 后窗口可见。
  - 中文文件级注释检查通过。
- 停止条件：
  - Git 缺少 `user.name` 或 `user.email`。
  - Rust MSVC 链接器不可用且无法在当前环境构建。
- 风险等级：L1。
- 计划提交：`feat(app): 建立可运行的黑色看板骨架`。

#### [ ] ATOM-02 建立唯一 UI 事件入口

- 所属交付单元：UNIT-01。
- 唯一目标：非 UI 线程可以安全地向 Slint 线程投递不可变结果。
- 依赖：ATOM-01。
- 允许修改：`src/app/`、`src/command.rs`、必要测试。
- 实现契约：
  - 定义 `UiEvent` 和 UI DTO。
  - 通过 `slint::invoke_from_event_loop` 进入 UI 线程。
  - UI 模型的可变引用不得离开 UI 线程。
- 明确不修改：不创建 Win32 消息线程，不实现剪贴板。
- 验收：后台线程投递后，测试或调试断言证明模型更新发生在 UI 线程。
- 停止条件：Slint 当前版本无法提供可靠事件循环投递接缝时重新规划。
- 风险等级：L2，编码前和提交前执行 DDD。
- 计划提交：`feat(app): 建立线程安全的UI事件入口`。

#### [ ] ATOM-03 注册 Alt+V 全局快捷键

- 所属交付单元：UNIT-01。
- 唯一目标：应用运行时按 `Alt + V` 能打开面板。
- 依赖：ATOM-02。
- 允许修改：`src/platform/windows/system_window.rs`、`hotkey.rs`、应用启动接线和测试。
- 实现契约：
  - 创建隐藏 Win32 消息窗口。
  - 调用 `RegisterHotKey`。
  - `WM_HOTKEY` 只转换为 `UiEvent::OpenPanel`。
  - 注册冲突必须显示明确错误。
- 明确不修改：不监听剪贴板，不实现快捷键设置。
- 验收：实际按键可打开窗口；占用热键时不静默失败。
- 风险等级：L2。
- 计划提交：`feat(hotkey): 支持Alt+V呼出面板`。

#### [ ] ATOM-04 保证程序单实例运行

- 所属交付单元：UNIT-01。
- 唯一目标：同一用户只能运行一个后台实例，二次启动只唤起主实例。
- 依赖：ATOM-03。
- 允许修改：单实例模块、启动顺序和进程级测试。
- 实现契约：
  - 在初始化 UI、SQLite 和热键前获取用户级命名互斥体。
  - 二次启动向主实例发送打开面板消息后退出。
  - 不依赖远端服务或管理员权限。
- 验收：连续启动两个进程时只有一个热键注册和一个长期存活进程。
- 风险等级：L3，涉及跨进程并发。
- 计划提交：`feat(app): 保证应用单实例运行`。

#### [ ] ATOM-05 完成临时面板生命周期

- 所属交付单元：UNIT-01。
- 唯一目标：面板在正确显示器打开，并按临时窗口规则关闭。
- 依赖：ATOM-04。
- 允许修改：`src/platform/windows/window/`、UI 回调和窗口测试。
- 实现契约：
  - 面板激活前保存原目标 HWND、PID 和完整性级别。
  - 使用鼠标所在显示器工作区计算窗口位置。
  - 再次按热键、按 Esc 或失焦时隐藏。
  - 隐藏不退出后台进程。
- 明确不修改：不执行自动粘贴，不实现固定模式。
- 验收：单屏、多屏和不同缩放下位置正确；保存目标不是面板自身。
- 风险等级：L2。
- 计划提交：`feat(window): 完成临时面板显示与隐藏`。

### 第二阶段：托盘和诊断

#### [ ] ATOM-06 增加系统托盘入口

- 所属交付单元：UNIT-02。
- 唯一目标：用户可从托盘打开或退出应用。
- 依赖：ATOM-05。
- 允许修改：`src/platform/windows/tray.rs`、系统窗口消息分发、临时托盘图标。
- 实现契约：
  - 使用 Win32 托盘接口。
  - 菜单只包含“打开”和“退出”。
  - 退出时注销热键、移除托盘图标并停止线程。
- 验收：面板隐藏后仍可打开；退出无残留进程或托盘图标。
- 风险等级：L2。
- 计划提交：`feat(tray): 增加托盘打开与退出操作`。

#### [ ] ATOM-07 建立不记录正文的诊断日志

- 所属交付单元：UNIT-02。
- 唯一目标：应用可以诊断事件和错误，同时不记录剪贴板正文。
- 依赖：ATOM-06。
- 允许修改：日志初始化、事件字段、日志测试。
- 实现契约：
  - 只允许记录记录 ID、类型、字节数、耗时、错误码和线程状态。
  - 禁止记录文本正文、图片字节和完整窗口标题。
- 验收：复制特征字符串后扫描日志，正文不出现。
- 风险等级：L2，涉及隐私。
- 计划提交：`feat(log): 建立隐私安全的诊断日志`。

### 第三阶段：文本剪贴板闭环

#### [ ] ATOM-08 定义文本记录领域模型

- 所属交付单元：UNIT-03。
- 唯一目标：以稳定领域模型表达文本记录摘要和完整 payload。
- 依赖：ATOM-07。
- 允许修改：`src/domain/clipboard_item.rs`、`hash.rs` 和单元测试。
- 实现契约：
  - 拆分 `ClipboardItemSummary` 和 `ClipboardPayload`。
  - 文本规范化只去除尾部 NUL，保留正常空格、换行和制表符。
  - 使用带文本域标签的 BLAKE3 哈希。
  - 摘要长度有上限，不把完整大文本交给列表模型。
- 验收：哈希稳定性、NUL、空白、Unicode 和摘要截断测试通过。
- 风险等级：L1。
- 计划提交：`feat(domain): 定义文本剪贴板记录模型`。

#### [ ] ATOM-09 读取复制来源程序

- 所属交付单元：UNIT-03。
- 唯一目标：记录复制事件发生时的来源 exe 和显示名称。
- 依赖：ATOM-08。
- 允许修改：`src/platform/windows/source.rs`、Win32 抽象和测试。
- 实现契约：
  - 在事件产生时读取前台 HWND 和 PID。
  - 使用有限权限打开进程。
  - 默认不读取窗口标题。
  - 进程退出或拒绝访问时返回无来源，不影响剪贴板捕获。
- 验收：普通程序、进程退出和访问失败用例通过。
- 风险等级：L2。
- 计划提交：`feat(source): 识别剪贴板来源程序`。

#### [ ] ATOM-10 在 ClipboardIO 线程读取 Unicode 文本

- 所属交付单元：UNIT-03。
- 唯一目标：安全、有限地读取 `CF_UNICODETEXT`。
- 依赖：ATOM-09。
- 允许修改：`src/clipboard/io_worker.rs`、`reader.rs`、格式定义和测试。
- 实现契约：
  - 建立专用 `ClipboardIoWorker`。
  - `OpenClipboard` 使用总时长受限的重试。
  - 读取前后核对 sequence。
  - 在关闭剪贴板前复制到 Rust 自有字符串。
  - 默认文本上限为 5 MiB。
- 验收：中英文、换行、空内容、忙碌剪贴板、失配 sequence 和超限文本测试通过。
- 风险等级：L3。
- 计划提交：`feat(clipboard): 在线程中安全读取Unicode文本`。

#### [ ] ATOM-11 接入 WM_CLIPBOARDUPDATE

- 所属交付单元：UNIT-03。
- 唯一目标：剪贴板变化能够可靠触发一次内容捕获。
- 依赖：ATOM-10。
- 允许修改：系统消息窗口、监听器、事件队列和并发测试。
- 实现契约：
  - 注册 `AddClipboardFormatListener`。
  - 消息线程只捕获 sequence 和来源快照并入队。
  - 队列有界且采用 `latest-wins`。
  - worker 发现 sequence 失配时丢弃。
- 验收：快速复制 A、B 不会把 B 记到 A 的来源名下。
- 风险等级：L3。
- 计划提交：`feat(clipboard): 监听Windows剪贴板更新`。

#### [ ] ATOM-12 将文本结果展示在列表顶部

- 所属交付单元：UNIT-03。
- 唯一目标：复制的文本以摘要卡片显示在列表顶部。
- 依赖：ATOM-11。
- 允许修改：UI 数据转换、文本卡片、列表模型和 UI 测试。
- 实现契约：
  - worker 结果必须通过 `UiEvent`。
  - 卡片显示摘要、来源和相对时间。
  - 列表不得持有完整大文本 payload。
- 明确不修改：当前阶段只使用内存列表，不访问 SQLite。
- 验收：复制后新摘要显示在顶部，线程断言通过。
- 风险等级：L2。
- 计划提交：`feat(history): 展示最近复制的文本`。

#### [ ] ATOM-13 合并重复文本

- 所属交付单元：UNIT-04。
- 唯一目标：重复文本不产生重复卡片。
- 依赖：ATOM-12。
- 允许修改：内存历史协调器和去重测试。
- 实现契约：
  - 相同规范哈希只更新时间、增加计数并置顶。
  - 保留原收藏状态。
- 明确不修改：当前是内存规则；持久化后由 ATOM-16 替代为数据库事务。
- 验收：连续复制、复制旧内容和收藏保留测试通过。
- 风险等级：L2。
- 计划提交：`feat(history): 合并重复文本记录`。

### 第四阶段：持久化历史

#### [ ] ATOM-14 验证 Slint 大列表策略

- 所属交付单元：UNIT-05。
- 唯一目标：在列表架构定型前验证 Slint 固定高度文本列表性能。
- 依赖：ATOM-13。
- 允许修改：性能探针、测试数据生成器和测量脚本。
- 实现契约：
  - Release 模式生成 20,000 条固定高度摘要。
  - 测量呼出 P95、首批显示、进程内存和长滚动。
- 验收：
  - 打开后内存 ≤ 60 MiB。
  - P95 呼出 ≤ 100 ms。
  - 首批结果 ≤ 50 ms。
  - 长滚动不冻结。
- 停止条件：任一硬门禁失败，必须重排为窗口化自定义 `Model`。
- 风险等级：L2。
- 计划提交：`test(ui): 增加大文本列表性能探针`。

#### [ ] ATOM-15 建立唯一 SQLite 存储线程

- 所属交付单元：UNIT-05。
- 唯一目标：所有 SQLite 操作只能在一个存储线程执行。
- 依赖：ATOM-14。
- 允许修改：`src/storage/worker.rs`、`migration.rs`、应用目录和 v1 SQL。
- 实现契约：
  - 创建 `StorageExecutor` 和存储命令协议。
  - SQLite 连接只能在线程内部创建。
  - v1 迁移使用事务和 schema 版本。
  - 数据库目录位于 `%LOCALAPPDATA%\ClipboardBoard\data`。
- 验收：迁移幂等；线程 ID 测试证明连接未跨线程。
- 风险等级：L3。
- 计划提交：`feat(storage): 建立单线程SQLite执行器`。

#### [ ] ATOM-16 实现文本事务 upsert

- 所属交付单元：UNIT-05。
- 唯一目标：文本记录可以按内容哈希事务写入或更新。
- 依赖：ATOM-15。
- 允许修改：存储命令、仓储内部实现和存储集成测试。
- 实现契约：
  - 首次内容插入新记录。
  - 重复内容更新时间、增加计数并保留收藏。
  - 事务失败不得产生半更新。
- 验收：首次、重复、同毫秒和事务回滚测试通过。
- 风险等级：L2。
- 计划提交：`feat(storage): 支持文本历史事务写入`。

#### [ ] ATOM-17 实现历史摘要游标查询

- 所属交付单元：UNIT-05。
- 唯一目标：通过复合游标稳定查询历史摘要。
- 依赖：ATOM-16。
- 允许修改：查询命令、仓储内部 SQL 和测试。
- 实现契约：
  - 排序使用 `copied_at DESC, id DESC`。
  - 后续页使用 `(copied_at, id)` 游标。
  - 提供按 ID 读取完整 payload 的命令。
  - 不使用大 OFFSET。
- 验收：相同毫秒记录不重复、不遗漏；首尾边界正确。
- 风险等级：L2。
- 计划提交：`feat(storage): 支持历史游标查询`。

#### [ ] ATOM-18 接入提交后可见和启动恢复

- 所属交付单元：UNIT-05。
- 唯一目标：SQLite 提交成为文本历史可见性的前置条件。
- 依赖：ATOM-17。
- 允许修改：历史协调器、启动恢复、UI 接线和集成测试。
- 实现契约：
  - 替换 ATOM-12/13 的内存直写路径。
  - 捕获内容先发存储命令。
  - 事务成功后才发 `UiEvent`。
  - 启动加载100条摘要缓存，UI 首批只放30条。
  - 完整 payload 按 ID 获取。
- 验收：数据库失败无幽灵记录；2万条数据库冷启动模型有界。
- 风险等级：L3。
- 计划提交：`feat(history): 接入持久化历史与有界恢复`。

### 第五阶段：键盘复制和自动粘贴

#### [ ] ATOM-19 实现键盘选择导航

- 所属交付单元：UNIT-06。
- 唯一目标：用户可以使用上下键选择记录。
- 依赖：ATOM-18。
- 允许修改：UI 选择状态、滚动定位和交互测试。
- 实现契约：
  - 面板打开默认选择第一项。
  - 上下键不越界。
  - 选中项自动保持可见。
  - 模型重置后选择安全恢复。
- 验收：空列表、单条、多条和结果重置测试通过。
- 风险等级：L1。
- 计划提交：`feat(ui): 支持键盘选择历史记录`。

#### [ ] ATOM-20 实现 Ctrl+Enter 仅复制

- 所属交付单元：UNIT-06。
- 唯一目标：选中文本记录后可只写回系统剪贴板。
- 依赖：ATOM-19。
- 允许修改：剪贴板 writer、预期写事务、UI 回调和集成测试。
- 实现契约：
  - 按 ID 从 StorageExecutor 获取 payload。
  - ClipboardIO 写入 Unicode 文本。
  - 写入前登记哈希和格式，写入后绑定精确 sequence。
  - 监听端一次性消费匹配事件。
- 验收：系统剪贴板内容正确；历史计数、时间和顺序不变化。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持仅复制文本记录`。

#### [ ] ATOM-21 实现 Enter 自动粘贴

- 所属交付单元：UNIT-07。
- 唯一目标：向面板打开前的普通权限目标自动粘贴。
- 依赖：ATOM-20。
- 允许修改：粘贴协调器、目标验证、焦点恢复和 Windows 集成测试。
- 实现契约：
  - 使用 ATOM-05 保存的目标会话。
  - Enter 时复核 HWND、PID 和完整性。
  - 同级目标才允许隐藏面板、恢复焦点并调用 `SendInput`。
  - 目标变化时不得向新前台窗口发送按键。
- 验收：普通记事本自动粘贴；失效或变化目标不收到输入。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持安全自动粘贴`。

#### [ ] ATOM-22 实现自动粘贴降级提示

- 所属交付单元：UNIT-07。
- 唯一目标：不能自动注入时可靠降级为仅复制。
- 依赖：ATOM-21。
- 允许修改：粘贴结果模型、提示 UI 和管理员目标测试。
- 实现契约：
  - 高完整性目标不调用 `SendInput`。
  - 焦点恢复或输入失败时提示“内容已复制，请按 Ctrl + V”。
  - 不误报自动粘贴成功。
- 验收：管理员目标和模拟失败用例均只复制并显示提示。
- 风险等级：L2。
- 计划提交：`feat(paste): 增加自动粘贴失败降级`。

### 第六阶段：搜索和历史管理

#### [ ] ATOM-23 实现存储层搜索和筛选查询

- 所属交付单元：UNIT-08。
- 唯一目标：按关键词、来源、类型和收藏查询历史。
- 依赖：ATOM-22。
- 允许修改：存储查询命令、SQL、筛选 DTO 和测试。
- 实现契约：
  - 所有查询由 StorageExecutor 执行。
  - 正确转义 `%` 和 `_`。
  - 结果继续使用复合游标。
- 验收：中文、英文、符号、来源、收藏、无结果和分页测试通过。
- 风险等级：L2。
- 计划提交：`feat(search): 支持历史搜索与筛选查询`。

#### [ ] ATOM-24 实现搜索防抖和旧结果作废

- 所属交付单元：UNIT-08。
- 唯一目标：快速输入时只展示最新查询结果。
- 依赖：ATOM-23。
- 允许修改：搜索协调器、generation 模型和并发测试。
- 实现契约：
  - 输入后等待120 ms。
  - 每次查询生成新 generation。
  - 迟到结果不得覆盖新结果。
- 验收：连续输入三个关键词时只展示最后一个。
- 风险等级：L2。
- 计划提交：`feat(search): 增加搜索防抖与结果代次`。

#### [ ] ATOM-25 接入搜索框和筛选标签

- 所属交付单元：UNIT-08。
- 唯一目标：用户可以输入关键词并切换基础筛选。
- 依赖：ATOM-24。
- 允许修改：搜索框、全部/文本/收藏标签和 UI 测试。
- 实现契约：
  - 输入即搜索。
  - 切换标签重置游标和选择。
  - loading、无结果和错误状态可区分。
- 明确不修改：图片标签在 ATOM-39 启用。
- 验收：搜索和标签结果正确，旧结果不闪回。
- 风险等级：L1。
- 计划提交：`feat(ui): 接入历史搜索和筛选标签`。

#### [ ] ATOM-26 支持收藏切换

- 所属交付单元：UNIT-09。
- 唯一目标：记录可以被收藏或取消收藏。
- 依赖：ATOM-25。
- 允许修改：存储命令、UI 回调和测试。
- 实现契约：先提交事务，再更新 UI；重复复制保留收藏。
- 验收：重启后状态存在；失败时 UI 不提前变化。
- 风险等级：L2。
- 计划提交：`feat(history): 支持收藏历史记录`。

#### [ ] ATOM-27 支持删除单条记录

- 所属交付单元：UNIT-09。
- 唯一目标：删除一条指定历史记录。
- 依赖：ATOM-26。
- 允许修改：删除命令、UI 行为和测试。
- 实现契约：事务成功后从 UI 移除；重复删除返回幂等结果。
- 验收：重启不恢复；失败时记录仍存在。
- 风险等级：L2。
- 计划提交：`feat(history): 支持删除单条记录`。

#### [ ] ATOM-28 支持清空未收藏记录

- 所属交付单元：UNIT-09。
- 唯一目标：确认后清空所有未收藏记录。
- 依赖：ATOM-27。
- 允许修改：批量删除命令、确认 UI 和测试。
- 实现契约：单个事务删除未收藏记录，收藏保持不变。
- 验收：取消无变化；确认后只保留收藏。
- 风险等级：L3。
- 计划提交：`feat(history): 支持清空未收藏记录`。

#### [ ] ATOM-29 支持强确认清空全部

- 所属交付单元：UNIT-09。
- 唯一目标：经过明确强确认后删除包括收藏在内的全部记录。
- 依赖：ATOM-28。
- 允许修改：全量删除命令、强确认 UI 和测试。
- 实现契约：默认清空操作不得隐式进入此路径。
- 验收：取消无变化；确认后数据库和 UI 均为空。
- 风险等级：L3。
- 计划提交：`feat(history): 支持确认后清空全部历史`。

### 第七阶段：图片捕获与展示

#### [ ] ATOM-30 建立图片持久化契约

- 所属交付单元：UNIT-10。
- 唯一目标：建立可迁移、可恢复的图片元数据和磁盘目录契约。
- 依赖：ATOM-29。
- 允许修改：v2 迁移、图片领域元数据、目录模块和迁移测试。
- 实现契约：
  - 增加图片路径、尺寸和格式字段。
  - 建立 original、thumbnail、staging 和 recovery 目录。
  - 原图路径按哈希前缀分片。
- 验收：v1 升级不丢文本；迁移重复执行安全。
- 风险等级：L3。
- 计划提交：`feat(storage): 增加图片记录迁移与目录契约`。

#### [ ] ATOM-31 定义规范图片哈希

- 所属交付单元：UNIT-10。
- 唯一目标：相同像素内容跨 PNG、DIB 和 DIBV5 得到相同哈希。
- 依赖：ATOM-30。
- 允许修改：图片领域模型、规范化和测试。
- 实现契约：
  - 顶向下。
  - 行连续。
  - RGBA8。
  - 哈希包含域标签、宽度、高度和像素。
  - 忽略编码元数据。
- 验收：同像素跨格式一致，不同像素或尺寸不同。
- 风险等级：L2。
- 计划提交：`feat(image): 定义规范图片内容哈希`。

#### [ ] ATOM-32 读取注册 PNG 格式

- 所属交付单元：UNIT-10。
- 唯一目标：从剪贴板安全读取注册 PNG。
- 依赖：ATOM-31。
- 允许修改：ClipboardIO 图片读取、PNG 解码入口和畸形数据测试。
- 实现契约：
  - 在关闭剪贴板前复制有界字节。
  - 校验 sequence。
  - 检查文件长度、像素数、尺寸乘法溢出和解码结果。
- 验收：有效、截断、超限、损坏和 sequence 失配用例通过。
- 风险等级：L3。
- 计划提交：`feat(image): 支持读取剪贴板PNG`。

#### [ ] ATOM-33 读取 DIB 和 DIBV5

- 所属交付单元：UNIT-10。
- 唯一目标：安全解析常见剪贴板 DIB 和 DIBV5。
- 依赖：ATOM-32。
- 允许修改：DIB 解析器、ClipboardIO 复制路径和测试。
- 实现契约：
  - 支持24/32位 `BI_RGB`。
  - 支持32位 `BI_BITFIELDS`。
  - 处理 top-down、bottom-up、行填充和 alpha。
  - 所有偏移与尺寸使用 checked arithmetic。
- 验收：颜色、方向、掩码、截断、溢出和畸形头测试通过。
- 风险等级：L3。
- 计划提交：`feat(image): 支持读取DIB与DIBV5`。

#### [ ] ATOM-34 增加 CF_BITMAP 回退

- 所属交付单元：UNIT-10。
- 唯一目标：缺少 PNG/DIB 时仍可读取 `CF_BITMAP`。
- 依赖：ATOM-33。
- 允许修改：ClipboardIO 位图读取、Win32 资源封装和测试。
- 实现契约：
  - 在 ClipboardIO 中调用 `GetDIBits`。
  - 复制为自有像素 DTO。
  - 不把 HBITMAP 交给 ImageWorker。
  - 正确管理本进程创建的 GDI 资源。
- 验收：回退成功且无句柄泄漏。
- 风险等级：L3。
- 计划提交：`feat(image): 增加CF_BITMAP读取回退`。

#### [ ] ATOM-35 建立耐久图片处理流水线

- 所属交付单元：UNIT-10。
- 唯一目标：把规范像素安全持久化为原图和缩略图。
- 依赖：ATOM-34。
- 允许修改：ImageWorker、编码器、staging 文件和测试。
- 实现契约：
  - staging 文件对本次捕获独占。
  - 生成 PNG 原图和最长边320px WebP。
  - 执行 flush、关闭、原子发布和发布后验证。
  - 不创建 Slint Image。
- 验收：中途失败不产生正式文件；低分辨率图片不被放大。
- 风险等级：L3。
- 计划提交：`feat(image): 建立耐久图片处理流水线`。

#### [ ] ATOM-36 提交图片记录并接入历史

- 所属交付单元：UNIT-10。
- 唯一目标：图片文件和数据库记录以一致顺序进入历史。
- 依赖：ATOM-35。
- 允许修改：图片持久化协调器、存储命令、删除回收队列和集成测试。
- 实现契约：
  - 文件发布成功后才提交数据库。
  - 数据库成功后才更新 UI。
  - 数据库失败只回滚本次拥有的文件。
  - 重复图片合并。
  - 单条删除和清空提交后排队回收无引用资产。
- 验收：无缺失原图记录；失败无幽灵卡片；删除后最终清理资产。
- 风险等级：L3。
- 计划提交：`feat(history): 接入持久化图片历史`。

#### [ ] ATOM-37 展示按需加载的图片卡片

- 所属交付单元：UNIT-10。
- 唯一目标：列表中显示图片缩略图且不长期解码不可见图片。
- 依赖：ATOM-36。
- 允许修改：图片卡片、缩略图加载器、像素 DTO 和 UI 测试。
- 实现契约：
  - 工作线程只产生可发送像素缓冲。
  - Slint Image 只在 UI 线程创建。
  - 图片卡片采用固定高度。
  - 缺失缩略图显示占位并请求重建。
- 验收：不可见图片不解码；快速滚动不阻塞 UI。
- 风险等级：L3。
- 计划提交：`feat(ui): 展示懒加载图片卡片`。

#### [ ] ATOM-38 支持复制和粘贴图片

- 所属交付单元：UNIT-11。
- 唯一目标：历史图片可以重新写回剪贴板并粘贴。
- 依赖：ATOM-37。
- 允许修改：图片 writer、预期写事务接线、粘贴协调器和集成测试。
- 实现契约：
  - 从耐久 original 读取。
  - 同一写事务提供注册 PNG 和 DIBV5。
  - 复用类型无关的自身写回抑制。
  - 接入 Ctrl+Enter 和 Enter。
- 验收：可粘贴到画图等程序；历史计数和排序不变化。
- 风险等级：L3。
- 计划提交：`feat(paste): 支持复制与粘贴图片`。

#### [ ] ATOM-39 启用图片筛选标签

- 所属交付单元：UNIT-08。
- 唯一目标：用户可以只查看图片历史。
- 依赖：ATOM-38。
- 允许修改：筛选枚举、存储查询和 UI 标签。
- 验收：全部、文本、图片和收藏结果正确，游标连续。
- 风险等级：L1。
- 计划提交：`feat(filter): 支持图片类型筛选`。

### 第八阶段：大历史分页和内存控制

#### [ ] ATOM-40 建立分页协调器

- 所属交付单元：UNIT-12。
- 唯一目标：统一普通、搜索和筛选列表的分页状态。
- 依赖：ATOM-39。
- 允许修改：分页协调器、游标状态和测试。
- 实现契约：
  - 首批30、后续50。
  - generation 隔离不同查询。
  - 防止重复页请求。
  - 明确 loading、has_more 和 error。
- 验收：切换查询后不混入旧页面；同页不重复。
- 风险等级：L2。
- 计划提交：`feat(history): 建立统一分页协调器`。

#### [ ] ATOM-41 接入滚动到底自动加载

- 所属交付单元：UNIT-12。
- 唯一目标：接近列表底部时自动加载下一批。
- 依赖：ATOM-40。
- 允许修改：ListView 回调、加载指示和交互测试。
- 实现契约：保持选择和键盘导航，不显示传统分页按钮。
- 验收：连续滚动无重复、遗漏或选择跳变。
- 风险等级：L2。
- 计划提交：`feat(ui): 支持历史无限滚动`。

#### [ ] ATOM-42 实现缩略图 LRU

- 所属交付单元：UNIT-12。
- 唯一目标：限制已解码缩略图和 UI 纹理数量。
- 依赖：ATOM-41。
- 允许修改：缩略图缓存、视口计算和缓存测试。
- 实现契约：
  - 默认只保留可视区及上下各10条。
  - 离开范围后释放 UI 纹理和解码缓冲。
  - 重新进入时按需加载。
- 验收：滚动500张图片时缓存容量保持有界。
- 风险等级：L3。
- 计划提交：`perf(image): 限制缩略图纹理缓存`。

#### [ ] ATOM-43 执行混合列表硬性能门禁

- 所属交付单元：UNIT-12。
- 唯一目标：证明最终文本和图片混合列表满足性能契约。
- 依赖：ATOM-42。
- 允许修改：混合性能场景、测量脚本和必要的原子内优化。
- 实现契约：
  - 10,000条文本摘要。
  - 10,000条图片摘要和代表性缩略图。
  - 使用最终卡片、分页和 LRU。
- 验收：达到内存、呼出、首批和滚动目标。
- 停止条件：失败必须切换窗口化 Model 并重新规划相关原子，不得拖到发布阶段。
- 风险等级：L2。
- 计划提交：`test(perf): 增加混合历史性能门禁`。

### 第九阶段：配置、隐私和清理

#### [ ] ATOM-44 建立配置工作线程

- 所属交付单元：UNIT-13。
- 唯一目标：设置可以可靠保存并从损坏中恢复。
- 依赖：ATOM-43。
- 允许修改：配置模型、SettingsWorker、默认值、原子保存和测试。
- 实现契约：
  - 配置位于 `%LOCALAPPDATA%\ClipboardBoard\config\settings.json`。
  - 同目录临时文件。
  - flush 后通过 Windows 原子替换发布。
  - 保留可恢复备份。
  - UI 线程不做文件 IO。
- 验收：缺失、损坏、未知字段、写入失败和备份恢复测试通过。
- 风险等级：L3。
- 计划提交：`feat(settings): 建立可靠配置存储`。

#### [ ] ATOM-45 支持暂停记录

- 所属交付单元：UNIT-13。
- 唯一目标：用户可以暂停读取剪贴板内容。
- 依赖：ATOM-44。
- 允许修改：记录策略、托盘菜单、计时状态和测试。
- 实现契约：
  - 支持5分钟、30分钟和无限期。
  - 在 ClipboardIO 读取正文前检查。
  - 定时暂停到期自动恢复。
  - 无限暂停跨重启保持。
- 验收：暂停期间文本和图片均未被读取或保存。
- 风险等级：L2。
- 计划提交：`feat(privacy): 支持暂停剪贴板记录`。

#### [ ] ATOM-46 支持排除指定程序

- 所属交付单元：UNIT-14。
- 唯一目标：被排除程序的复制内容不进入应用进程。
- 依赖：ATOM-45。
- 允许修改：排除规则、事件路由、配置快照和隐私测试。
- 实现契约：
  - 支持规范化完整路径和 exe 文件名。
  - Windows 大小写差异不影响匹配。
  - 在 ClipboardIO 读取正文前阻断。
- 验收：被排除程序的文本和图片均不进入历史，日志也不含正文。
- 风险等级：L3。
- 计划提交：`feat(privacy): 支持排除指定程序`。

#### [ ] ATOM-47 实现记录数量和保留天数清理

- 所属交付单元：UNIT-15。
- 唯一目标：普通历史按数量和保留天数自动收敛。
- 依赖：ATOM-46。
- 允许修改：StorageExecutor 清理命令、触发器和测试。
- 实现契约：
  - 启动完成后、插入后和配置变更后触发。
  - 删除最旧未收藏记录。
  - 不使用周期轮询。
- 验收：超过2000条或30天时收敛；收藏不被删除。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 清理过量和过期历史`。

#### [ ] ATOM-48 实现图片空间配额

- 所属交付单元：UNIT-15。
- 唯一目标：图片磁盘使用量自动回到配置上限。
- 依赖：ATOM-47。
- 允许修改：空间统计、配额清理命令和测试。
- 实现契约：
  - 统计 original 和 thumbnail。
  - 超限时淘汰最旧未收藏图片记录。
  - 记录删除提交后排队资产回收。
- 验收：最终空间不超过500 MiB；收藏图片保持。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 控制图片存储空间`。

#### [ ] ATOM-49 实现图片资产对账

- 所属交付单元：UNIT-15。
- 唯一目标：安全修复数据库和图片文件之间的非正常不一致。
- 依赖：ATOM-48。
- 允许修改：对账任务、StorageExecutor 屏障、缓存重建和测试。
- 实现契约：
  - 排除 staging、in-flight 和 recovery。
  - 删除前重新查询引用。
  - 缺失缩略图从原图重建。
  - 缺失原图记录隔离为不可用，不伪装可粘贴。
  - 启动后延迟执行，不阻塞首屏。
- 验收：不误删正在提交或共享哈希资产；异常状态最终收敛。
- 风险等级：L3。
- 计划提交：`feat(cleanup): 对账图片记录与磁盘资产`。

### 第十阶段：设置和窗口模式

#### [ ] ATOM-50 增加历史设置界面

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以配置历史和图片限制。
- 依赖：ATOM-49。
- 允许修改：设置窗口、历史设置 DTO、校验和测试。
- 实现契约：
  - 最大记录数。
  - 保留天数。
  - 图片空间。
  - 是否记录图片。
  - 是否记录来源程序。
  - 保存后立即触发相关策略。
- 验收：非法值被拒绝；重启后保留。
- 风险等级：L2。
- 计划提交：`feat(settings): 增加历史记录设置`。

#### [ ] ATOM-51 增加隐私设置界面

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以管理排除程序和隐私开关。
- 依赖：ATOM-50。
- 允许修改：隐私设置页面、排除列表 UI 和测试。
- 实现契约：
  - 添加和删除排除程序。
  - 查看与调整暂停状态。
  - 窗口标题记录默认关闭。
  - 改动应用到下一次事件。
- 验收：配置保存和运行时策略同步。
- 风险等级：L2。
- 计划提交：`feat(settings): 增加隐私设置`。

#### [ ] ATOM-52 支持安全修改全局快捷键

- 所属交付单元：UNIT-16。
- 唯一目标：修改快捷键失败时不丢失当前可用热键。
- 依赖：ATOM-51。
- 允许修改：热键设置 UI、注册事务、配置保存和测试。
- 实现契约：
  - 先注册新热键。
  - 注册成功后保存配置。
  - 保存成功后使旧热键失效。
  - 旧热键注销失败时 handler 忽略旧 ID，并在下次启动清除。
  - 注册或保存失败时旧热键继续工作。
- 验收：冲突、保存失败和成功重启测试通过。
- 风险等级：L3。
- 计划提交：`feat(hotkey): 支持安全修改全局快捷键`。

#### [ ] ATOM-53 支持开机启动

- 所属交付单元：UNIT-16。
- 唯一目标：用户可以控制当前账户登录时是否启动应用。
- 依赖：ATOM-52。
- 允许修改：HKCU 启动模块、设置 UI 和注册表抽象测试。
- 实现契约：
  - 使用当前用户 Run 项。
  - 正确引用包含空格的路径。
  - 操作幂等。
  - 不要求管理员权限。
- 验收：开启、关闭和重复操作正确。
- 风险等级：L2。
- 计划提交：`feat(startup): 支持当前用户开机启动`。

#### [ ] ATOM-54 支持固定窗口模式

- 所属交付单元：UNIT-17。
- 唯一目标：用户可以把临时面板切换为可长期停留的固定窗口。
- 依赖：ATOM-53。
- 允许修改：窗口状态机、固定按钮、拖动调整和尺寸配置。
- 实现契约：
  - 固定后失焦不隐藏。
  - 支持拖动和调整尺寸。
  - 保存合法尺寸。
  - 取消固定后恢复临时窗口规则。
- 验收：模式切换、重启恢复和非法尺寸回退测试通过。
- 风险等级：L2。
- 计划提交：`feat(window): 支持固定看板模式`。

### 第十一阶段：恢复和发布

#### [ ] ATOM-55 实现数据库损坏恢复

- 所属交付单元：UNIT-18。
- 唯一目标：数据库损坏时保留可恢复证据并启动新库。
- 依赖：ATOM-54。
- 允许修改：恢复协调器、存储启动、资产根切换和恢复测试。
- 实现契约：
  - 停止相关工作线程。
  - 把损坏数据库和整个图片资产根移动到时间戳 recovery 包。
  - 新数据库使用新的资产根。
  - 新库对账不得处理旧 recovery 资产。
  - 不自动删除 recovery 包。
- 验收：损坏库可被隔离；应用可启动；旧原图不会被误清。
- 风险等级：L3。
- 计划提交：`feat(recovery): 隔离损坏数据库与历史资产`。

#### [ ] ATOM-56 增加正式版本和图标资源

- 所属交付单元：UNIT-19。
- 唯一目标：Release 程序具有正确产品身份和图标。
- 依赖：ATOM-55。
- 允许修改：版本、Windows 资源、应用图标和托盘状态图标。
- 实现契约：Release 构建不显示控制台窗口。
- 验收：资源管理器、任务管理器和托盘显示正确。
- 风险等级：L1。
- 计划提交：`chore(release): 增加应用版本与图标资源`。

#### [ ] ATOM-57 建立发布性能回归

- 所属交付单元：UNIT-19。
- 唯一目标：自动判断 Release 构建是否满足性能预算。
- 依赖：ATOM-56。
- 允许修改：性能脚本、测试夹具和结果输出。
- 实现契约：
  - 采集待机内存。
  - 面板打开内存。
  - 呼出 P95。
  - 首批结果。
  - 搜索耗时。
  - 空闲 CPU。
  - 长滚动状态。
- 验收：全部指标达到第4节预算。
- 风险等级：L2。
- 计划提交：`test(perf): 建立发布性能回归`。

#### [ ] ATOM-58 创建 Inno Setup 安装程序

- 所属交付单元：UNIT-19。
- 唯一目标：生成不要求管理员权限的 Windows 安装包。
- 依赖：ATOM-57。
- 允许修改：Inno Setup 脚本、Release 构建入口和安装测试。
- 实现契约：
  - 当前用户安装。
  - 创建开始菜单入口。
  - 支持升级。
  - 卸载时由用户选择保留或删除应用数据。
- 明确不修改：不做代码签名和自动更新。
- 停止条件：缺少 Inno Setup 编译器且当前环境无法获得。
- 风险等级：L3。
- 计划提交：`build(installer): 增加Windows安装程序`。

#### [ ] ATOM-59 增加自动化发布冒烟测试

- 所属交付单元：UNIT-19。
- 唯一目标：自动验证 Release 二进制和安装包的核心路径。
- 依赖：ATOM-58。
- 允许修改：PowerShell 冒烟脚本、测试夹具和结果报告。
- 实现契约：
  - 启动。
  - 单实例。
  - 文本复制。
  - 重启恢复。
  - 搜索。
  - 清理。
  - 安装与卸载。
- 验收：测试可以重复执行且不会依赖手工遗留状态。
- 风险等级：L2。
- 计划提交：`test(release): 增加Windows发布冒烟测试`。

#### [ ] ATOM-60 完成发布验收矩阵

- 所属交付单元：UNIT-19。
- 唯一目标：形成正式版本发布决定所需的完整证据。
- 依赖：ATOM-59。
- 允许修改：验收脚本、测试夹具和中文发布验收记录。
- 验收范围：
  - Windows 10。
  - Windows 11。
  - 100%、150%、200% DPI。
  - 单屏和多屏。
  - Chrome、微信、VS Code、截图工具和画图来源。
  - 快速连续复制。
  - 管理员目标降级。
  - 剪贴板忙碌。
  - 数据库损坏。
  - 磁盘空间不足。
  - 图片缺失和损坏。
- 完成判定：所有必须项有通过证据；性能失败、数据丢失或错误窗口注入均阻止发布。
- 风险等级：L2。
- 计划提交：`test(release): 完成正式版本验收矩阵`。

## 8. 里程碑

### M0：技术骨架

- 原子范围：ATOM-01 至 ATOM-07。
- 结果：应用可以运行、通过热键打开、通过托盘退出，并具备线程和日志基础。

### M1：文本可用版

- 原子范围：ATOM-08 至 ATOM-22。
- 结果：文本历史、持久化、键盘选择、仅复制和安全自动粘贴形成完整闭环。
- 建议内部版本：`v0.1.0`。

### M2：文本完整 MVP

- 原子范围：ATOM-23 至 ATOM-29。
- 结果：具备搜索、筛选、收藏、删除和清空。

### M3：图片完整 MVP

- 原子范围：ATOM-30 至 ATOM-43。
- 结果：图片捕获、缩略图、粘贴、无限滚动和内存控制完成。
- 建议内部版本：`v0.2.0`。

### M4：隐私和设置

- 原子范围：ATOM-44 至 ATOM-54。
- 结果：暂停、排除、清理、设置、启动项和固定窗口完成。

### M5：正式版本

- 原子范围：ATOM-55 至 ATOM-60。
- 结果：恢复、性能、安装和发布验收完成。
- 建议版本：`v1.0.0`。

## 9. 逐原子执行记录模板

完成每个原子后，在本节末尾追加记录，不覆盖历史。

```markdown
### YYYY-MM-DD ATOM-XX 原子名称

- 状态：已完成 / 已满足 / 已阻塞。
- 完成内容：
- 实际修改文件：
- 明确未做：
- 针对性验证：
- 回归验证：
- 中文注释检查：
- 风险等级：
- DDD 结果：
- 本地提交：`<hash> <message>`
- 远端同步：未授权，未推送 / 已按用户授权推送。
- 计划偏差：
- 下一原子：
```

## 10. 计划变更记录

| 日期 | 版本 | 变更 | 原因 |
|---|---|---|---|
| 2026-07-28 | v3 | 首次落地60个原子任务、全局不变量、状态规则和执行记录模板 | 避免开发途中遗漏任务或破坏原子边界 |

